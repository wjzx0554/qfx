# 2.5.3.1分析工具进阶-技巧-数字格式

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E8%BF%9B%E9%98%B6/%E6%8A%80%E5%B7%A7/%E6%95%B0%E5%AD%97%E6%A0%BC%E5%BC%8F/content.html?timestamp=1775633256724

---

# 数字格式

分析工具的备选字段，可以设置数字格式。详见[数据分析与可视化/分析工具总体介绍/备选字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tlmhf2wv7#4.2)。

将备选字段创建为度量之后，这些分析字段也可以再单独设置数字格式。详见[数据分析与可视化/分析工具总体介绍/分析字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj#3.3)

在数据斗方设计器的图表属性中，针对数轴、标签等展示细节，还可以再设置数字格式。

以上这些，都是在数据分析与可视化工具中。如果往前看，在数据准备的定义阶段，也可以给数值型字段设置初始状态的数字格式。见[数据准备/定义阶段/操作概要](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#2.2)。

![[b4c8e5316b59ce62c0e2c6f535a3606f_MD5.png]]

数字格式弹窗

在这些地方进行设置，都会有类似上图这样的弹窗（部分场景未提供“自定义”）。

## 1 常规设置

**● 小数位数**

小数点后保留几位数。当显示精度小于实际精度时，按四舍五入处理。

**● 千位分隔符、万位分隔符**

分隔符可以二者选其一，也可以反勾选两者都不使用。

按照西方文化习惯，数字每三位分一段，分别为K（kilo-，即thousand，即千）、M（million，即百万）等单位。这就是千位分隔符，例如将数字显示为：123,456,789.00，可以方便阅读为 123M 456K 789。

按照中国的文化习惯，读数时每四位分一段，分别是万、亿等单位。这就是万位分隔符，例如将数字显示为：1,2345,6789.00，可以方便读为 1亿 2345万 6789。

**● 数量单位**

本质上这个属性是在移动小数点位置。常用的百分数（数量单位“百分之一”）相当于小数点向右移动两位，数字放大了一百倍；数量单位“万”相当于小数点向左移动四位，数字缩小为万分之一。因此，一般也需要在“后缀”补上单位名称。这其中，当选择百分之一或千分之一时，在“后缀”属性上会自动填上百分号或千分号；反之，当从百分之一或千分之一切换走，“后缀”属性上的百分号或千分号会自动擦除。

**● 前缀/后缀**

不难理解，就是数字格式化之后，在其前面或后面再拼接上固定的额外的字符。常见的做法，前缀可以写货币符号，后缀可以配合“数量单位”填写。

**● 负数**

默认情况下，负数就是比正数多了负号。另有几种负数的处理模式可供选择，例如不显示负号，又例如不显示负号且在前后缀外加上括号，再例如负号显示在前缀之前等。该属性的下拉列表在选项上直接显示了效果，一目了然。

**● 小数点符号、分隔符号**

当小数位数大于零时，可以输入小数点用什么字符；当勾选了分隔符时，可以输入分隔符用什么字符。

这两处通常不必理会，保持默认即可。但如有必要可以自行填上希望使用的其它符号。因为不同文化背景下，对数字格式的表示存在差异。小数点不一定总是点号（.），分隔符也不一定总是逗号（,）。

勾选万位分隔符，并将分隔符号改成空格，也是一种不错的阅读体验。

## 2 自定义

在提供了自定义数字格式设置的场景，从“常规”切换到“自定义”页签，可以在文本框中自主输入格式化串。

![[de0086efe0be72328c184fea370974aa_MD5.png]]

自定义数字格式

格式化串的规则如下所述：

① 用两个分号（;）隔开三段，分别表示正数、负数和零对应的格式。如果只有一个分号隔开两段，即省略了对零的描述，则零与正数相同格式。如果仅有一段没有分号，即省略了对负数和零的描述，则负数和零都是与正数相同格式（负数会自动补负号）。每一段又都遵循下列规则。

② 字符0表示一位占位的数字，实际不存在时补0；井号#表示一位数字但不强行占位，实际不存在时就不显示。其中，如`0.0`，夹在两个字符0中间的符号被识别为小数点；如`#,#`，夹在两个字符#中间的符号被识别为分隔符。小数点向前到分隔符之间有三位数字就是千位分隔符，四位数字就是万位分隔符。

③ 用一对花括号及当中的数字N，表示数值以10的N次幂为单位。简单理解就是十进制数字的小数点偏移，对应“数量单位”属性。花括号中是正数N时表示放大单位，读数变小，小数点向左移N位；花括号中是负数-N时表示缩小单位，读数变大，小数点向右移N位。例如`{-2}`是“百分之一”，`{4}`是“万”。

④ 前缀写在前面，后缀写在后面。注意除了前、后缀的内容之外，以上规则中所有的符号都是半角的英文字符。

设置界面上，在“常规”页签先做一些设置，再切换到“自定义”页签，可以看到相应的格式化串。以下是几个完整的格式化串的例子：

`#,##0.00` 两位小数，有千位分隔符。

`{-2}0.0%` 一位小数，百分之一为单位，百分号为后缀。

`${6}#,##0;-${6}#,##0` 正数和负数分别描述，负数的负号在前缀之前，其余和正数一致。没有小数，有千位分隔符，以百万为单位，美元符号为前缀。

格式化串的写法，与Excel的“单元格格式”有些类似，但又不完全一样。像分号分三段、0和#基本规则就是一样的；像百分数的表示方式、数量单位、万位分隔符、自定义的小数点等就不能一一对应。

  

 **示例：** 零不显示。

通常在交互分析过程中，不推荐这么表达，特别是当数据中有空值存在时，或者当交叉表交叉出不存在的单元格时（参考[立方体“稀疏”的插图](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T9sch2wps#2)），零和空有各自的语义，不应该混淆。但是在报表的应用场景，时常会要求将零抹去。那么利用三段式的自定义格式化串，在零的部分放空就能实现。

![[3a536d42c8a61765074db870f8a15a82_MD5.png]]

示例：零不显示