# 2.3.4.4数据分析-视图计算-按日期计算•累计

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90/%E8%A7%86%E5%9B%BE%E8%AE%A1%E7%AE%97/%E6%8C%89%E6%97%A5%E6%9C%9F%E8%AE%A1%E7%AE%97%E2%80%A2%E7%B4%AF%E8%AE%A1/content.html?timestamp=1775613167978

---

# 按日期计算·累计

## 1 概念

累计是[同环比](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tgssooyn7)之外，另一种依赖于日期维度的计算。

在分析模型中，按照日期维度的时间粒度的顺序，度量值从开头依次累加，就是“累计”。只在同一年份中累计，或者理解为跨年重来，则为“年内累计”。

更直白地表达，一月的累计值就是一月的度量值，二月的累计值就是一月加二月的度量值，三月的累计值就是一月加二月加三月的度量值……十二月的累计值就是从一月、二月一直加到十二月的度量值。这就是一个以月份为时间粒度的累计的例子。

累计是在立方体的单元上做加法运算。换个角度观察，依据日期维度，最后一项的累计值，相当于小计/总计在汇总计算方式设置为求和时的值。

## 2 设置

度量字段的设置菜单中，展开“按日期计算”设置项，与同环比并列的两个子项：累计、年内累计，开启后可以将度量字段展现为累计值。

![[bab493786b306f5d0646be3bfed67036_MD5.png]]

累计的设置

其中打勾的开关设置项，与同环比有相同的逻辑；按日期计算的约束，也与同环比一致，都不再赘述。

## 3 数据筛选

累计与同环比一样都是按日期计算，因此数据筛选时机是否延迟也是一样的。如果筛选器字段的时间粒度与日期维度的时间粒度相同时（指选项型筛选器，不包括日期范围型筛选器），筛选的发生会延迟到聚合运算完成且按日期计算结束之后。这样，即便由于数据筛选使中间断开几个期间，也不会影响到后续期间的累计值。

## 4 计算字段

累计的函数是`RUNNINGSUM(aggExp [,type])`。其中，参数aggExp通常是一个聚合函数。方括号括起来的参数type表示可以缺省，缺省时是可以跨年的累计，type用字符串"Y"则表示年内累计。在计算字段编辑窗口的函数说明里可以查到语法及参数说明，或者查看[公式系统/分析引擎特有函数](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ttcwdijj1#2)。

直接在度量字段上设置累计就可以，不需要创建计算字段写表达式。只有当计算需要组合，不能直接在度量字段的设置菜单上勾选时，才考虑用计算字段。例如，年内累计的同比增长率：`SAMEPERIODLASTYEAR(RUNNINGSUM(SUM([销售额]),"Y"))`。