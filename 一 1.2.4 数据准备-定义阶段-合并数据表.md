
# 1.2.4定义阶段-合并数据表
**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%87%86%E5%A4%87/%E5%AE%9A%E4%B9%89%E9%98%B6%E6%AE%B5/%E5%90%88%E5%B9%B6%E6%95%B0%E6%8D%AE%E8%A1%A8/content.html?timestamp=1774595148768

---
# 合并数据表

数据表的合并很类似于SQL查询语句中的“union”，就是将两个结构相同或相近的表上下拼接，这在 [数据准备/入门基础/基本概念](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tmwv6m6pb#3) 第3小节中已做了介绍。

怎样设置合并，见 [数据准备/定义阶段/操作概要](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#4) 第4小节。

## 1 再论合并

在数据表的合并设置中，字段名称相同，直接对接上，这很好理解。但是如果字段不同，会发生什么？

一种情况是错开。对应不上的字段，另一个表的所有行，该字段的值就是空。

![[cf19bf11a99731c40878705f5064a06f_MD5.png]]

合并数据表示例

另一种情况，是字段名称虽然不同，但表达的意思是一样的，各自的值也可以混合在一起。那么设置中可以手工选择对应。如下图中的例子，原来表中叫[订单日期]的字段，合并进来的表中叫[订单时间]，虽然命名不同，但是语义相同，类型也一样，应该选择对应。

![[4f5ad63a69f377db6e39cc1eaf971062_MD5.png]]

合并数据表手工选择匹配字段

多个表的合并需要逐次操作“合并数据表”。但是“修改合并状态”时会将全部合并源陈列在一起，此时可以修改字段的对应关系。表头上的删除按钮，可以将中间的某一次合并删除。

![[0656de7e806fafcab4dad9873d25d636_MD5.png]]

修改合并状态

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

对于“主键”类的字段，用于标识每一行数据的唯一性，在合并数据表之后，有可能不再唯一。对后续使用是否有影响，需要主观判断。

## 2 业务实体

以业务实体作为数据表的数据源时，常见的是实体封装了单据及其分录。在已创建的数据表列表中，实体表现成树形的层级结构。由于实体的特殊结构，系统仅支持实体与实体合并，非实体的普通单个表与单个表合并。

对实体进行“合并数据表”时，设置界面左侧是先确定分录的对应关系。然后选择每一行，多个表（单据头是一个表、每个分录又各是一个表）逐个在右侧设置它们的字段的对应关系。

![[9464e08ff538edb6541ad574e592df84_MD5.png]]

合并数据表——实体

对实体“修改合并状态”，界面是同样的模式。界面左侧第一行，每个合并源的单据头，有操作菜单按钮。

![[29f20c3b7a8b6e48e34d87f49ceede8b_MD5.png]]

修改合并状态——实体

另外，系统在自动匹配字段时，是按字段名称，而非字段显示名称处理的。所以有时候会看到字段显示名称不同，但仍被自动对应上。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

业务实体在合并之后，会自动多出一个[实体名称]的字段，用于区分每一行数据来自哪个实体。

## 3 自定义SQL

以连接数据库（包括当前数据中心）作为数据源，并以“自定义SQL”的方式创建的数据表，在“修改合并状态”弹窗界面上，表头有编辑按钮，可以对合并源重新编辑自定义的SQL语句。

![[96a29e98c5c85d49b6b9be2ac7ee6692_MD5.png]]

修改合并状态——自定义SQL

## 4 合并发生的时机

数据表合并发生的时机，是在“数据准备”的数据提取阶段。系统按照设置中各个合并源分别取数，然后统一写入同一数据表中，以供后续分析使用。

各个合并源（非实体）可以来自不同的数据源，因此，跨系统、跨数据库、跨类型的数据，都有机会合并在一起。

结合“数据准备”的原理理解，数据提取每次都是将目标数据从源头（通常是数据库）读取并重新写入轻分析存储，不存在增量数据的问题，对于合并数据表是一样的道理。

## 5 适用场景

业务A与业务B有各自对应的功能模块，数据当然也是分别存储。它们中有共性的部分数据，可能需要合并在一起进行分析。

以前使用了其它品牌的业务系统，现在使用了金蝶云。某一业务的数据，希望将以前的历史数据与现有的数据合并分析。

用Excel手工记录数据，每个月一个文件（或者一个Sheet页签）,需要对它们一起进行分析（文件类数据源需要逐次上传数据文件且历史不可更改）。

以上举例的这些，可能是合并数据表的适用场景。