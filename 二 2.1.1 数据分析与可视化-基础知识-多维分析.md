# 2.1.1基础知识-多维分析

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/%E5%A4%9A%E7%BB%B4%E5%88%86%E6%9E%90/content.html?timestamp=1774595294832

---
# 多维分析

在业务系统中，通常业务发生则记录一笔数据。将大量的、零散的数据以某种或某些角度分类统计，这是分析工具要解决的问题。

数据是以“数据表”抽象表达的，通常“数据表”表示业务对象，一“行”就是一笔记录，一“列”（字段）则表示业务对象的一个属性。这在 [数据准备/入门基础/基本概念](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tmwv6m6pb) 里已有介绍。

![[70bf54b0cbbe7b9f6ac468032f23e4a0_MD5.png]]

明细业务数据

## 1 聚合运算

明细业务数据，按不同的角度分类统计，这个计算过程，需要将多行明细数据合并成一行。对于数值来说，将多个值合并在一起，就是“聚合运算”。以什么算法合并，就是“聚合运算方式”，或简称为“聚合方式”。常见的聚合方式有：求和、求平均值、找出最大或最小值、数一数有多少个（即计数）等等。

![[7ddab8b743e3e87973af388aabe903d4_MD5.png]]

聚合运算

## 2 维度、度量

上述的统计计算，更为具体的描述，是分类字段的值相同，表示它们是同一类，这些“行”就合并在一起成为一“行”。其中，用于分类的字段，相同的值合并之后仍然保持同一值；用于统计的字段，合并就必须指定聚合方式。

这些分类的字段，称为“维度”；聚合运算的字段，即目标统计字段，则称为“度量”。

![[70d54297eb88c235a7a61000b86d682c_MD5.png]]

维度与度量

维度字段的每个值称为“维度成员”。

![[de3db01e2b2d49a612fbda61dd16bbbf_MD5.png]]

维度成员

## 3 立方体模型

分类的字段“维度”有多个，它们组合在一起，或逐级细分，或研究它们之间数据的相关性，这就是“多维分析”。

常用“立方体”来表示多维分析的数据概念模型。

以从立方体某一个顶点出发的三条边为轴，每个轴表示一个维度。各个维度的维度成员交叉出来的小块，称为立方体的一个“单元”。度量值就是这个单元对应的值。

![[c67a7709e7dc8ceeebc9826bbd382bd7_MD5.png]]

立方体模型

世界是三维的，因此用三维的立方体来表示概念模型，一方面是便于理解，另一方面是表示“多”个维度。但这并不意味着维度总是三个。实际上，0、1、2、3、4、5……都可以是维度的个数。

## 4 多维分析与轻分析

“多维分析”及以上“立方体模型”、“维度”、“度量”等都是行业通用的概念，但“轻分析”不是。“轻分析”是金蝶云·苍穹的数据服务平台的一个产品名称，是品牌。轻分析正是要解决多维分析的问题，这在 [轻分析入门/简介](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Txdser612) 一文中已有阐述。

在多维分析中，有“切片”、“切块”等概念，这是固定立方体中某些维度的维度成员，减少这些维度以便于进一步的展现。有“旋转”的概念，是从不同方向观察数据的意思。在轻分析中，为降低理解难度，不直接出现这些概念。轻分析在产品上表现为将字段放置在表格的行、列方向自由组合（特指“数据分析”工具），将字段放置在筛选器区域等等。

在多维分析中，有“下钻”、“上卷”的概念，是指向更细或更粗粒度的维度级别观察数据。在轻分析的“数据分析”工具中，是通过自助的人机交互，添加、移除字段来实现，表格中的小计也是相关的功能；“数据斗方”的部分支持钻取的图表，可以在设计器中添加可钻取字段，这样发布后的卡片上就可以沿着设置好的钻取路径逐级下钻或返回。另外，仪表板中可以用页签控件组合多个数据斗方组件，设置图表联动和页签切换，来实现跨图表钻取。

## 5 再论维度与度量

### 5.1 与数据类型的关系

在轻分析的分析工具中，字段的数据类型已抽象为文本、日期、数值三类。能不能这么认为，文本型、日期型的字段就是维度，数值型的字段就是度量？不是。数据类型是字段的概念，维度、度量是多维分析的概念，两者没有必然的联系。多数情况下，文本型、日期型的字段会作为维度，数值型的字段会作为度量。但是反过来，文本型、日期型的字段作为度量（聚合方式是计数）也可以，数值型的字段直接、或者加工处理（例如分段）之后作为维度，也完全没有问题。

### 5.2 日期字段

日期型字段在多维分析中，是较为特殊的存在。如果作为维度，必须指定时间粒度，例如是按“年”分、按“季度”分、按“月”分，还是按“日”分等等。如果作为度量，聚合方式除了和文本型一样，有“计数”、“去重计数”之外，还可以有数值特征的“最大值”、“最小值”。

### 5.3 属性

在多维分析中，与维度、度量会同时出现的另一个概念是“属性”。它是与某一维度对应的另外一个字段，通常该维度的每一个确定的维度成员，该字段都有确定的值与之对应。例如将[员工姓名]字段作为维度，[学历]字段作为属性，员工“张三”的学历属性是“大学”。在轻分析中，为降低理解难度，不出现“属性”的概念（注：部分图表有相关的逻辑，表现为“维度”）。

## 6 与数据库有关吗？

### 6.1 与关系数据库

关系数据库的SQL查询语句，使用“group by”子句及其相关的语法，就是分组汇总的聚合运算。这与上述“聚合运算”的概念是一致的。

轻分析在技术实现上不依赖于任何数据库，是独立实现的一种内存计算。实际上数据库仅作为数据源，为“数据准备”时定义的“数据表”提供明细业务数据（相关原理见：[数据准备/入门基础/原因与原理](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tiv1ur2u0#2)）。

但是，在数据库层面进行聚合计算（此处指“数据准备”的“自定义SQL”）也并非毫无用处。一种场景是仪表板用于大屏展现，对响应速度有较高要求，而仪表板往往又是同时需要呈现的内容很多。此时如果用“自定义SQL”提供数据，在SQL中提前按展现目标进行了聚合运算，再结合“数据准备”的“定时预提取”功能，将会得到很好的改观。另一种场景是计算的复杂度很高，例如需要依赖聚合运算结果挑选数据，再进一步聚合运算。如果分析工具的能力未覆盖到，依赖“自定义SQL”提前先做一次聚合和筛选，可能是变通的技术方案。

### 6.2 与多维数据库

多维数据库是基于多维度立方体模型的数据库，本身就是支持多维分析的，它在数据的记录过程中也在进行聚合运算。注意它是一种数据库，这意味着数据的写入能力，而轻分析的分析工具对数据总是只读的。

另外，多维数据库产品，对于维度往往表现成“父子维”，这是一种树状的结构（关系数据库中“id”与“parentId”两个字段的设计就是一种用来表示树状结构的方式）。多维数据库关注的是数据在父子维上的向上汇总并入库。从每天汇总到月份，再汇总到年。从小部门汇总到大部门，再汇总到公司，再汇总到集团。这一点与通用分析工具不同。