# 3.2.2.12 设计器-组件-下拉列表

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E4%BB%AA%E8%A1%A8%E6%9D%BF/%E8%AE%BE%E8%AE%A1%E5%99%A8/%E7%BB%84%E4%BB%B6/%E4%B8%8B%E6%8B%89%E5%88%97%E8%A1%A8/content.html?timestamp=1775635238124

---

# 下拉列表

“下拉列表”组件是仪表板的筛选器之一，与数据类型为文本型的字段对应。

## 1 固定的高度及文字标签

作为“单行控件”型的筛选器组件，下拉列表组件的高度是固定不变的。组件在设计区中被选中，操作框的上下边调节块是灰色的，不允许拖拽调整高度（[仪表板/设计器/总述/设计区操作](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tmexfunuy#3)）。在属性设置区，“高度”属性编辑框是灰色的不可编辑状态。高度禁止修改的同时，当组件参与页面缩放时，高度也是锁定的（[仪表板/设计器/总述/页面缩放规则](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tty8t2pc0)）。

![[b05e5ac2d9154aadd80798cbd3e4b3a7_MD5.png]]

下拉列表

什么是“单行控件”型的筛选器？是指筛选器组件对应的控件，是一个单行的输入框。除了下拉列表组件，弹窗选择器组件、日期范围组件、日期-年组件、日期-年季组件、日期-年月组件、日期-年月日组件等都是这种类型。它们除了高度固定的特征之外，还可以自带一个文字标签。

标签可以选择位于“左侧”或者“上方”。当选择“左侧”时，还必须设置标签的宽度，这个宽度使得当页面缩放时，文字标签是固定不变的，只有下拉列表控件宽度伸缩。当选择“上方”时，组件的高度变成原先的2倍，一半给标签，一半给控件。高度依然是不能调整。

## 2 外观

“文字缩放”属性允许将显示在界面上的文字（包括标签文字及筛选器本身的显示文字，不包括下拉选择或弹出选择界面）在100%至150%之间六个档位调整。

## 3 备选值

下拉列表组件的备选值，可以选择“引用”或“自定义”。

“引用”即是绑定字段。数据可以来自某个可视化内容组件（数据斗方或数据分析），引用了它背后的数据源（通常是一个业务主题）。

这里一定要注意，仪表板中的筛选器组件，备选值的设置，引用了数据斗方组件，是引用了它背后的数据源，与数据斗方组件的方案以及方案的执行是无关的。所以，在数据斗方组件的方案中，添加计算字段，在备选值设置这里，也不可能出现。

“引用”的设置，还可以点开“高级设置项…”，绑定“显示字段”及“排序字段”。在不绑定显示字段时，备选项的值和显示文字是一样的，都是由主字段提供。特殊情况下，例如希望备选项的值是由[编码]字段提供，而显示出来的文字又是由[名称]字段决定，则可以分别绑定。“排序字段”道理相同。

备选值的“自定义”，是指手工填上固定值。设置界面上，有“值”、“显示值”两列，它们与绑定字段的“字段”、“显示字段”是一样的逻辑。这里两列都是必填的，一个影响值传递，一个影响本身的显示。

## 4 默认选中项

“默认选中项”属性定义了筛选器的初始状态。可以让“第一项”选项，或者指定任何一个选项作为初始选择。

有一种情况可能会造成理解上的偏差。由于设计器是所见即所得的，在设计区中筛选器可以直接起作用（在设置完联动之后），这种使用状态，并不会随着仪表板保存。所以，下一次打开，或者发布之后的执行，初始状态是以“默认选中项”属性设置为准的。

## 5 联动

联动包含了“作用于”和“页签切换”。

“作用于”是筛选器能够起作用，影响目标组件的必要设置。而“页签切换”就是将文本型筛选器当成页签导航按钮。

当创建下拉列表组件，并设置其备选值绑定字段时，系统自动为下拉列表组件添加了一条“作用于”目标，使其用相同字段影响备选值引用的组件。如果这不是设计的目标，在“作用于”属性的设置弹窗中，将其删除即可。

文本型筛选器可以“作用于”另一个文本型筛选器，这就形成了筛选器的级联。通常有层级关系的字段，可以做成级联，例如[省]、[市]、[县]三个字段。层级关系也意味着包含关系，那么级联的最后一个筛选器联动图表就可以了，前面的筛选器无须联动图表。即“省”作用于“市”，“市”作用于“县”，“县”作用于数据斗方组件。当然，前提是末级筛选器没有勾选“携带选项‘全部’”。

可以被下拉列表“作用于”的目标组件:

|下拉列表联动“作用于”的目标组件|作用方式（●表示支持，×表示不支持）|   |
|---|---|---|
|绑定字段作为筛选器|参数|
|数据斗方|●|●|
|数据分析|●|●|
|轻报表|×|●|
|网页|×|●|
|文字、多行文字|×|●|
|文本型筛选器|●|×|

联动的整体介绍，参考：[仪表板/设计器/组件间的联动](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzwez4pxh)。