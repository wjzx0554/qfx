# 2.5.1.2分析工具进阶-原理-立方体

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E8%BF%9B%E9%98%B6/%E5%8E%9F%E7%90%86/%E7%AB%8B%E6%96%B9%E4%BD%93/content.html?timestamp=1775633113474

---

# 立方体

多维计算模型，也俗称为立方体模型。

通常提及“分析引擎”时，对应分析工具整个处理过程，是宏观上的统称。更为细分来看，其中关键的一个阶段，由“多维计算引擎”来完成。多维计算引擎访问明细业务数据，生成立方体模型。

![[e5b3ec6ee4c0845b92d448d5d75fdf22_MD5.png]]

分析工具的内部过程

因此，对于分析过程的理解，根本上是对立方体模型的理解。对于分析工具所表现出来的现象的理解也是如此。

立方体模型在[数据分析与可视化/基础知识/多维分析](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ty374iylb)已有介绍，下面将结合分析工具，再做一些补充。

![[c67a7709e7dc8ceeebc9826bbd382bd7_MD5.png]]

立方体模型

## 1 再论聚合

假如有一个字段[金额]，拖拽它创建了分析字段，设置为度量，聚合方式为求和，这是一种用法。另一种用法，也可以创建一个计算字段`SUM([金额])`，并拖拽使用它生成分析字段。这两种用法，推荐的是第一种，因为简单的聚合运算没必要创建计算字段，用起来又麻烦，效率又低。这里将两种用法放在一起，只是为了说明它们效果是相同的，为了进一步来理解聚合运算。

那么，如果在计算字段中写上`SUM([金额])`，它是什么意思，是把哪些值加起来了呢？这就需要理解“维度”了。

聚合运算是在维度明确的基础上进行的。

仍以上述[金额]为例，假如分析模型中没有维度，那么求和就是整份数据的[金额]字段加起来一个值。

假如分析模型中有[地区]维度，它的维度成员有“广东”、“湖南”等等，那么聚合运算就是对地区为“广东”的那些数据，[金额]字段加起来是一个值；对地区为“湖南”的那些数据，[金额]字段加起来又是一个值。

再假如分析模型中有[地区]和[月份]两个维度，那么聚合运算就是对“广东”、“一月”的那些数据，[金额]字段加起来是一个值；对“广东”、“二月”的那些数据，[金额]字段加起来是一个值；对“湖南”、“一月”的那些数据，[金额]字段加起来是一个值；如此等等。

这就是立方体模型，一个维度相当于X、Y、Z中的一个轴，一个维度成员相当于轴上一个刻度。各个维度的不同维度成员组合起来，就形成了立方体模型中的众多单元。聚合运算就是看每一条业务数据落在哪个单元中，每个单元中如果落入多条业务数据则合并成一个信息。所谓的合并成一个信息就是求和、求平均值等聚合方式。

## 2 稀疏

人类生活在三维的世界中，因此多维计算模型也就形象地描绘成了三维的立方体。实际上，维度的个数可以任意多个。

而最终计算结果的可视化表达，又是在二维的平面上，以表格的方式来呈现（这里特指“数据分析”工具的可视化视图；数据斗方的图表理论上相同，但大多不是表格的形式，暂且忽略不论）。

表格视图，[分组表](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ts7nzn503#4)的模式，所有维度呈现在一个方向上，体现了“分组”的特征，即“大”维度套“小”维度；[交叉表](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ts7nzn503#4.3)的模式，维度呈现在两个方向上，各自形成分组，结合一起又主要体现出“交叉”的特征。

来看下面的例子。[省份]和[产品子类别]两个维度形成了交叉表，在“贵州”的“桌子”、“西藏”的“书架”等单元格上没有度量值的显示，当鼠标移到它们上面时不出现[详细信息](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ticvinc3g#1)。这是由于这些维度成员的组合没有对应的业务数据。

![[1004419cbdaa84b6644b8887cdac0a9f_MD5.png]]

“稀疏”的立方体呈现为交叉表

追溯原因，立方体在计算过程中，没有出现的数据也不会生成计算单元，这有利于更好地节省内存。立方体的这一特性，称为“稀疏”。但是，由于表格对两个维度的交叉，表格上的这些单元格不得不出现，于是显示了空白。

![[ff2208399cf8ad8e843305c474301415_MD5.png]]

“稀疏”的立方体模型

如果把维度[产品子类别]从“列”移到“行”上，变成分组表，这时的视图，很明显地说明了问题。“贵州”的“桌子”、“西藏”的“书架”等这些组合是不存在的。

![[1cfdb56ec2c1e42febbff7615f8b0fcd_MD5.png]]

“稀疏”的立方体呈现为分组表

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

表格的单元格显示为空白，原因不一定总是连一行数据都没有。也有可能聚合计算之后度量字段的值为空（null），或者数字零被人为地格式化成空白。但是对于后两者，在交叉表中，鼠标移上去会出现详细信息，也可以查看到明细数据；在分组表中，维度组合也会存在。

## 3 稀疏的影响

从原始业务数据聚合运算生成立方体模型，再从立方体模型生成可视化视图。原始数据 → 立方体 → 表格，这是从数据到可视化的链条。反过来说，视图依赖了立方体模型，立方体模型又依赖了原始数据。

当有一些具体的维度组合没有业务发生时，立方体就是稀疏的。而稀疏的立方体也可能导致视图上的部分“空缺”。就像上一小节中的例子，在一段时间内，贵州没有卖出一张桌子、西藏没有卖出一个书架，在交叉表中就存在空白的单元格。

再看另外一种类型的情况，表现就可能没有那么自然。

“按日期计算”的特性，包括了同环比和累计，这是聚合运算之后，在立方体模型上再进行的一轮计算。如果某一期没有数据，则这一期的立方体单元就不存在。虽然它的上一期（或去年同期）是有数据而存在的，但它自身不存在，就没办法进行计算。因此，交叉表的视图上，如果有“上期值”、“累计值”之类的显示，就会空缺。

![[1e4124ba51b5feeb13bf81ec2dea2b8f_MD5.png]]

“稀疏”的立方体影响上期值显示

![[69f546034995677face4e47aed12f14a_MD5.png]]

“稀疏”的立方体影响累计值显示

如果是以制作固定格式的报表为目标，这种情况会显得很不自然。所以，这也是在[数据分析与可视化/数据分析/概述/适用性](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tnki6bckh)中，强调不应该把“数据分析”工具当成报表开发工具看待的原因之一。

## 4 特殊的折线图

在众多的图表中，折线类的图表（包括折线图、面积图等）对于日期维度的表达有特殊性。考虑到折线图通常结合日期维度作为横轴来呈现趋势，这时候表示时间的横轴要保证完整性，才能不影响折线的斜率。因此，即使立方体是稀疏的，到了折线图的日期维度的横轴上，仍然保留了位置。

![[1a311d72abd9b74cb0c68fee0c994f53_MD5.png]] ![[fa7fbe4ab2225678a556f1ac59df23a5_MD5.png]]

折线图的日期维