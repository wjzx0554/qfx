# 2.5.3.2分析工具进阶-技巧-日期格式

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E8%BF%9B%E9%98%B6/%E6%8A%80%E5%B7%A7/%E6%97%A5%E6%9C%9F%E6%A0%BC%E5%BC%8F/content.html?timestamp=1775633300787

---

# 日期格式

## 1 格式化

数据类型为日期型的值，在系统内存中可能是某种形式，但最终显示出来给人看，必须是文本（字符串）。将日期值转换成字符串，称为日期的格式化。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

在数据准备中，区分“日期”和“日期时间”两种类型。在数据分析与可视化工具中，统一描述为日期型，但允许带有时分秒。根本上是一样的。

日期如何显示，是由“格式化串”决定的。格式化串是一个描述显示格式的字符串。

假设给某日期信息设置一个格式化串`yyyy-MM-dd`，例如日期值“1997年7月1日（可能还有时分秒）”，将会显示为文字：1997-07-01。在格式化串中，y、M、d这些字符，具体是什么意思，详见下文的标识字符说明。

在进行格式化时，有特定涵义的标识字符，将会被替换为特定的内容，其它没有特定涵义的字符，将会保持不变。

## 2 标识字符说明

|标识字符|描述|
|---|---|
|yy  <br>yyyy|2位或4位数字的年。|
|q|1位数字的季度。|
|M  <br>MM|M表示1位或2位数字的月，例如：1、12；  <br>MM表示总是2位数字的月，例如：01、12。|
|MMM  <br>MMMM|语言相关的月份。  <br>在英文中，3个M是缩写，例如：Oct；4个M是完整拼写，例如：October。  <br>中文则无差别，都是：十月。|
|d  <br>dd|d表示1位或2位数字的日，例如：1、17；  <br>dd表示总是2位数字的日，例如：01、17。|
|H  <br>HH|H表示1位或2位数字的小时，例如：9、18；  <br>HH表示总是2位数字的小时，例如：09、18。  <br>大写H是24小时制的每天几点，取值范围0~23。|
|h  <br>hh  <br>a|小写h是12小时制的每天几点，取值范围1~12。  <br>同时可以配合a显示：上午、下午。|
|m  <br>mm|m表示1位或2位数字的分钟，例如：0、59；  <br>mm表示总是2位数字的分钟，例如：00、59。|
|s  <br>ss|s表示1位或2位数字的秒，例如：0、59；  <br>ss表示总是2位数字的秒，例如：00、59。|
|E  <br>EEEE|语言相关的星期几。  <br>在英文中，1个E是缩写，例如：Sun；4个E是完整拼写，例如：Sunday。  <br>中文则无差别，都是：星期日。|

## 3 使用场景

### 3.1 日期维度

将日期型备选字段作为分析字段，并设置为维度，这是日期维度。日期维度默认的时间粒度是“年”，可以修改为“年月”等其它粒度。可回顾[数据分析与可视化/分析工具总体介绍/分析字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj#3.1) 3.1小节对维度、度量的描述。日期维度支持设置“日期格式”，这在上述链接的3.2小节也有阐述。

在此处设置中，从“系统默认”切换为“自定义”，填写上日期格式化串，可影响日期维度的显示结果。

![[8384bcdbd2a98ea8159e74fe6deffb52_MD5.png]]

设置日期维度的显示格式

需要再次强调，这是日期维度，而不是原始业务数据的日期字段。在这里设置的格式化串，必须与维度的时间粒度匹配，无关的标识字符都会被当成普通字符处理。以时间粒度“年月”为例，每一个维度成员的信息中，只有年份、月份是有效信息，日、时分秒等，或许可以取得到1号、0时0分0秒，但都是无效信息。因此，假如设置了`yyyy-MM-dd`，dd代表的日是无效信息，就会显示成“2025-08-dd”之类的格式，dd和横杆一样被当成普通字符原本输出了。

![[8d30fb84164b1a62783e935aa0089c22_MD5.png]]

日期维度的时间粒度与格式化的有效信息

### 3.2 计算字段

在计算字段中，使用FORMAT()函数，将日期型字段的日期值转换成字符串，是日期格式化的另一种使用场景。这种用法，可以让日期型字段转变成文本型字段。例如计算字段：`FORMAT([日期字段], "yyyy-MM")`。

以文本型字段作为维度，从分类统计的效果上看，没有差异。但是，从技术角度上看，失去了日期维度的特征，也就不支持同环比等按日期计算的相关功能了。这一点需要注意。

## 4 排序相关

如果是日期型字段作为日期维度，不管格式化显示成什么样子，维度成员有日期对象存在，顺序可以按时间序得到有效保障。

但是如果是使用FORMAT()函数格式化成字符串，或者数据来源就是文本型，当需要排序时则是以字符串本身作为排序依据。这就涉及到计算机技术相关的字符串排序的知识点了。

字符串的排序，是逐个字符进行比较，以字符内码判断大小。

一个典型的例子，“1月”~“12月”，十二个字符串进行排序，结果顺序是"10月”、“11月”、“12月”、“1月”、“2月”、“3月”……

在字符串中，10看起来是十，但实际上是幺和零两个字符，没有数字的涵义。

![[12f04cf712ad18b1b2cc6edebbeebf72_MD5.png]]

字符串排序示意图

因此，这个时候最简单有效的做法，FORMAT()函数的格式化串不要用M表示月份，而是用MM。这样会统一成两位数的月份，顺序就能够正确了。“01月” < “09月” < “10月”，只是视觉上不够自然。

另外也有其它做法。用于显示的字段保持不变，但同时需要另一个字段作为排序依据。排序依据的字段可以是数字类型，也可以是确保顺序正确的字符串，没有限制。如果是明确的、可枚举的少量几个值，也可以手动调整顺序。具体可参考[数据分析与可视化/分析工具总体介绍/备选字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tlmhf2wv7#4.4) 4.4小节，备选字段的自定义顺序。