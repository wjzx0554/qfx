# 2.5.3.3分析工具进阶-技巧-条件计算

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E8%BF%9B%E9%98%B6/%E6%8A%80%E5%B7%A7/%E6%9D%A1%E4%BB%B6%E8%AE%A1%E7%AE%97/content.html?timestamp=1775633337035

---

# 条件计算

不管是数据分析，还是数据斗方，在可视化展现之前，总是会根据维度、度量的定义，对数据进行聚合运算。在这个过程中，如果目标不是整份数据，而是有针对性地挑选出部分数据来，应该怎么做？

熟悉Excel者，可能会想到SUMIF()之类的函数，因此会先入为主地在计算字段中找寻相同的函数，碰壁是必然的结果。分析工具不是Excel这种电子表格类型的工具，就像仪表板不是PPT演示文稿一样，不能用习惯性思维来思考。

在分析工具中，条件计算的首选方式是用筛选器做数据筛选，这是最为正确的方式。少部分用筛选器无法达成的目标，才考虑使用计算字段。

## 1 数据筛选

筛选器的详细介绍，见[数据分析与可视化/分析工具总体介绍/筛选器](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tk4lat0ic)。另外，“数据分析”工具中，筛选器的使用，参考[数据分析与可视化/数据分析/视图上的操作/数据筛选](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tpltwmoqj)。

简而言之，筛选器与字段对应。选项型筛选器，对备选值直接勾选表示保留该选项的数据，不勾选则被过滤掉不参与计算；数值范围型、日期范围型筛选器也都清晰明了，范围内是保留的部分，范围外被过滤掉。

对于常用的选项型筛选器，如果选项特别多，不容易一目了然，在弹窗模式中，可以在搜索栏中用“包含”、“开头是”、“结尾是”等方式，输入字符进行搜索，查找到选项后再勾选或反勾选。如果同一字段有多个条件，可以先搜索出一批，勾选，再搜索出另一批，再勾选，这就叠加上去了。

以勾选为准，结合自行搜索，决定权交给了最终的阅览者。这样灵活的方式，在基于交互式分析为目标的“数据分析”工具中会很有用。在数据斗方设计器中，沿袭相同的模式也没什么问题；而且图表卡片发布之后，开放给阅读者的筛选器也是有逻辑一致的表现。

 **示例一：** 柱形图对比各地区的复印机和传真机加一起的利润。实现方式是筛选出[产品名称]中带有“复印机”和“传真机”的数据。

（1）柱形图，以[地区]作为维度，放在横轴；以[利润]求和作为度量，放在纵轴。[产品名称]作为筛选器，如果是在“数据分析”工具中可以切换为“多选弹窗选择器”模式，当前是数据斗方设计器，本身就是弹窗模式。

![[873bd39a8f5e4ce5baf0630658c7bcd6_MD5.png]]

数据筛选 示例一步骤（1）

（2）在[产品名称]筛选器弹出窗口，反勾选“全选”清除全部选项，并将搜索模式切换为“仅搜索，不影响选中状态”。必须切换为“仅搜索”，否则不能多次搜索组合条件。对于早期版本，此处没有选项，本来就是仅搜索状态，注意版本差异。

![[4a4c86c4c9f151d6aa9ba06843c8617c_MD5.png]]

数据筛选 示例一步骤（2）

（3）在搜索栏输入“复印机”，点击“全选”将全部名称包含复印机的选项勾选。

![[3a444ec867dbfbf6da225a2156e5edaa_MD5.png]]

数据筛选 示例一步骤（3）

（4）在搜索栏输入“传真机”（由于模式为“仅搜索”，再次搜索不会自动清除刚刚的选中），再点击“全选”将全部名称包含传真机的选项也勾选。此时勾选上的就是复印机和传真机。

![[707fa1fdcb28526b976ad3b99ed9e1ed_MD5.png]]

数据筛选 示例一步骤（4）

（5）弹窗“确定”后，完成数据筛选。

![[9fb0d614ac65c00accb0565f8f5dd740_MD5.png]]

数据筛选 示例一步骤（5）

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

每次进行搜索并勾选，对于交互式分析没有任何问题。但是如果保存成分析方案，则被勾选的选项也逐一保存在其中。如果下一次打开执行时数据变化了，可能不是预期的结果。

## 2 用计算字段判断条件

虽然直接使用原始字段创建筛选器是最正确的方式，但复杂的问题可能用简单的方式无法解决，那么尝试使用计算字段。在计算字段中判断条件是基本的做法，其中又分为将计算字段作为筛选器和作为度量等不同用法。

### 2.1 筛选器字段

用计算字段创建筛选器。

有一种情况是希望组合的条件固定在分析方案中，阅览者不必每次都去操作筛选器。例如上一小节的示例中，“复印机”和“传真机”是固定的条件，即使以后经营了更多的品牌或型号，具体的产品名称增加了，但[产品名称]字段中带这两个关键字的条件不变。这种情况可以创建计算字段，将条件映射成选项，然后再用计算字段来创建筛选器。

 **示例二：** 从示例一继续，移除原始字段的筛选器，重新以计算字段作为筛选器。

（1）创建计算字段，[目标产品]：`IF(OR(FIND("复印机",[产品名称])>0, FIND("传真机",[产品名称])>0), "图像设备", "其它产品")`。

![[349645f982acb4b5d333c90cad9fe1b6_MD5.png]]

计算字段用作筛选器 示例二步骤（1）

（2）用字段[目标产品]创建筛选器，文本类型字段自动表现为选项型筛选器。在数据能够匹配条件不同分支的情况下，有“图像设备”和“其它产品”两个选项。反勾选“其它产品”，保留“图像设备”选中。这样的筛选效果和以上示例一的效果相同，而且保存为方案之后，今后产品名称中出现更多带有复印机、传真机的数据也没有关系。

![[828c4165a220aeeb13017e73e81302c6_MD5.png]]

计算字段用作筛选器 示例二步骤（2）

### 2.2 度量字段

用计算字段创建度量。

有一种情况是在同一视图中，希望看到不同筛选条件的统计结果，而且这些不同的筛选条件还有包含关系。例如本年、本月、本日的某个度量值，如果拆开按年、月、日分别作为筛选条件是可以的，但是它们要在同一视图中显示，而且几个条件还有包含关系，不能非此即彼地分类。这种情况，可以针对每个统计目标创建计算字段，在不同条件分支中取值或放空，然后用计算字段来创建度量。

 **示例三：** 当日、当月、年度的订单数量一起显示。

（1）创建计算字段，[当日订单]、[当月订单]、[当年订单]，分别为：

`IF(INT([订单日期])=INT(NOW()), [订单ID], null)`

`IF(FIRSTDAY([订单日期],"MD")=FIRSTDAY(NOW(),"MD"), [订单ID], null)`

`IF(YEAR([订单日期])=YEAR(NOW()), [订单ID], null)`

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

以上如果改成IF(条件,[订单ID],"")，把null改成空串行不行？不行。目标都是计数，如果聚合运算是用“计数”、“去重计数”的话，应该IF(条件,值,null)；如果聚合运算是用“求和”的话，则IF(条件,1,0)或者IF(条件,1,null)都可以。注意第二个条件分支，数字零或者空字符串都是有值，都会被“计数”加一，只有空值null才不会被“计数”加一。

![[0a1fd526a8028dddc255eda7dde61177_MD5.png]]

计算字段用作度量 示例三步骤（1）

（2）将此三个计算字段分别创建为度量，聚合方式为去重计数，以统计订单数量。

![[a2d3778c60f7534e6fbba1e6aba1ef75_MD5.png]]

计算字段用作度量 示例三步骤（2）

（3）很多零，有点影响对整体阅读的快速辨识，尝试设置数字格式将零隐藏。并给分析字段重命名。

![[eace5f1f28c4bf1edb671dbf2ab32314_MD5.png]]

计算字段用作度量 示例三步骤（3）

  

还有一种情况是在没有日期维度的情况下，要实现按日期计算。已有的同环比等“按日期计算”的功能，必须依赖日期维度的存在，但是像数据斗方中指标类型的图表，只有度量值没有维度，从而无法直接实现同环比。这种情况，可以创建计算字段，按条件取要对比的那部分的值做聚合运算，取被对比的另一部分的值也做聚合运算，再将它们做减法、做除法，以实现增长差额或者增长率。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

用数据斗方的自定义列表，可以既带上日期维度，又模拟出单值的指标，这是另一个话题。

 **示例四：** 仪表板，“日期-年月”筛选器联动数据斗方，参数名称yearAndMonth。数据斗方用“业务指标”图表显示当月的度量值及月环比增长率。

（1）创建计算字段，[当月销售额]和[上月销售额]。分别为（为方便阅读排版成多行）：

```
IF
(
    AND
    (
        YEAR([日期]) = TONUMBER(LEFT(PARAM("yearAndMonth"), 4)), 
        MONTH([日期]) = TONUMBER(RIGHT(PARAM("yearAndMonth"), 2))
    ), 
    [销售额], 
    0
)
```

```
IF
(
    AND
    (
        YEAR(NEXTDAY([日期], 0, 1)) = TONUMBER(LEFT(PARAM("yearAndMonth"), 4)), 
        MONTH(NEXTDAY([日期], 0, 1)) = TONUMBER(RIGHT(PARAM("yearAndMonth"), 2))
    ), 
    [销售额], 
    0
)
```

![[8fbe16731683341485f0bb7d396cc331_MD5.png]]

计算字段用作度量 示例四步骤（1）

（2）创建计算字段[月增长率]。为：`SUM([当月销售额]) / SUM([上月销售额]) - 1`。

![[b8d21ec2d4e1281e323fc3b23adc043c_MD5.png]]

计算字段用作度量 示例四步骤（2）

（3）将[当月销售额]拖入“主指标”功能卡，自动生成度量，聚合方式为求和。将[月增长率]拖入“副指标”功能卡，自动生成度量，由于计算字段本身已带有聚合运算，不能再指定聚合方式。修改数字格式，添加前后缀文字修饰。

![[09fa581a5df09c8247e911def3626a3c_MD5.png]]

计算字段用作度量 示例四步骤（3）

（4）在仪表板中，可以切换筛选器的年月，联动数据斗方图表。

![[513c9409f198fd932f82fabeb7f0f8d5_MD5.png]]

计算字段用作度量 示例四步骤（4）

## 3 计算字段的副作用

通过计算字段来完成条件计算的目标，当数据量少的时候，额外开销可以忽略不计，但是当数据量多了，就要考虑性能的影响。

如果计算字段作为筛选器，筛选发生的时机会比原始字段更靠后。这在[数据分析与可视化/分析工具总体介绍/筛选器](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tk4lat0ic#5)有过阐述。

如果计算字段作为度量，实质上并没有发生数据筛选。所有数据都参与了计算，只不过是判断条件满足时才保留度量值。这种用法可以称为“伪筛选”。

除了性能影响之外，“伪筛选”最为明显的副作用是影响查看明细数据。表面上统计结果是符合某条件的值，一查看明细却是更多的数据。这在之前的章节[数据分析与可视化/数据分析/视图上的操作/详细信息及数据溯源](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ticvinc3g#2.2)中，也有过说明。影响查看明细数据也意味着影响单据联查。

综上，使用工具的过程中，应该权衡利弊，正确判别。