# 3.2.1.3设计器-总述-页面

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E4%BB%AA%E8%A1%A8%E6%9D%BF/%E8%AE%BE%E8%AE%A1%E5%99%A8/%E6%80%BB%E8%BF%B0/%E9%A1%B5%E9%9D%A2/content.html?timestamp=1775633854395

---

# 页面

页面是特殊的组件，是最顶层的容器。整个仪表板方案有且仅有一个页面组件，所以页面不能创建，也不能删除。

## 1 多页面

### 1.1 多页面怎么实现？

需要特别注意，仪表板中的页面组件，只是设计器中的顶层容器，与视觉效果上的页面，不是完全对等的概念。当需要在视觉效果上呈现出多个页面时，用多页签来解决问题。

在页面中创建“页签控件”组件，并调整为与页面一样大小（或者是除页面标题栏外的大小），必要时可以隐藏“页签控件”的导航栏。“页签控件”中的每一个子“页签”，就是一个视觉效果上的页面。

### 1.2 多个仪表板，还是一个仪表板多个页面？

如果内容足够繁杂，必将出现多个视觉页面时，到底应该制作多个仪表板，还是同一个仪表板有多个视觉页面，这是制作者在“下笔”前应该思考的问题。

这样的问题没有定论，需要结合业务场景、功能需求综合考虑，甚至有时候还不得不考虑到性能。

简而言之，如果不同的页面（意味着内容、布局不同）给不同的人看，或者虽然是给同一个人看，但在不同时间或不同场景，果断做成多个仪表板。如果多个页面之间有交互操作，首选同一个仪表板的多页面。

## 2 页面属性

### 2.1 页面大小 与 页面缩放

进入设计器开启仪表板设计之旅，第一步是要先确定页面属性“设计时页面大小”。

先确定页面大小，最主要的原因和仪表板的页面缩放有关。此处“页面缩放”是指仪表板按照运行环境的可视画面的范围，动态地自适应调整每个组件的尺寸。“页面缩放”与下文“展示模式”属性相关，与浏览器的缩放设置无关。（页面缩放具体规则可以参考[仪表板/设计器/总述/页面缩放规则](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tty8t2pc0)。浏览器缩放设置影响屏幕分辨率，参考[仪表板/快速入门/拾遗·之屏幕分辨率](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tc04keqsu)。）

仪表板的页面缩放与PPT不同。通过设计器制作好保存下来的方案，本质上只是一个模板——描述了格式，不携带数据。每一次打开查看，数据可以是动态变化的。页面缩放不是按设计时的状态等比例缩放界面上每一个像素，这是为了尽可能多地展示有效信息，当空间充足时显示更多的数据，当空间紧张时保留更多的关键信息。

这个过程字体可视的大小不会随着变化（个别场景另当别论，例如数据斗方的业务指标图）。这样的特性和PPT是有差别的，不能用制作PPT的习惯性思维来思考。

基于以上讨论的特性，通常在较小尺寸的页面设计出来的内容，页面放大之后效果不差；反之则不一定，在较大尺寸的页面设计出来的内容，缩小之后有可能由于空间不足而影响效果。所以应该遵循“仪表板在有页面缩放的情形下，在较小尺寸设计，在同等或较大尺寸阅览”的原则。

![[3d37cbc77be83299306c13d8c5f805fa_MD5.png]]

错误示例

理论上，既要不出现滚动条（对应展示模式“充满”、“适应”、“横向充满”），又要让字号足够大，还要让信息密度高（展示更多的图表，或一个图表展示更多的条目），三者往往不可得兼。实践中通常应该决定何者优先级更高，从而放弃优先级更低的诉求。

先确定页面大小，另一个重要的原因是容器大小的修改不会影响容器中组件的位置和大小。修改页面大小，意味着页面中所有组件都要逐层向下逐一修改。先确定页面大小，可以避免后续的重复劳动。

### 2.2 展示模式

页面属性“展示模式”，是仪表板在最终的展示环境中页面的调整策略。除了最后一个选项“不缩放”，其它选项都是允许页面缩放。页面缩放也意味着不出现滚动条。

![[fb729d99b6e6df0902d6c73f27a646a5_MD5.png]]

页面属性 展示模式

以投屏展示为特征的大屏场景，首选“充满”，页面将会在横向和纵向分别缩放到与可视画面（全屏时就是整个屏幕）一致。

如果要求保持设计时的页面宽高比例，选择“适应”，代价是可能出现上下或左右的留白。

以交互操作为特征的个人桌面场景，内容特别多时，可以考虑“横向充满”，这意味着纵向出现滚动条。配合该展示模式的一个特性是“页面标题栏”组件的属性“滚动时冻结”，全局的筛选器此时有必要放置在页面标题栏中。

至于“不缩放”选项，除非页面尺寸与目标空间刚好一样，否则效果是右侧留白或横向出现滚动条，下方留白或纵向出现滚动条。

### 2.3 外观风格

作为页面属性的“外观风格”，决定了整个仪表板的视觉效果。通常需要结合页面另一个属性“背景图片”一起考虑，外观风格中的配色应该与背景图片颜色协调，避免深色背景图片配浅色外观风格之类的设置。特别是外观风格中“卡片”分类的“背景颜色”属性，调整为透明、半透明时，可能颜色不影响卡片外观，但仍然影响着图表的绘制。

![[8e44c91c877b63c0817216286c0e0346_MD5.png]]

错误示例

另外，页面属性“卡片边框”、“卡片标题栏”是影响全局的统一设置。具有卡片外观的组件，受此影响。如果个别需要与众不同，可以在个别组件自身的相关属性上，将相关属性值“页面统一”切换为“自定义”，再另行设置。

### 2.4 移动端外观

与移动端相关的页面属性，请参考：[仪表板/设计器/总述/移动端](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Trxv5t2mt)。