# 1.2.6定义阶段-过滤

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%87%86%E5%A4%87/%E5%AE%9A%E4%B9%89%E9%98%B6%E6%AE%B5/%E8%BF%87%E6%BB%A4/content.html?timestamp=1774595192656

---

# 过滤

在前面的章节中，[数据准备/定义阶段/操作概要 第3小节 数据表的行](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#3) 已经对过滤的设置有了较为全面的介绍，包括通过系统变量控制“行”的访问权限，不再一一重复。本文对于未提及的问题再做一些补充。

## 1 业务实体的单据与分录

以业务实体作为数据表的数据源时，常见的是实体封装了单据及其分录，很多时候单据还是多分录的。

在“数据准备”的数据提取中，当在单据对应的数据表上设置过滤，系统会自动将单据包含的分录也过滤掉。反之，在分录对应的数据表上设置过滤，则不影响到其所在的单据。

## 2 比较符与数据类型

在过滤的设置中，字段的数据类型不同，比较符是有差异的。例如数值类型有“大于”、“小于”，文本类型没有；而文本类型有“包含”、“不包含”，数值类型没有，等等。如果字段进行了类型转换的设置，此依赖以转换后的类型为准，而不是原始类型。

如果以原始类型的字段参与过滤，有机会将过滤条件前置；如果以转换后的字段参与过滤，则会延后。这对数据提取速度会有影响，以下将继续讨论性能的话题。

## 3 性能的影响

“数据准备”的数据提取阶段，业务数据从数据库读取并转存到“轻分析存储”中，转换为轻分析特有的数据文件。在数据提取过程，过滤发生在数据库服务器（也就是过滤条件前置），还是发生在轻分析服务器，这对传递的数据量有直接的影响，从而也就影响到“数据准备”的时长。

对照下图，即过滤发生在“①数据读取”的传递之前，还是传递之后。答案是都有可能，需要看具体情况。

![[f39bb31784bffc0853b19516d2ea6b59_MD5.png]]

过滤发生在哪一头？

具体而言，如果过滤中用到以下字段，将不可能使条件前置，是低效的做法。

- ◆ 转换过数据类型的字段（“空”或“非空”判断除外）
- ◆ 计算字段
- ◆ “列转行”生成的新字段

对于实体（苍穹）而言，除了上述所列，还包括：

- ◆ 实体中的主键、外键
- ◆ 合并实体生成的[实体名称]字段
- ◆ 行政区域字段（原因在于显示值是个多层级信息，与记录值不一致）
- ◆ 多类别基础资料（原因在于不确定的引用字段）
- ◆ 多选基础资料（原因在于“多选”：一个字段记录多个值）
- ◆ 多选下拉列表（原因在于“多选”：一个字段记录多个值）

其它系统的业务实体，道理相同或相似，不赘述。