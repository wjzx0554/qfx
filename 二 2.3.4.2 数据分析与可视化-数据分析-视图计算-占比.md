# 2.3.4.2数据分析-视图计算-占比

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90/%E8%A7%86%E5%9B%BE%E8%AE%A1%E7%AE%97/%E5%8D%A0%E6%AF%94/content.html?timestamp=1775613045386

---

# 占比

## 1 概念

表格视图中单元格里的值，相对于其所在分组的比例，称为占比。

从算法上看，占比是分子除以分母。其中表格视图的单元格中聚合运算结果作为分子，小计/总计值作为分母。

## 2 占比的设置

度量字段的设置菜单中，有“占比”设置项。系统提供了六项常用的占比设置，相当于不同的分母的位置。勾选其中一项，可以将度量值改变成其对应的占比值；反勾选则回到显示度量值的状态。

六个选项中，“分组内同一行”是指交叉表中，列方向上展开的维度，最小的一级分组小计，它与目标单元格在同一行，以它作为分母计算占比。“分组内同一列”道理相同，只是方向上的差异。而“分组内同一块”则是行和列两个方向一起考虑。

“整行”是指交叉表中，列方向上展开的维度，最大的一级合计，即行总计，它与目标单元格在同一行，以它作为分母计算占比，这就是整行的占比。“整列”道理相同，只是方向上的差异。而“整表”则是行总计和列总计一起考虑，以整个表格所有单元格的合计值作为分母。

![[d0160620516167535d16dc57641ccbfc_MD5.png]]

占比的设置与对应逻辑

当列方向上只有一个维度字段时，最小的一级分组小计就是行总计，此时“分组内同一行”和“整行”效果相同。当列方向上没有维度字段时，“分组内同一行”和“整行”没有意义，因为此时的分母就是单元格自身，总会得到100%的占比；而“分组内同一块”和“整表”则分别退化成“分组内同一列”和“整列”。行方向与列方向同理。

设置占比时，通常要求显示成百分数的格式，而不是显示零点几几的小数格式。只需要在度量字段的设置菜单中，点开“数字格式”的设置，改一下数量单位即可。分析字段数字格式的设置，见[数据分析与可视化/分析工具总体介绍/分析字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj#3.3)。

![[bdd5f23965c9c84a591cc15c21b4920e_MD5.png]]

占比的效果

占比的显示与小计/总计的显示没有必然关系，它们只是算法上相关。如果不关注，任何一级的小计/总计都可以不开启。

如果度量值与占比需要同时显示，则应该创建两个相同的度量字段，其中一个保持显示度量值，另一个设置为占比即可。表头上显示文字的修改，可以回顾分析字段的重命名，见[数据分析与可视化/分析工具总体介绍/分析字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj#3.4)。

![[d05325a0037fad7b6fed947869fe34ee_MD5.png]]

度量值与占比同时显示

## 3 与图表有关吗？

“数据分析”工具中，“占比”是表格视图上的表现，与图表无关。饼图等以展现比例为目的的图表，天然就是占比的可视化展现，与这里的讨论没有任何关系。

## 4 计算字段

表格中的占比，通常六个常用设置项就够用了，不需要创建计算字段写表达式。

但是，如果计算占比的分组层级既不是由内向外的最近一层分组，也不是最外面的表格整体，而是两者之间的某一层级，则计算字段可以有效地解决问题。

在计算字段中，`TOTAL()`函数用于获取小计/总计值，具体语法及参数说明，在计算字段编辑窗口的函数说明里可以查得到，或者查看[公式系统/分析引擎特有函数](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ttcwdijj1#2)。

占比的计算字段（以聚合方式求和为例 ，下同），基本模式如下：`SUM([字段])/TOTAL(SUM([字段]),x,y)`，其中，x、y是非负的整数，分别表示横向及纵向向外第几层分组。

假设在“行”功能卡中拖入日期字段，并展开为[年]、[季度]、[月]三个维度。此时需要展示每月的[销售额]合计在年份中的占比，它既不是分组内同一列（季度分组），也不是整列（所有年份），而是向外第二层分组。那么，可以创建度量目标的计算字段，表达式为：`SUM([销售额])/TOTAL(SUM([销售额]),0,2)`，并放置到“数值区域”功能卡中。

![[ecfa8e1feb3d06979eb1d445346e70fb_MD5.png]]

计算字段实现的占比

综上，还可以得出以下一些推论：

“分组内同一行”，分母就是`TOTAL(SUM([字段]), 1, 0)`；

“分组内同一列”，分母就是`TOTAL(SUM([字段]), 0, 1)`；

“分组内同一块”，分母就是`TOTAL(SUM([字段]), 1, 1)`；

“整行”，分母就是`TOTAL(SUM([字段]), 99, 0)`；

“整列”，分母就是`TOTAL(SUM([字段]), 0, 99)`；

“整表”，分母就是`TOTAL(SUM([字段]), 99, 99)`。

特别地：

`TOTAL(SUM([字段]), 0, 0)` = `SUM([字段])`；

`TOTAL(SUM([字段]), 99, 99)` = `TOTAL(SUM([字段]))`。

以上的数字99，只是一个足够大的数字，用来表示最外层的总计，改成其它更大或更小一点的数字都没有关系。虽然这些写法没有什么用，或者通过简单的属性设置就可以得到结果，但是读懂它们，对于函数的参数的理解，对于占比的理解，不无裨益。