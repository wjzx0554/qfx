# 3.2.2.13 设计器-组件-按钮组

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E4%BB%AA%E8%A1%A8%E6%9D%BF/%E8%AE%BE%E8%AE%A1%E5%99%A8/%E7%BB%84%E4%BB%B6/%E6%8C%89%E9%92%AE%E7%BB%84/content.html?timestamp=1775635270435

---

# 按钮组

“按钮组”组件与“下拉列表”组件在数据逻辑上一致，同样都是单选，同样都是与数据类型为文本型的字段对应。因此，备选值、默认选中项、联动等属性不再重复介绍，参考[下拉列表](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tpqltvsk9#2)组件。

“按钮组”组件与“下拉列表”组件的不同在于外观。在组件的矩形区域内排布按钮，当显示不完全时，在区域前后出现滚动键，这是按钮组的基本外观。

“按钮布局”分“单行”和“多行”两种。按钮“尺寸”有“大”、“中”、“小”三档，分别对应的按钮高度为40px、32px、25px。

按钮的宽度与布局相关，“单行”时每个按钮由各自的字数决定宽度，“多行”时按钮的宽度是统一的，由组件宽度及相关参数共同决定。

当“按钮布局”属性选择“单行”时，组件的高度跟随“大”、“中”、“小”确定下来且不能改变。组件在设计区中被选中，操作框的上下边调节块是灰色的，不允许拖拽调整高度（[仪表板/设计器/总述/设计区操作](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tmexfunuy#3)）。在属性设置区，“高度”属性编辑框是灰色的不可编辑状态。高度禁止修改的同时，当组件参与页面缩放时，高度也是锁定的（[仪表板/设计器/总述/页面缩放规则](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tty8t2pc0)）。

![[e2f5d7ff3fe4fa7af1916e94aa750b3a_MD5.png]]

单行按钮组带左右滚动键

当“按钮布局”属性选择“多行”时，组件的矩形区域可任意调整。此时需要确定“每行最多个数”、“每个最少字数”两个参数。按钮布局时，系统确保按钮的宽度至少能显示“每个最少字数”参数指定的字数（汉字），这是按钮宽度的下限。如果组件宽度充足，系统在排布“每行最多个数”参数指定的按钮数之后，将会换行，形成多行的按钮布局。此时如果组件宽度足够，每个按钮的宽度将被平均拉伸，以填满组件宽度。另外，按钮间距是跟随按钮尺寸自动调整的。而且当组件高度充足时，按钮的行间距也会被适当拉大。

![[ac79b813eb65df126fe77c9afc3eaf7e_MD5.png]]

多行按钮组带上下滚动键

不难发现，将“多行”时的“每行最多个数”设置为1，相当于按钮布局的“单列”模式。