# 2.4.2.7数据斗方-设计器之图表属性-调色板

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E6%96%97%E6%96%B9/%E8%AE%BE%E8%AE%A1%E5%99%A8%E4%B9%8B%E5%9B%BE%E8%A1%A8%E5%B1%9E%E6%80%A7/%E8%B0%83%E8%89%B2%E6%9D%BF/content.html?timestamp=1775614092962

---
# 调色板

对于大多数图表，“调色板”属性条目位于“绘图区”分类中。正如前文[“分类：图例”](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Trbqb82u9)所述，颜色分为离散颜色和连续颜色两种，那么调色板的设置界面同样有两种，由具体图表支持的是哪一种颜色所决定。

个别图表，例如树图，既支持离散颜色，又支持连续颜色，打开调色板设置弹窗时的表现，则与设置当时的状态有关。当设计区的“颜色”功能卡中未放置分析字段或者已放置了维度字段时，弹出的窗口是离散颜色的设置界面；当设计区的“颜色”功能卡中放置了度量字段时，弹出的窗口则是连续颜色的设置界面。

## 1 离散颜色

### 1.1 颜色

当离散颜色不做自定义设置，选择系统默认时，界面上显示的“（自动适配外观风格）”是什么意思？这和仪表板有关。当数据斗方出现在仪表板时，仪表板的[“外观风格”](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tctlxh7l4)中包含了可应用于图表的调色板，它将成为系统默认。应该注意到，仪表板的外观风格是可以自定义的，即外观风格里的调色板可以自定义。这意味着可以为特殊的外观风格的背景调配相应的图表用色，而仪表板中的每个数据斗方，只需要保持系统默认即可。

离散颜色的自定义调色板，提供了最多二十个颜色的设置。对于柱图、饼图等图表，用到基本调色板是默认10个颜色，对于折线图、散点图等图表，用到的强对比调色板默认是6个颜色。设置界面上的二十个方块直观可见，未定义颜色的方块绘制出对角线也清晰可辨。

每个颜色方块下方的控件，可以修改颜色值。已设置了颜色的方块，右上角的删除按钮可以将颜色清除。颜色方块选中后，点击右侧的“上移”、“下移”、“左移”、“右移”按钮，可以让它和相邻的方块交换位置，以便调整顺序。如果由于调整顺序或者清除操作使位于前面的颜色方块未定义颜色，在设置窗口“确定”后，这些位置会自动被剔除，位于后面的颜色项会自动向前补位。

![[b8189ac14655dcc8d65b47331f0a44dd_MD5.png]]

“调色板”属性（离散颜色）

### 1.2 渐变

在较新的版本中，部分图表支持渐变色，则设置窗口中有“渐变”页签。注意版本差异及所支持的具体图表类型。

支持渐变色的图表，包括：

- ◆ 柱形图（多系列柱形图、堆积柱形图、百分比堆积柱形图）
- ◆ 条形图（多系列条形图、堆积条形图、百分比堆积条形图）
- ◆ 进度图（柱形进度图、条形进度图、环形进度图）
- ◆ 饼图（饼图、环形图、玫瑰图）

颜色渐变效果的选项，包括“系统默认”、“不渐变”、“按规则渐变”和“调色板每一项分别设置”。“系统默认”与上述调色板颜色的逻辑相同，跟随执行环境的统一定义，目前表现为没有渐变效果。“不渐变”是对当前图表明确指定不要渐变效果。“按规则渐变”是指对于调色板中的每一项颜色（称为主颜色），采用一致的算法进行渐变，不必每一项去做设置。相对而言，“调色板每一项分别设置”则允许基于调色板已定义的颜色项，每一项各自再进行设置。

对于“按规则渐变”选项，又有“同色调自动变化”和“向半透明过渡”两种选择，直观表现为同一颜色深浅的变化。其中，环形进度图受限于技术实现而禁用了“向半透明过渡”选项。

![[4a101c2584409f8398b23befc405ea5c_MD5.png]]

渐变：按规则渐变

补充解释一下“同色调自动变化”。调色板主颜色每个颜色值可以解析出H（色相）、S（饱和度）、L（亮度）三个分量（参考[数据分析与可视化/基础知识/数据可视化](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Teg3p7ovt#2.2)），当S（饱和度）较高时降低、较低时提高，L（亮度）亦然，而H（色相）保持不变，就可以构造出另一个新的颜色。主颜色和新颜色成为渐变色的两端，由于H保持不变，这就是所谓的同色调。当同时使用调色板中的多个颜色时，可能存在一个弊端，有的颜色项看起来由深变浅，有的颜色项看起来由浅变深。这可以改成“调色板每一项分别设置”去做任意的调整。

对于“调色板每一项分别设置”选项，先明确一个概念，“颜色”页签中确定的每一项颜色，称为主颜色。这一选项的规则为，渐变色是在主颜色的基础上，允许人为插入更多的关键点而形成。每个关键点包括一个百分比位置和一个颜色值。其中，主颜色是一个可以调整位置但不能删除的关键点。值得注意的是，如果采用这一选项，建议“颜色”页签中选择自定义的调色板，而不要选择系统默认，以避免主颜色跟随仪表板外观风格变化时造成的不确定性。

![[93524c73e082e19eea6192dbaa0cc839_MD5.png]]

渐变：调色板每一项分别设置

在进度图（柱形、条形、环形）中，由于只有与实际值对应的一项参与渐变，因此没有“调色板每一项分别设置”，对应存在的是“自定义渐变”，设置界面也相应简化。

![[62b08a0c0765f5897483a00697838607_MD5.png]]

进度图的自定义渐变

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

环形进度图采用了特殊的绘制算法。对于相同的渐变设置，在环形进度图上，与柱图类（包括柱形进度图和条形进度图）可能视觉效果上有所差异。

### 1.3 值映射

设置窗口的“值映射”页签，用于维度成员与颜色的绑定。通常在一个统一的环境中（例如同一个仪表板），不同图表的用色应该统一。假如在其中的一个图表，绿色表示“已完成”的状态，在另一个图表，绿色又表示“进行中”的状态；又假如在其中的一个图表，浅蓝色表示[产品类别]的“家具”，在另一个图表，则是天青色表示[产品类别]的“家具”。诸如此类，综合起来阅读就会体验很糟糕，甚至于获得错误的理解。此时，让确定的维度成员总是与确定的颜色一一对应，就很有必要。

设置界面上，“自动”选项表示不做维度成员与颜色的绑定，执行时会按维度成员的顺序，逐一使用颜色。切换到“按颜色顺序指定值”选项，则可以做值映射的设置。

![[09b1f84cd73db92d57187e88c4b79fe1_MD5.png]]

维度成员与颜色绑定

值映射的设置逻辑，是在第一个页签“颜色”中，调色板已确定的颜色顺序的基础上，让每个颜色对应一个维度成员。一些简单的规则必须遵循，例如不能多个颜色对应同一个维度成员。最终的设置结果，必须优先映射靠前的颜色，不能跳过某个颜色不用。如果相对靠前的某个颜色不使用，应该回到第一个页签，调整颜色顺序，将不作映射的颜色移动到后面。

值映射的设置过程，除了可以从备选值里选择之外，还允许直接点击“维度成员”列的单元格，进行文字编辑。在设计期未出现的值，预期以后将会出现，输入进去就好了。

经过值映射设置的分析方案，到了执行期，如果数据中出现了更多的未映射的维度成员，它们将排在已映射维度成员的后面，依次使用调色板中的其它颜色。

需要补充说明的是，如果设计区中颜色相关的功能卡（视具体图表而定，可能叫“颜色”，可能叫“系列”，也可能叫其它）中未创建维度字段，则无法进行值映射的设置。这种情形下，切换到“值映射”页签，将出现“没有表示颜色的维度字段，不能做值映射。”的提示。另外，有的图表支持用多个度量字段来表示多系列，如果是这种情况，度量字段的顺序与调色板的颜色顺序一一对应。在调色板中调整颜色的顺序，或者在设计区的功能卡中拖拽调整度量字段的顺序，都可以改变对应关系。

## 2 连续颜色

在数据斗方中，树图、地图等图表支持连续颜色。

此前“数据分析”工具的相关章节，[数据分析与可视化/数据分析/其它功能/颜色](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tmbf0gt67#2)，已详细讨论过连续颜色的自定义设置。数据斗方的设置界面完全一致，不重复。

![[d6eb11ab21d3628c0cdb7261e113bc65_MD5.png]]

“调色板”属性（连续颜色）

## 3 其它

应该注意到，业务指标图、进度图（包括柱形、条形、环形）、瀑布图等虽然也有称为“调色板”的属性，但都与具体图表相关，不是通用的颜色属性。待具体图表中再作介绍。