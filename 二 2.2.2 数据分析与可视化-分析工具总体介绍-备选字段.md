# 2.2.2工具总体介绍-备选字段

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E6%80%BB%E4%BD%93%E4%BB%8B%E7%BB%8D/%E5%A4%87%E9%80%89%E5%AD%97%E6%AE%B5/content.html?timestamp=1774596028008

---

# 备选字段

“数据分析”工具和“数据斗方”设计器，界面上完全相同的部分，是字段区域。字段区域中列出了全部备选字段。

![[2dc40c678917c0d8de9837cf34d1c8d1_MD5.png]] ![[44456302c974b017920d9bfddc806b1b_MD5.png]]

字段区域

备选字段是原始业务数据的“元数据”（描述数据的数据称为元数据），即它是对原始业务数据的描述。如果是嵌入式分析，来源于所在的业务模块，通常是单据的实体元数据；如果是主题式分析，来源于所在业务主题在数据准备中设置的数据表。

## 1 数据类型

在分析工具中，字段的数据类型被概括为三种：文本型、日期型、数值型。其中，日期型也是时间戳，可携带精确到毫秒的时间。关于数据类型，在 [数据准备/入门基础/基本概念](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tmwv6m6pb#2) 中，有描述它们和数据准备中的类型的对应关系。

每个字段前的小图标，能识别字段的数据类型。

![[888171f4418f9a5a5b5f53ce07a5b37d_MD5.png]]

备选字段数据类型

数据类型关系到分析中对字段的使用及相关的设置，也关系到可视化的具体表现，是需要关注的细节。也正因为如此，系统中将不同的数据类型分别集中排列。

## 2 拖拽参与多维分析

将字段拖拽到设计区域各功能卡中，生成维度、度量；或者将字段拖拽到设计区域筛选器功能卡中，生成相关的筛选器。这是备选字段参与多维分析的基本操作。

“拖拽”是鼠标的动作。鼠标指针移动到备选字段上，按下鼠标左键不放，移动到目标位置，再放开鼠标左键，完成一次拖拽。

![[8f8115e7294f9663511e30c8eb7cbfa8_MD5.png]]

拖拽字段

拖拽生成了分析字段，即字段参与多维分析的角色：维度或度量。大多数情况下，在拖拽进入功能卡后，文本型、日期型字段生成维度（其中日期型的时间粒度默认为“年”），数值型字段生成度量（聚合方式默认为“求和”）。当然这要看具体的图表类型以及具体的功能卡而定，也有可能文本型、日期型字段生成聚合方式为计数的度量，数值型字段生成维度。但无论如何，这仅仅是系统提供的默认设置，如果不符合预期，完全可以再修改。这部分内容，见 [数据分析与可视化/分析工具总体介绍/分析字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj)，有更为详细的介绍。

## 3 查找字段

如果字段区域中数据表很多、备选字段很多，不容易观察到想要使用的目标字段，可以通过标题栏右侧的“查找字段”开关按钮，打开搜索栏，输入文字进行查找。

![[a423de54de31e0faa2c0f8bf52be7371_MD5.png]]

查找字段

进一步思考，对于主题式分析来说，如果字段多到需要搜索才能找到，那么很可能这个业务主题的设计是有问题的。在数据准备的定义阶段，业务主题应该按实际的分析需求，即所谓的“主题”来定义数据表，控制数据表及其字段的数量，而不是一股脑加入尽可能多的表和字段。

## 4 操作菜单

字段区域中的备选字段，允许在分析工具的层面上进行一些预处理。相关的操作入口，集中在标题栏的下拉菜单中。

![[47a344e5a04afe01407c262df0639043_MD5.png]]

操作菜单

操作菜单中的选项，有时会处于灰色的禁用状态，这是由于操作菜单遵循“选中谁，操作谁”的逻辑。例如创建计算字段，需要知道新建的计算字段放置在哪个数据表中，因此必须先选中某一个数据表，再打开菜单进行操作。例如删除计算字段，当然也需要知道删除的是哪一个，因此必须先选中一个计算字段，再打开菜单进行操作。

如果需要对多个字段同时进行相同的属性设置，或者多个计算字段一起删除，可以多选，再进行操作。按住键盘Ctrl键，再用鼠标逐一点击选择字段，即可将多个字段同时选中。点击字段区域以外任何地方，清除选中字段。

![[6f5faa42169cfde5e862ec1166b27488_MD5.png]]

多选字段

### 4.1 计算字段

在分析过程中，常用的、单一的计算规则，通过弹出菜单对分析字段进行设置即可完成；而复杂的、组合的计算规则，才需要写计算表达式。这样的表达式，可以创建为自定义的字段，称为“计算字段”。

计算字段必须“挂”在某个数据表之下，当有多个数据表存在时，必须先明确这个目标，因此，在创建之前，需要先在字段列表中选中某个数据表，或者选中某个数据表下的字段以代表该数据表。同样的道理，编辑和删除计算字段，也是需要选中目标。其中，删除操作支持多选目标字段。

![[7fdb973fa207f7bda4c128a093e34cf9_MD5.png]]

创建计算字段

关于计算字段的语法，参考[公式系统](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tjsk1vkmu)。

编辑计算字段时，双击下方备选字段，可以将字段名称（通常是[表名.字段名]的格式）填到表达式文本框的光标处；双击下方函数列表中的函数，也可以将函数名连同括号自动填到表达式文本框的光标处。

计算字段在编辑过程中，会随时进行语法校验。如果语法正确，“表达式”标题后面显示绿勾图标；如果检查到语法错误，相同位置显示红叉图标并出现错误提示。点击提示文字，可以使光标定位到出错的地方。简短的表达式，容易一眼看清对错，但是冗长的表达式，就不容易了。借助光标定位，能有效地进行检查和修改。值得注意的是，语法校验并不能识别撰写表达式的意图，错误提示也只是语法解析过程发现不合法时的一种解读，这种报错很有可能与撰写意图风马牛不相及。举个例子，本来要输入`SUM([利润])`，但是没有写上一对括号，语法引擎会识别成`SUM`和`[利润]`两个子表达式，认为中间应该有个什么符号，因此提示“缺少运算符”。此时虽然不能得到符合撰写意图的提示，但是光标定位可以找到SUM之后的位置。

![[81b81d46617a0d9efc0d099609787d4e_MD5.png]]

计算字段语法校验

计算字段根据表达式内容，自动识别数据类型。在备选字段列表上，数据类型图标之前，多了一个等于号，以此作为标识。计算字段总是位于所在数据表的末尾，按创建顺序排列。

![[99a1669fe8a9bae2886e4a698e5a0a1f_MD5.png]]

计算字段的等于号标识

### 4.2 数字格式

每一个备选字段，都有初始的数字格式。这个初始状态，或来自于“数据准备”定义阶段的设置（数值型字段），或来自于系统自动生成。

数字格式的初始状态是允许在分析工具中修改的。选中目标字段，打开操作菜单选择“数字格式”项，弹出设置窗口。如果多选目标字段，可以同时对它们进行修改。

![[cd5338e5080309dfc3e031dea7fd9fc6_MD5.png]]

设置数字格式

由于工具不能识别字段的具体语义，数值型字段的格式一般按2位小数预置。如果字段用于表示数量（指不可切分的整体的个数），应该修改成整数，即“小数位数”为零；如果字段值是一长串数字的流水号（或者称为ID、或者称为编码等等），除了格式化成整数，通常也不应该出现逗号的“千位分隔符”；如果字段表示百分比（小数，1等于100%），则可以修改成“数量单位：百分之一”……

为什么在备选字段中，文本型、日期型的字段，也有数字格式的设置？这是因为当它们作为度量时，例如聚合方式为计数，就会用得到。所以它们的初始状态也表现成格式化为整数，即“小数位数”为零。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

此处可以延伸一下思考，为什么日期型的备选字段没有日期格式的设置？这是因为日期字段作为维度时，还需要设置它的时间粒度：年、年月、年月日等等，日期格式必须对应具体的时间粒度来定义。作为备选字段，还未被使用，根本不清楚会是什么。

### 4.3 会计年度

有些国家的会计年度，与自然年份不是完全对应。例如从十月份开始，是下一年份的财年。

这可以通过在日期型的备选字段上定义“会计年度”来实现。当这样的字段作为维度且时间粒度定义为年时，年份的值会按所定义的起始月份来计算，而且在默认未修改格式时以字母FY为前缀。当这样的字段作为维度且时间粒度定义为季度时，季度的值也会以会计起始月份为第一季度的起始。

![[60367d7e625bcffa1f019abc222cdccb_MD5.png]]

会计年度

从“一月”开始，等同于默认未设置的状态，即按自然年划分。

### 4.4 自定义顺序

“自定义顺序”用于预定义文本型字段的排序依据。注意这个描述，它并不是在做“排序”，而是针对文本型的备选字段，提前给它约定了依据，当它作为维度时，它的维度成员如果进行排序，将按此约定中的依据进行。

如果没有此设置，文字排序时以语言相关的字符内码为排序依据，英文表现成字母序，简体中文表现成拼音序（GB2312字符集收录的一级汉字三千多个，即常用字，是以拼音为序的；非常用字则不是）。

假设这样一个场景，某字段[区域名称]有“一区”、“二区”、“三区”、“四区”等几个值，默认的顺序是按拼音的：二（er）、三（san）、四（si）、一（yi）。在自定义顺序设置中，排序依据选择“手动”，可以人工调整为一、二、三、四。

![[75223edbc1eb0ea3aed3d6468377f987_MD5.png]]

排序依据：手动调整

假如同时存在[区域编码]的字段，而编码的顺序可以反映名称的显示顺序。例如：A001（一区）、A002（二区）、A003（三区）、A004（四区）。那么排序依据也可以选择“依据字段”，给[区域名称]绑定[区域编码]作为排序依据即可。

一般来说，相对固定不变且少量的几个值适合手动调整，数量较多的值则应该考虑以另一个字段作为依据。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

在本文档中，文字表达也以方括号来表示字段名称，这与计算字段表达式的语法相同，便于阅读。一般是：[字段名]，如果需要识别不同的数据表，则是：[表名.字段名]。其它章节也遵此规则，不再说明。

### 4.5 查看原始数据

在分析工具中，如果需要了解数据“长什么样”，或者确认数据表有多少行，可以在字段区域的操作菜单中，选择“查看原始数据”。

与创建计算字段的道理相同，当存在多个数据表时，需要先选择一个数据表，明确目标，再下拉菜单进行操作。

![[19206b9b6b881aeb34154272201ada67_MD5.png]]

查看原始数据