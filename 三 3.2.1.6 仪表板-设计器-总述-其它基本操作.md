# 3.2.1.6 设计器-总述-其它基本操作

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E4%BB%AA%E8%A1%A8%E6%9D%BF/%E8%AE%BE%E8%AE%A1%E5%99%A8/%E6%80%BB%E8%BF%B0/%E5%85%B6%E5%AE%83%E5%9F%BA%E6%9C%AC%E6%93%8D%E4%BD%9C/content.html?timestamp=1775634681763

---

# 其它基本操作

## 1 工具栏

**保存** —— 每个仪表板模板可以保存成一份方案，下一次进入设计器直接打开该方案。

**撤消/反撤消** —— “撤消”是指回退设计器中的最近编辑动作；“反撤消”是反向重做刚刚被撤消的编辑动作。

**AI换装** —— AI换装功能可一键智能完成外观相关的各种属性设置，详见[仪表板/设计器/关键特性/AI换装](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T2gtqg0p3)。

**全局视图** —— 在较新的版本中，提供了“跨容器移动组件”的便捷操作，详见 [仪表板/设计器/总述/设计区操作](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tmexfunuy#5) 之5；而“组件关系视图”是用于全局检查组件间的联动关系，详见 [仪表板/设计器/组件间联动](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzwez4pxh#4) 之4。

**预览** —— 预览是脱离设计器编辑环境，模拟发布后的场景进行查看。预览分为桌面端和移动端。“桌面端”可以在没有编辑干扰的情况下体验交互操作，并且可以整体全屏检查设计效果。“移动端”是模拟了手机界面进行查看。这只是简单的模拟，运行环境还是桌面端，所以筛选器等一些细节仍然表现为桌面端风格。不要在意这些细节，此处预览的主要目的是检查在移动端组件排布是否符合预期。

**发布** —— 发布是指将当前编辑的内容复制一份，并在目标阅览入口挂载访问链接，同时还要指定谁可以访问。目标阅览入口就是不同的“发布到……”选项，指定谁可以访问就是“授权”的设置。为了满足个性化的需求，发布时是将方案复制了一份，所以针对发布过的内容的修改，最准确无误的做法是在“发布管理”中对发布记录进行“编辑发布方案”。

**定时推送** —— 让系统在指定的时间向电子邮箱或移动办公平台发送消息及仪表板的访问链接。与发布相同，设置了定时推送，同样是将方案复制了一份，所以如果需要修改方案，也是应该在“定时推送管理”编辑推送配置记录。与发布不同，推送发生时，除了方案，仪表板中的数据可视化组件（数据斗方、数据分析）所用到的业务数据也会被复制。所以，通过定时推送的链接查看到的视图是一种“数据快照”（想象成用照相机在当时拍了一张照片），因此推送者也不能在“定时推送管理”中对推送过的明细条目进行修改（可以删除）。

关于发布和推送，可以参考：[仪表板/发布及推送](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tc91nr9zs)。

## 2 内容大纲区

内容大纲区以列表的形式展示了已创建的组件，并且缩进形成树状结构。

在这里，列表的选中状态与设计区是完全对应的。所以，大纲既可以用来选中组件，也可以用来查看设计区中选中了哪个组件。

鼠标移动到条目上方，在其右侧会出现三个圆点的小图标，点击它可以弹出操作菜单。操作是针对该条目对应组件的。

![[4d00bc6fbcda6d189950966e565a88fc_MD5.png]]

大纲区域 组件的操作菜单

**重命名** —— 系统以组件类型名称加数字流水号对组件进行自动命名。当仪表板足够复杂时，为了方便识别，可以对组件进行重命名。在复杂的仪表板中，组件名称的识别度，对于联动设置尤为有用。

**删除** —— 除了设计区中可以删除组件，这里提供了另一种删除组件的方式。而且，对于少数特定情况，例如从页签控件中移除子页签，这是唯一的操作方式。

**添加页签** —— 这是页签控件特有的操作，也是页签控件增加子页签的唯一方式。

**剪切** —— 结合“粘贴”，可以将组件从一个容器移动到另一个容器中。单独的剪切像极了删除。误操作时，不要忘了工具栏上的“撤消”按钮。

**复制** —— 在较新的版本中，可以复制组件，结合“粘贴”使用。

**粘贴** —— 容器特有的操作。必须遵循一些规则：页面标题栏只能出现在页面中，页签控件中只能有页签，页签只能出现在页签控件中，组合卡片中不能有组合卡片等等，如果不符合规则系统会有提示。

**调整组件顺序**

内容大纲区右上方有“上移”、“下移”两个按钮，它们用来调整组件在容器中的顺序。先选中组件，再点击上下移按钮即可。

可以发现上下移按钮时不时会出现灰色禁用状态，那是因为规则上不允许移动了。通用的规则是位于容器中的第一个子组件不允许再上移，位于容器中的最后一个子组件不允许再下移。特殊规则例如页面标题栏总是页面中的第一个子组件不能改变顺序。

改变组件顺序有什么作用？

第一，页签控件中的页签，如果需要调整顺序，这是唯一的方式。

第二，虽然不推荐组件重叠，但是当组件不得不重叠时，存在谁盖住谁的问题（排在后面的盖住排在前面的），顺序的调整就有必要。

第三，桌面端仪表板在移动端显示，会自动改变组件的位置，使之适应手机屏幕。当页面属性“组件在移动端自适应布局”选择“按大纲中的顺序”时，组件顺序就有用了。

## 3 属性区

设计器右侧的属性设置区，展示了当前选中组件的属性项及其属性值，查看属性、修改属性在此处进行。

遵循“选中谁，设置谁的属性”的原则，需要随时关注设计区中的组件选中状态是否符合预期，属性区顶部也显示了组件名称，方便识别。

属性区中有如下常见的属性编辑控件，这里对控件的操作做统一的简单介绍。

**数字微调框** —— 获取焦点后，可通过键盘输入数字。数值会有范围限制，输入超出范围时，自动变成临近的最小或最大值。鼠标点击右侧的上下微调小按钮，也可以按步长增减数值。

**下拉列表框** —— 鼠标点击，在弹出的选项列表中选择即可。

**弹窗编辑框** —— 鼠标点击右侧的编辑小按钮，弹出窗口进行编辑，需要按“确定”或“取消”按钮退出窗口。弹窗编辑框通常对应相对复杂的信息和编辑过程，控件本身也很难显示编辑结果，常见显示“已设置”的文字反馈。针对具体的某些属性，如果允许清除编辑结果，则鼠标移动到弹窗编辑框上时，右侧编辑小按钮前还会出现删除小按钮，点击删除小按钮可以清除已编辑的内容。

**颜色选择框** —— 鼠标点击，下拉颜色选择面板。如果备选色块中没有想要的颜色，选择“其它颜色”，弹出自定义颜色设置窗口。部分属性的颜色可以半透明，则颜色框后会跟随一个百分比的下拉框，可以选择0%至100%的不透明度。

**开关** —— 鼠标点击方框勾选或反勾选。

**单选按钮组** —— 鼠标点击目标项的圆圈即可。

![[03f2f55f56ec12289a9cee3be087e80e_MD5.png]]

属性设置区中的属性编辑控件