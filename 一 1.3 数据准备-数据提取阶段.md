# 1.3数据提取阶段

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%87%86%E5%A4%87/%E6%95%B0%E6%8D%AE%E6%8F%90%E5%8F%96%E9%98%B6%E6%AE%B5/content.html?timestamp=1774595236656

---
# 数据提取阶段

“数据准备”分为定义阶段和数据提取阶段。经过定义阶段的设置之后，数据提取阶段才能正常地执行。

数据提取有“实时提取”和“定时预提取”两种策略。具体设置见 [数据准备/定义阶段/操作概要，第6小节 数据提取策略](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#6)。 很多时候，实时提取的直观感受就一个字——慢。性能，将会是这一部分重点讨论的内容。

## 1 快与慢的辩证

在 [数据准备/入门基础/原因与原理](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tiv1ur2u0) 中已探讨过数据提取的话题。为确保分析工具与数据频繁交互的流畅性，业务数据事先会被提取到轻分析存储中。这是数据提取的原因，注意区别于常见的报表工具的实现方式。

![[4ebdee4e6a7436e3375947c88cc21816_MD5.png]]

数据提取及使用

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

传统的报表开发者，可能会寄希望于报表视图上由阅览者操作的过滤条件，传递到取数过程中与SQL查询语句或者存储过程的参数对接，这在轻分析中不可能实现。

虽然后续的使用可能更为流畅，但一次性提取全部备用数据，也使得“数据准备”的压力倍增。实时提取的数据准备策略，可能带来“启动很慢”的体验。这或许是数据量确实庞大带来的后果，或许只是设置上忽略了一些小技巧而已。

## 2 改进措施

### 2.1 减少行、列

简而言之，就是主动减少数据量。包括减少字段、减少数据的行数。

用不到的字段不选，这在 [数据准备/定义阶段/操作概要，第2.1小节](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#2.1)已有提及。另外，如果是实体数据源，其中的基础资料外键字段，取数时会自动关联并替换为名称。这是需要成本的。这样的字段如果不选用，除了减少该字段本身的成本，降低关联成本也很重要。

用不到的数据（行），也应该过滤掉，这算是常识。设置操作见 [数据准备/定义阶段/操作概要，第3.1小节](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#3.1)。

### 2.2 过滤条件前置

在过滤的设置中，如果将计算字段、类型转换过的字段等用于过滤条件，结果将是数据从数据库传递到轻分析服务器，再进行筛选判断。这是低效的。

尽量使过滤条件可以前置，是优化的有效措施。详见 [数据准备/定义阶段/过滤](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tcdhw9fti) 中第3小节 性能的影响。

### 2.3 定时预提取

“定时预提取”是终极解决方案。

将数据提取安排在业务低峰期，例如半夜进行。一方面，减少读取数据库对业务系统的干扰；另一方面，第二天可以获得快速启动的体验。

当然，数据提取之后如果再有业务发生，新的数据将不会出现在目标集合中。必须降低对数据实时性的要求，是唯一的遗憾。但是对于分析系统来说，放弃短时间内的新发生的业务数据，大多数情况下是可以接受的。况且，即使是选择“实时提取”，数据也不是绝对的“实时”，提取之后再有业务发生，同样不被包含在内。

需要注意的是，“实时提取”是按当前执行人的身份进行，“定时预提取”是按主题制作者的身份进行。如果设置中存在[使用系统变量作为权限控制](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#3.2)，或者实体数据源本身控制了权限，显然就会有影响。在新的版本中，“定时预提取”已经提供了可以指定到人的设置，注意版本差异。对于未升级到新版本的情况，则应该考虑此不良影响的后果。

### 2.4 放弃业务实体？

业务实体所带来的好处显而易见，但通过业务实体获取数据，几乎也是最慢的方式。如果作为开发人员，对业务系统内部实现足够了解，或许可以考虑放弃使用业务实体，改用“自定义SQL”。

“自定义SQL”可能带来的另一个好处是，关联的提前。数据表的关联，是另一处性能瓶颈，下面将会讨论。通过 “自定义SQL”，依赖数据库的能力提前实现关联。可以再结合“定时预提取”，则速度将得以显著提升。对于特定的可视化场景，例如一些大屏展现，这是值得考虑的选择。

## 3 没有意义的排序

在“数据准备”中，对原始业务数据进行排序是没有意义的。例如在自定义SQL中加上“order by”子句。既增加开销，又不能带来任何有用的效果。

分析工具的执行引擎，总是会进行聚合运算，在此过程中原始数据的顺序总是会丢失。即使原始数据与最终的可视化视图一致，也是如此。因此，完全没有必要在提供数据时进行排序。

## 4 合并与关联

数据表的合并，发生在数据提取阶段，而数据表的关联并不是。

在同一主题中，如果提供了多个数据表，后续分析工具的使用，可能只用到其中一个表，则没有关联发生；可能用到其中几个表，则按实际使用情况发生关联。

设置了多表关联，虽然不影响“数据提取”的速度，但影响到使用时的速度。这是另一处可明显感受到“慢”的地方。

![[cefdc18281c7bc5289460f683664be11_MD5.png]]

关键内部处理耗时示意图

不要让关系两端的数据表同时“保留无法关联的行”（即“full outer join”），是避免更慢的必要措施。

## 5 数据量

轻分析不对原始业务数据的数据量做限制。但是，进行聚合运算的“立方体”单元数量有限制，某些图表的展现单元也有限制，这在后面的章节再讨论。

因此，在“数据准备”的数据提取阶段，数据表行数没有上限。当然，磁盘空间是一种客观的限制，可接受的等待时间（包括数据提取的时间 ，也包括数据使用时访问、计算的时间）是一种主观的限制。通常情况下，对于单一（特指不发生关联）的数据表，分析引擎面对千万级的数据行数，可以得到秒级反应的交互体验。下一章，数据分析与可视化工具，将会进一步探讨相关话题。