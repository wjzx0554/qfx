# 2.3.3.1数据分析-视图上的操作-表格视图

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90/%E8%A7%86%E5%9B%BE%E4%B8%8A%E7%9A%84%E6%93%8D%E4%BD%9C/%E8%A1%A8%E6%A0%BC%E8%A7%86%E5%9B%BE/content.html?timestamp=1775612807603

---
# 表格视图

这里，表格视图是指“数据分析”工具界面上，内容面板中的可视化视图区域。可能是图表类型为表格时的视图，也可能是其它图表的棚架视图。

![[d47734743a4a45b6a0941d2f6e3a3e90_MD5.png]]

数据分析工具界面

以下提及“表头”时，除了交叉表左上角的斜线表头外，还包括行、列方向上的维度部分，这与[“冻结表头”](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ts7nzn503#3.1)设置中描述的表头概念一致。

![[608cedce3cfa47629f01022de21e6591_MD5.png]]

表头

## 1 调整行高列宽

当鼠标移动到表格的表头区域中，位于行（列）分隔线上时，出现可上下（左右）调整的鼠标指针，此时可拖拽调整行高（列宽）。

![[8eb8a6de6ada5c62f302ccd90fb67995_MD5.png]]

拖拽调整行高列宽

如果调整的目标是由维度成员生成的行（列），则调整后每一行（列）的高度（宽度）一样。换成具体的说法，在左表头调整行高，则表体每一行的高度变化一致；在左表头调整列宽，只影响相应的维度字段那一列的宽度；在交叉表的上表头调整列宽，则表体每一列的宽度变化一致；在交叉表的上表头调整行高，只影响相应的维度字段那一行的高度。

![[b6a7e3deec1e4a27850a7837e6c1d49c_MD5.png]] ![[8dce4102d91153abccd30786a2f26a40_MD5.png]]

调整行高及效果示例

对于图表的棚架视图，操作是完全一样的。当图表没有棚架时，也会保留一小块的表头空间，目的就是为了可以拖拽调整大小。

![[41884c6a567218a9542b6c01cc41ae09_MD5.png]]

拖拽调整图表尺寸

调整行高列宽是可撤消的操作。调整后的状态，将会随分析方案保存。

## 2 表格排序

### 2.1 排序操作

当鼠标移动到斜线表头上时，行、列方向上的维度字段对应的位置，出现排序按钮，点击按钮可对维度成员进行排序。当鼠标移动到左表头的最后一列，或者上表头的最后一行时，每一个单元格上都可能出现排序按钮，可对相应的行/列，按度量值进行排序。

![[73e8a2cc49e02fd737847922de3a71c4_MD5.png]]

排序按钮

### 2.2 规则简述

下面以行的排序为例，简单了解一下相应的规则。另一个方向，列的排序同理。

假设行方向上有n个维度形成多级分组，列方向上至少有一个维度横向展开，另外有一个度量。在n个维度中，前面的n-1个，可以同时设定排序，排序的值是各自维度成员，影响的是分组的顺序。最后一个维度的排序，影响的是每一行；上表头每一列的排序按钮，针对的是度量值的排序，影响的也是每一行。所以最后一个维度与上表头的排序按钮是互斥的，它们在同一时间只能设定一个排序，点击一个按钮会自动清除其它按钮的排序状态。

![[b11b302c7609d1d39640b9e6a0064126_MD5.png]]

排序规则举例一

行的排序总是在分组内进行的，分组的排序总是在更大的分组内进行的。在每一级分组都设定排序的情况下，就形成了从外层到内层的逐级有序。但是，当某一层分组没有设定排序，它可能受到邻近的内层已设定排序的影响。如果这个邻近的内层是最内一层，而且排序是针对度量的，此时受影响的分组的排序依据，在开启显示分组小计时优先采用分组小计值，未开启小计则采用分组内第一行的值。

![[5a4b17f1d22c1d7e7838d9fa9b3237a8_MD5.png]]

排序规则举例二

排序按钮为三态，点击时在“无”、“升序”、“降序”之间轮流切换。默认状态为“无”时，对于维度相当于升序（内层未排序时），对于度量相当于跟随维度成员，不做排序处理。

### 2.3 排序依据

度量值或日期维度的排序，按照值的大小没有疑义。文本型字段的维度成员的排序，英文表现成字母序，简体中文表现为拼音序（仅常用字）。如果需要修改文本型字段的排序依据，参考备选字段的[自定义顺序](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tlmhf2wv7#4.4)。

### 2.4 棚架视图

图表的棚架视图也是表格，当单元格有值时，也可以按单元格的度量值排序。单元格是否有值，不同的图表类型有不同的表现。例如折线图、散点图之类，一个单元格对应一个图表，那么单元格总是不可能有值；例如柱形图/条形图，一个单元格对应一个柱子，那么单元格总是有值；对于饼图、树图之类，如果有“大小”的视觉设置，虽然一个单元格对应一个图表，但图表有大小之分，即单元格有值。

![[fbb4b38bf4617d27c2298ca00390a09c_MD5.png]] ![[8ac5b1f49d5870c91e464db148fc3450_MD5.png]]

棚架视图的排序

## 3 复制文字

由于分析工具需要支持鼠标的拖拽操作，界面上禁用了选择功能，不能像普通的网页一样拖拽选择文字。

如果需要复制表格视图上的文字，可以先用鼠标点击一下单元格，然后键盘Ctrl+C，这样就已经把该单元格的文字复制下来，到其它地方Ctrl+V（粘贴）就可以了。