# 2.3.3.2数据分析-视图上的操作-详细信息及数据溯源

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90/%E8%A7%86%E5%9B%BE%E4%B8%8A%E7%9A%84%E6%93%8D%E4%BD%9C/%E8%AF%A6%E7%BB%86%E4%BF%A1%E6%81%AF%E5%8F%8A%E6%95%B0%E6%8D%AE%E6%BA%AF%E6%BA%90/content.html?timestamp=1775612866635

---

# 详细信息及数据溯源

## 1 详细信息

在表格视图上，当鼠标移动到度量值对应的单元格中时，跟随鼠标的悬停窗口展示出对应的详细信息。在此可以阅读到单元格在表头上的维度成员以及度量值本身。

详细信息显示格式是各字段名称和字段值。其中，字段名称就是分析字段的显示名称，可以在[分析字段重命名](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj#3.4)对其修改。度量值格式跟随视图上的显示，可以在[分析字段数字格式](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj#3.3)修改。

![[83b1a81c639e3dcba9f47afa3733b602_MD5.png]]

表格的悬停信息

对于图表，不同类型略有差异。但原则也是鼠标移动到相应位置上，显示相关的信息。如果有棚架视图，则表头的维度信息也附加其上。在图表中，各种堆积和多系列的折线，会将各颜色及其对应的维度成员和度量值，同时显示在悬停信息上，方便对比阅读。

![[fe21490578d9c97737575c5734b7b690_MD5.png]] ![[94463cc2daf6e00842f0c17cc8974641_MD5.png]] ![[1a7b960af3c1cd65823128464c9f1a42_MD5.png]]

几种图表的悬停信息

## 2 查看明细数据

上一小节所述的详细信息，与视图上可见的值对应，本质上是维度和度量的值，是多维计算引擎聚合计算后的结果。下面将看到的是，参与该维度组合的计算的明细数据都有哪些。

### 2.1 操作

在出现悬停窗口时，点击一下鼠标左键，悬停窗口会固定不动并在上方撑开操作栏。此时鼠标向窗口方向移动，通常是右下方向（当目标位于右/下角落时，悬停窗口也可能在左/上方向），点击操作栏上的“查看数据”按钮即可。注意如果在悬停窗口固定不动时，鼠标向其它方向移动，则自动解除固定状态并收起操作栏。

![[45747fa574f345e2730ed37ecfcfd0df_MD5.png]]

查看明细数据入口

“查看数据”是一个弹出窗口，逐一列出参与目标单元格（或单元格内的图表局部）聚合运算的原始业务数据。如果当前分析模型中用到的字段来自多个数据表，数据引擎会自动进行关联，弹窗中所展示的也是关联后的结果。

![[4d2d997fbf18631a999a8e30fca36d0f_MD5.png]]

查看明细数据

### 2.2 规则

展示出来的明细数据，以如下两条规则为准。

- （1）明细数据是以视图上各维度成员的交叉单元为查找依据。
- （2）明细数据是参与目标单元的聚合运算的数据。

对于规则（1）来说，各维度成员的交叉就是立方体单元的意思。此时可能存在一种误解，例如存在“按日期计算”的度量“上期值”、“环比增长率”之类，基于表格上这些单元格去查看明细数据，以为可以查出上期的数据，其实并不会。这是因为基于规则（1），它们与所谓的本期值（普通的度量值）在视图上都是相同的各维度成员的交叉，因此查看到的是本期的数据。上期的数据应该在视图上维度为上期的单元格中去查看。

![[66d8e72ba9ed388d45b922f1ed34915a_MD5.png]]

查看明细辨析--规则（1）

对于规则（2），工具在被使用过程中，使用者选择不同的技术手段，也可能使得计算结果的数值与明细数据在业务语义上看起来不符。可以看如下两种做法的比较。

仍以查看北京市、2016年、家具类别为例，这次省份作为筛选器。查看明细数据时看到的行数，是被筛选过的（北京），参与2016年家具类别这个单元的聚合运算的数据，与[销售额]的统计值是相符的。

如果使用计算字段`IF([省份]="北京",[销售额],0)`，虽然效率低一些，但与数据筛选的做法统计结果是一样的。这种情况，实际上参与2016年家具类别这个单元的聚合运算的数据是所有的省份，明细数据与[销售额]的统计值，直观上就不相符了。

![[dedf76628d347fe2ea8e2902c0475221_MD5.png]] ![[66567fbdfad950fc1ff010ac6c54a87d_MD5.png]]

查看明细辨析--规则（2）

以上两条规则，在使用工具的过程中，应该正确识别。

## 3 单据联查

从视图单元格上的详细信息，到查看参与聚合运算的明细数据，这是一层数据穿透。从明细数据，进一步查询到相应的业务单据，这又是一层数据穿透。

单据联查的前提条件是业务单据的嵌入式分析场景，或者主题式分析中以业务实体作为数据源。其它方式的数据源无法联查到业务单据。设置方式是在工具栏“设置→单据联查设置”中勾选数据源对应的业务实体（或其相关的页面），具体在[数据分析与可视化/数据分析/数据穿透/单据联查](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Trfz4zvlj)中另有介绍。现在仅关注设置了单据联查的分析视图，如何操作可以穿透到业务单据。

与查看明细数据的操作相似，在详细信息悬停窗口撑开的操作栏中，有另外一个按钮就是“单据联查”，点击它即可。这里对应的是多行明细数据，亦即多条业务单据，因此将会跳转到单据列表界面。注意上一小节查看明细数据的规则，单据联查与之完全一致，也就是说，联查到的单据就是明细数据对应的那些单据。

![[a0c4f761c42fc2a2d5e7c64aee45ef00_MD5.png]]

视图上的单据联查

如果进入到查看明细数据的界面，在每一行数据最前面的一列是操作栏，鼠标移到某一行上时，操作栏中自动出现“单据联查”按钮。如果从这里进行单据联查，由于明确到具体的某一行，即是具体的某一条单据，因此将会跳转到单据的编辑界面。

![[e405b9184aabda11d7b674608f41076b_MD5.png]]

明细数据的单据联查

以上两处单据联查的入口，当设置时勾选了多个业务实体，或者勾选了业务实体对应的多个页面时（如果有多个可供选择），点击“单据联查”按钮操作时将会有询问窗口或下拉菜单，选择其中的目标即可。

 **示例：**

![[159960a676b1e5378d042b7f293212b0_MD5.gif]]

视图上的单据联查操作示例

![[06d0cbac243b3b9b012373433dc3bf0e_MD5.gif]]

明细数据的单据联查操作示例