# 2.3.4.3数据分析-视图计算-按日期计算•同环比

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90/%E8%A7%86%E5%9B%BE%E8%AE%A1%E7%AE%97/%E6%8C%89%E6%97%A5%E6%9C%9F%E8%AE%A1%E7%AE%97%E2%80%A2%E5%90%8C%E7%8E%AF%E6%AF%94/content.html?timestamp=1775613130738

---

# 按日期计算·同环比

## 1 概念

### 1.1 前提条件

以日期型字段作为维度时，必须指定时间粒度。这在设置分析字段的[维度、度量](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj#3.1)一节中做过介绍。

基于日期维度的时间粒度，可以计算同比和环比。

### 1.2 定义

“同比”是指某一个目标期间的一个度量值，与去年相同期间的同一度量值相比较。显然，年与年之间的比较，要求在分析模型中必须有维度为年的信息；而这里的“期间”，通常是指日期维度的时间粒度为季度、月份等。

“环比”是指某一个目标期间的一个度量值，与连续的上一个期间的同一度量值相比较。“期间”同样是指日期维度的时间粒度，当它是一年中的第一个期间时（比如1月），上一个期间就是上一年的最后一个期间（比如上一年的12月）。特别地，当时间粒度选择为年时，环比和同比是一样的结果 。

![[314b5404a7b25c13121263af8080ceaa_MD5.png]]

同环比图示

俗称的“同比”和“环比”，一般是指“同比增长率”和“环比增长率”，即：

```
（当前期间的值 - 相比较期间的值） ÷ 相比较期间的值 × 100％
```

上述公式中，100%就是1，对于结果没有影响，它只是描述了要以百分数来表示。在分析工具中，设置数字格式即可。

另外还有两组与度量值同等单位的取值，一组是增长率中的分母，“去年同期值”或“上期值”，即：

```
相比较期间的值
```

还有一组是增长率中的分子，“同比差额”或“环比差额”，即：

```
当前期间的值 - 相比较期间的值
```

## 2 设置

度量字段的设置菜单中，有“按日期计算”设置项。鼠标移上去，拉开二级菜单之后，可以看到环比相关的三个子项，就是上述的上期值、差额和增长率；同比相关的同样有三个子项；另外还有两项累计将在接下来另外一篇中讨论。这是将度量字段设置为同比、环比的地方。

![[e07d59acda672cec78446f3c33aed141_MD5.png]]

同环比的设置

仔细观察不难发现，这些设置项是以打勾的形式表示选中，这与维度、度量，或者汇总计算方式以圆点的形式表示选中不同。圆点表示单选（一组选项中有且只有一个被选中）确认无疑，打勾是否意味着允许多选（一组选项中可以有多个同时被选中）？不是的。在菜单中的打勾，只是开关的意思。某一项选择之后勾上了表示开，同一项再次选择之后不勾了表示关。回顾一下维度字段中小计的开关是不是如此。在菜单中，按日期计算以及占比，划分在一个分组中（注意菜单中的分隔线），它们被限制为单选。与圆点表示形式不同的是，这些选项当勾选后可以再次选择以被关闭。

![[1f1beb9cace2801478638c619c672df4_MD5.png]]

菜单中的圆点与打勾

和设置占比的道理一样，同比增长率和环比增长率，通常也是要求显示成百分数的格式，而不是显示零点几几的小数格式。同样是在度量字段的设置菜单中，点开“数字格式”的设置，改一下数量单位为“百分之一（％）”。分析字段数字格式的设置，见[数据分析与可视化/分析工具总体介绍/分析字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tzhigowpj#3.3)。

![[f23c8975d56382418b25ff6deca2dcc2_MD5.png]]

增长率的数字格式

## 3 约束

需要强调“按日期计算”的前提条件，分析模型中必须有日期维度。也就是说，文本型字段、数值型字段也能表示业务上的期间或者日期，但它们不是日期型字段，没有时间粒度的定义，系统不能识别，也就无法进行相关的计算。

![[d8a9dbc9beb830522b8f09277dae2133_MD5.png]]

没有日期维度的错误示例

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

这对于有日期维度，同环比能计算出来，但小计、总计中却没有（特指由于小计、总计而减少了日期维度的场景），是同样的道理。

第二个要点是日期维度的时间粒度必须符合实际。例如时间粒度设置为“月”，没有年的信息，去年同期也就无从谈起。

![[3f117b7824e739d7fa6ace1171195d18_MD5.png]]

没有年份信息的错误示例

第三，在分析模型中，不能有多个日期字段同时出现。例如[订单日期]和[发货日期]都是日期型字段，同时作为维度出现，可能引起按日期计算的混乱。但是可以将同一个日期字段，重复创建出多个不同时间粒度的维度，且位置任意。例如月份维度在年份维度之前、季度维度和年份维度交叉等等。

![[fa2d6258642af1338fc9522f5458e6fe_MD5.png]] ![[f7bf73f7dd3a6032e0ba651efcc9ebda_MD5.png]]

不同时间粒度的日期维度可以任意摆放

## 4 数据筛选

试想一下，数据表中已提取了最近几年的业务数据，只需要查看今年的部分，那么用筛选器指定今年即可。同时，关注的目标又包含了月份的同比增长率，它与去年的数据相关。如果今年以前的数据被过滤掉，同比还能算得出来吗？

### 4.1 特殊规则

回顾之前的章节，[数据分析与可视化/分析工具总体介绍/筛选器](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tk4lat0ic#5)，已探讨过数据筛选发生的时机。如果将日期的筛选延迟到下图所示③的时机，即聚合运算生成立方体模型之后，可以解决问题吗？先聚合运算生成立方体模型（维度中包含月份），再计算同环比，然后过滤掉立方体中今年以前的月份……

![[8dba28ba93de27f2836d13f887d4b508_MD5.png]]

数据筛选的时机

看起来似乎完美。但是，这要求筛选条件必须是立方体中的维度，否则明细数据已被聚合进立方体的单元中，就不可能再过滤掉了。

举个例子，分析模型中以“年月”作为维度进行聚合运算，筛选器是日期范围类型。如果试图先聚合再筛选，聚合之后立方体只有“年月”的划分，已经没有了精确到天的日期范围的信息，筛选就不可能进行了。

再举个例子，分析模型中以“月”作为维度进行聚合运算，注意是月份，没有年的信息，筛选器类型是日期字段以“年”为粒度的选项型（就是按年打勾的模式）。这时候不同年份的数据会聚合到月份维度中，聚合后想再按年份筛选已经不可能了。

综上，系统约定了，当度量字段中包含了按日期计算的设置时，如果筛选器字段的时间粒度与日期维度的时间粒度相同时（指选项型筛选器，不包括日期范围型筛选器），筛选的发生会延迟到聚合运算完成且按日期计算结束之后，以便“上一期”、“去年同期”的值可以参与计算，不会提前被过滤掉。

![[bbc623540a841836f118a940e0d655d5_MD5.png]] ![[89b4b311c2b825cfbba9732de07c06be_MD5.png]]

是否延迟筛选示例

尽管如此，筛选的时机最迟也必须在小计、总计之前，否则影响可能存在的其它度量。因此，如果开启了小计，并希望在小计中也显示同环比（注：这里特指没有因为小计而减少日期维度的场景），受筛选影响的部分也无法实现。这时候，可能就不得不考虑下一小节的办法——妥协。

### 4.2 妥协

这是尝试从另一个角度解决问题。

“数据分析”工具用于将杂乱的数据进行统计，从中获取有价值的信息，这是一个人机交互的过程。因此，它不会也不应该像报表开发工具一样强调制作出来的报表的格式。既然如此，如果希望得到同环比的信息，数据筛选时主动把参与比较的去年同期、上一期也包含进来，结果只是视图上多出一些单元格，并不妨碍目标信息的获取。

![[841fec733723e301c6eca84a7fea7306_MD5.png]]

主动带上参与计算的数据

## 5 计算字段

直接在度量字段上设置同环比就可以，不需要创建计算字段写表达式。

对于早期的一些版本，可能菜单中没有同比差额和环比差额，计算字段中也未提供直接的获取方式。如果需要，则可以用计算字段，通过增长率乘以相比较期间值算回去。即增长率乘以它的分母，得到它的分子。

同比的函数是`SAMEPERIODLASTYEAR(aggExp [,type])`，环比的函数是`LASTPERIOD(aggExp [,type])`。参数的形式是一样的。其中，参数aggExp通常是一个聚合函数。方括号括起来的参数type表示可以缺省，缺省时就是取增长率。type可以用字符串"ratio"表示同环比增长率，“value"表示同期或上期值，“diff"表示同环比差额。在计算字段编辑窗口的函数说明里可以查到语法及参数说明，或者查看[公式系统/分析引擎特有函数](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Ttcwdijj1#2)。

举几个例子：

销售额的同比增长率：`SAMEPERIODLASTYEAR(SUM([销售额]))`；

销售额的上期值：`LASTPERIOD(SUM([销售额]),"value")`；

销售额的环比差额：`LASTPERIOD(SUM([销售额]),"diff")`；

环比差额早期写法：`LASTPERIOD(SUM([销售额]),"ratio") * LASTPERIOD(SUM([销售额]),"value")`。