# 2.4.3.8数据斗方-图表-地图类

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E6%96%97%E6%96%B9/%E5%9B%BE%E8%A1%A8/%E5%9C%B0%E5%9B%BE%E7%B1%BB/content.html?timestamp=1775632793450

---

# 地图类

地图类图表有“地图”、“GIS地图”。

![[19d09c30b2bc9865c118ecc7c99dc0ca_MD5.png]]

地图类的图表

这是两种完全不同的地图类型。其中，“地图”也称“SVG地图”或“矢量地图”，配套了地图设计器，可以自由定义地图上的各个区域。“GIS地图”也称“地理信息地图”，封装了百度地图开放平台的基本显示服务，以经纬度标记地点显示信息。

在数据可视化呈现上，“矢量地图”分区域按度量值映射成深浅变化的颜色填色。“地理信息地图”则按经纬度确定的点显示为不同分类颜色的锚点，或者再加上度量值呈现为气泡。

## 1 地图

### 1.1 综述

**地图**

“地图”也称“SVG地图”或“矢量地图”。从实现技术上看，它是由SVG矢量图形描绘的一个个的区域。从地图内容上看，它既可以表达国家、省、市等行政区划，也可以表达超市平面图、仓库货架图、员工坐位图等等各种自定义的图示。从使用场景上看，它既可以和度量字段绑定，不同区域填充上深浅变化的连续值颜色，以实现数据的可视化；也可以在仪表板中作为联动源组件，成为像下拉列表一样的筛选器，以实现图形化的选择。

**地图模板**

地图模板可以简单理解为一个未绑定数据的“空壳”地图。使用图表时必须选择具体的地图模板，这个步骤是地图与其它图表的显著差异。

系统提供了地图库，并预置了《中国省份示意图》等一些模板以抛砖引玉。地图库中有地图设计器，可以创建自己的地图模板。对于地图来说，往往需要结合具体的业务场景，去自行编辑地图模板。

地图库及地图设计器的说明，详见：

[辅助功能与公共资源/公共资源管理/地图库](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T0j8lblkl)、

[辅助功能与公共资源/地图设计器](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tbxdhvrpr)。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

预置模板为什么称为《中国省份示意图》，而不是《中国地图》？一方面是研发团队没有地图出版资质，另一方面图中对于众多岛屿等细节并未详细标示（如果按实际比例标示也会因为太小而看不见）。尽管预置模板在专业性上可能不够充分，但国家领土完整一寸也不能少！

**地域映射**

图表的设计过程中，还有一个必不可少的步骤是地域映射。设计过程中定义好维度字段后，将维度成员与地图模板中的区域名称绑定，这个步骤称为“地域映射”。

技术原理上，多维计算引擎将明细数据按维度聚合，可视化引擎再将维度成员按照映射关系，呈现到地图模板上。因此，地域映射时，一般是一一对应的关系。

理论上也允许例外，同一数据可以绑定到不同的区域名称，例如[地区]维度的“东北”，可以绑定到按省份划分的地图模板的黑龙江、吉林、辽宁三个区域名称上，三个省份将会填上相同的颜色。

但是反过来不行，例如位于广东省内的多家门店，不可能绑定到按省份划分的地图模板的广东省区域名称上。必须有一个字段表示它们属于哪个省，以此字段作为维度，再映射到地图模板上。所谓的多个地点要绑定到地图的一个区域上，本质上应该是多维计算引擎的聚合运算，也就是要求有这么一个维度字段。而如果是多家门店要分别展示出来，则应该考虑使用GIS地图。

### 1.2 图表的制作与呈现

#### 1.2.1 图表的制作

图表类型确定为“地图”。①在图表属性区的“地图模板”属性项上，选择地图模板。②在“地域”功能卡中创建一个维度字段，在“数值（颜色）”功能卡中创建一个度量字段。③再次到图表属性区，从“地域映射”属性条目弹出设置窗口，为每一个地图上的区域，选择对应的数据项进行映射。以上操作完成后，即可呈现出度量值按颜色可视化呈现的地图。

![[ef0d948b1b3a8de309983c34cdba1951_MD5.png]]

制作地图图表

如果关注更多的信息，可以将其它字段拖入“详细信息”功能卡中，创建为维度或者度量都是允许的。这样，通过鼠标悬停信息，可以按区域阅读到各自的这些详情。需要注意的是，以“地域”功能卡中的唯一一个维度为准，计算更多的度量，自然没有问题；但是添加更多的维度，在这里实则是属性，这才能仍以地域维度为基准。因此，如果以[省份]字段作为地域维度，加入[地区]详细信息（粒度更大），“广东省”的地区可以显示为“中南”；如果仍以[省份]字段作为地域维度，加入[城市]、[产品名称]详细信息（粒度更小或无关），“广东省”的城市显示为星号，产品名称也显示为星号，因为它们不唯一。

![[cc9ee0bdc121a10eae2d26c8905b7ee2_MD5.png]]

悬停信息中的更多详情

#### 1.2.2 图表的呈现

使用过程中，随着卡片调整大小，图表显示范围变化，地图都能在保持宽高比例的前提下，最大地显示在可视范围内。

当鼠标移到地图的某块区域上时，跟随鼠标的悬停信息，将显示名称（数据上“地域”维度字段的值，不是地图模板的区域名称）、度量值以及其它详细信息。如果未设置“数值（颜色）”度量字段，或者“地域”维度字段没有值映射到区域上，该区域会以地图模板中定义的鼠标划过颜色来显示。

### 1.3 图表属性

#### 1.3.1 “地图”属性分类

**● 地图模板**

从地图库选择地图模板。这是使用地图图表的必要步骤。

**● 地域映射**

设置地图模板中的每个区域与“地域”维度字段值的映射关系。

在设置地域映射时，如果是从未映射过，比如第一次打开窗口，或者属性项清除后重新打开窗口，数据项刚好与地图模板上的区域名称一样，则会自动匹配上。这一点可以延伸出有意思的思考：如果数据在此之前经过加工处理，是否可以在处理阶段让其与地图模板的区域名称一致；如果地图模板是通过地图设计器自己定义的，是不是应该让区域名称的命名与数据一致。

在设置地域映射时，如果数据项成百上千个，不容易选择，这很可能是用法的问题。地图模板上的区域，通常达到数十项就已经足够眼花缭乱了，而数据和区域应该是一一对应的才对。区域不多而数据很多，要么是存在很多“多余”的数据，要么是维度字段用错了。

当然，有时候这种很多“多余”数据的情况也是合理存在的，特别是交互式的数据探索过程。例如有一份全国所有城市的数据，现在只关注广东省的部分，要呈现在广东省按城市划分的地图上。那么，以[省份]字段作为筛选器并选择“广东省”，这一步显而易见。地图模板上的区域，广东省的地级市是二十来个，而数据选项却是全国的数百个。

此时可以再添加一个[城市]字段（与“地域”维度相同）作为筛选器，并设置为“备选值仅为相关值”，保持全选。回到地域映射设置弹窗，数据选项就只有广东省的城市了。映射完成后再删除这个筛选器字段。

![[aa93b7e4a989f61a72d3569c07c444bc_MD5.png]] ![[f65b4a94b0362316c85b921ddae3df0b_MD5.png]] ![[8608f471bd7dea177b3d8f681f44ba25_MD5.png]]

用筛选器辅助控制地域映射的选项

#### 1.3.2 “悬停信息”属性分类

**● 自动展示**

当用于大屏场景时，可以尝试开启悬停信息的“自动展示”开关。当此开关开启，虽然没有人操作鼠标，但悬停信息会自动按照地图模板上的区域顺序，依次显示，循环不断。

“自动展示”开关开启后会出现另一个属性“每项时长（秒）”，指悬停信息在每一项上的停留时间。

#### 1.3.3 其它

“绘图区”属性分类，调色板属性，详见[数据分析与可视化/数据斗方/设计器之图表属性/调色板](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Thwwslr0o)。地图仅支持连续值颜色。

“图例”属性分类，详见[数据分析与可视化/数据斗方/设计器之图表属性/分类：图例](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Trbqb82u9)。

“通用”属性分类，详见[数据分析与可视化/数据斗方/设计器之图表属性/分类：通用](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tipglthcp)。

### 1.4 进阶应用示例

 **地图应用示例1：** 变形的世界地图

如果业务跨多个国家或地区，直接使用标准的世界地图，可能不是好的选择。面积大的国家（地区）如俄罗斯，在地图上一大片；面积小的国家（地区）如梵蒂冈，一个像素点都不到。根据具体的业务需求，通过地图设计器定制地图，适当地进行变形，特别是将面积小的目标夸大，是可行的做法。

在这个例子中，目标地区是曾经的亚洲四小龙：韩国、中国台湾省、中国香港特别行政区、新加坡，假设某企业的业务就在这四个地方开展。使用标准地图，香港和新加坡实在太小了，几乎看不到。可以尝试制作一个图片，其中有放大了的香港和新加坡。用这个图片放到地图设计器里作为底图，它将用于辅助描绘区域图层的路径，也可以作为地图的一部分最终呈现（要“携带底图输出”）。

在数据斗方中，选择使用这个定制的地图，将业务数据中的地区与地图中的区域映射。这样，韩国和台湾省面积不算太小，直接按比例呈现，而香港和新加坡经过处理也有足够大的面积来呈现信息了。

![[296bdbd277d889203dca9388adcd49e9_MD5.png]] ![[8e2eb07040226d1d8de57fc6d0fd232a_MD5.png]]

地图应用示例1

 **地图应用示例2：** 楼盘选择器

这个例子中，目标是要通过一个显示住宅小区楼房的图片，可视化地选择具体某一栋楼，进而展示这一栋楼的更多信息。

将小区楼房的图片作为图地设计器的底图。按照楼栋的轮廓描绘路径，每一栋楼是一个区域图层。必须选择“携带底图输出”，区域的填充颜色选择透明，鼠标划过的填充颜色选择醒目颜色的半透明（醒目颜色是为了提示选中某一栋楼，半透明是为了还能看得见楼栋）。

在仪表板中，以呈现地图的数据斗方组件作为联动源，联动其它数据斗方的图表。其中，在数据斗方设计过程中，地图图表不设置度量字段，这样楼栋就不会被数据填上颜色，而是保持透明。如此，地图图表仅起到一个类似筛选器的用法。

![[36874d230429d7b561c2beee036413fb_MD5.png]] ![[ff8b13bf10effcaf237ea2319f30f119_MD5.png]] ![[e7ec89d4901c8ce6cad391c1850f31b2_MD5.png]]

地图应用示例2

## 2 GIS地图

### 2.1 综述

“GIS地图”也称“地理信息地图”。是以现实世界的地图为基础，在地球上的某些地方标示数据的一种数据可视化方式。

数据斗方的“GIS地图”封装了百度地图开放平台的基本显示服务，提供地理信息地图的展示。地图是一个可缩放的、可在显示窗口内移动的世界地图，使用时数据需要自带经度和纬度，以表示在地图上的某个点。经度是西经180度到东经180度，其中西经用负数，即-180≤经度≤180；纬度是南纬90度到北纬90度，其中南纬用负数，即-90≤纬度≤90。整数部分表示度，比度更小的单位分和秒则换算成小数表示。

### 2.2 图表的制作与呈现

#### 2.2.1 图表的制作

图表类型确定为“GIS地图”，将数据中表示经度和纬度的字段，依次拖入“经度、纬度”功能卡中，分别创建维度字段。这就可以在地图上显示出标记点来。

在GIS地图中，以经度和纬度作为多维计算的维度，“名称”等其它文字信息都只是其相关的属性。而且，这也是为数不多的以数值类型的字段作为维度的合理情况。

虽然以上操作，有了经纬度就可以出现标记点，但通常至少还需要一个名称。将表示地点名称的字段，拖入“名称”功能卡中，创建为维度字段（本质上是属性）。这样，当鼠标移动到某个标记点上时，悬停信息将显示该地点的名称。

![[a57f550df9a2aac9311ce1c3cb19b5aa_MD5.png]]

制作GIS地图（基本）

在已经有经纬度的前提下，可以在“详细信息”功能卡中创建更多的维度或者度量字段，这些信息都将显示在悬停信息窗口中。

![[d61d7f444d940fb2f18147d35074d6b9_MD5.png]]

制作GIS地图（带详细信息）

在已经有经纬度的前提下，可以在“颜色”功能卡中创建一个用于分类的维度字段。按照维度成员的不同表示分类项，标记点将呈现为不同的颜色。具体颜色可以在调色板属性中配置。

![[20b4f6b7504b48c02e3a1ccdc7f9cdc0_MD5.png]]

制作GIS地图（带颜色）

在已经有经纬度的前提下，可以在“大小”功能卡中创建一个度量字段，同时需要将图表属性中的“标记”项的值，由“位置标记”改成“气泡”。这样，度量字段的数值将映射成圆形气泡大小，作为一个标记。

![[5402ef592adcc849500ba0f11ec32b34_MD5.png]]

制作GIS地图（气泡）

值得注意的是，“大小”只能表示气泡，对于位置标记无效。“大小”和“颜色”可以分别出现，也可以同时出现。另外，上文已经阐明，只有“经度、纬度”是维度，它确定了具体的地点，其它功能卡中出现的维度字段，本质上都是它的属性。因此，如果属性值不唯一，将会显示为星号。

#### 2.2.2 图表的呈现

根据图表的“标记”属性所指定，地图上呈现为位置标记或者圆形气泡。这是数据斗方按照业务数据叠加在地图上的图层。底下的地图则是百度地图本身的呈现，包括悬停信息窗口也是明显与其它图表不同。

默认情况下，地图通过按住鼠标左键的拖拽可以移动，通过鼠标滚轮可以放大缩小。在数据斗方设计器中，对地图的预览状态，将会跟随方案保存。换句话说，在设计器中将地图调整到某个适当的位置（以中心点为准），发布之后打开阅览，就是显示在这个位置。

尽管地理信息地图使用的是第三方的服务，但也并非只有默认的显示状态不能改动。百度地图开放平台提供了很多API，可以在初始化过程中对地图进行配置，详见下文的“自定义脚本”的说明。但是，也是受限于第三方平台的设计理念和API的能力，有些想法可能也不能或不容易实现，例如想在地图中只显示中国不显示外国，就没有发现合适的做法。

### 2.3 图表属性

#### 2.3.1 “GIS地图”属性分类

**● 自定义脚本**

GIS地图的“自定义脚本”属性，支持写一些简单的JavaScript代码，在地图初始化的过程中，可以做个性化的定制。

其中，有两个特别的变量，命名以下划线开头。一个是`_target`，是百度地图核心类Map的实例，就是地图本身，所有的代码都应该与其相关。另一个是轻分析提供的接口`_helper`。

百度地图的API，可在其官网查看，[百度地图开放平台 JavaScript API v3.0类参考](https://mapopen-pub-jsapi.bj.bcebos.com/jsapi/reference/jsapi_reference_3_0.html)。注意是v3.0这个版本。

 **GIS地图自定义脚本示例1：** 禁用默认的鼠标滚轮缩放操作，改成在界面上放置缩放控件，效果就变成点击加减号按钮缩放地图。

在自定义脚本输入框中写如下两行代码。第一行禁用了滚轮操作。第二行则添加了一个平移缩放控件，类型type是标准的大号，停靠位置anchor是右上角。

```
_target.disableScrollWheelZoom();
_target.addControl(new BMap.NavigationControl({
    type: BMAP_NAVIGATION_CONTROL_LARGE,
    anchor: BMAP_ANCHOR_TOP_RIGHT
}));
```

![[cd9426a72d9d4712ed4a749fffe7e8d5_MD5.png]]

自定义脚本示例1

 **GIS地图自定义脚本示例2：** 指定地图中心点及缩放级别。

这个示例做了画蛇添足的事情。因为在设计器中，地图移动到某个位置，调整好缩放级别，然后发布（保存方案），以后打开就是以此位置及缩放级别为初始状态，没有必要再用代码去设置。这里只是演示了自定义脚本的可能性，并强调系统其实会自动保存状态这一点。

代码中，以东经113度、北纬25度为中心点，并显示为第6级缩放。

```
_target.centerAndZoom(new BMap.Point(113, 25), 6);
```

![[527222a1cd40a385e7ff272b608114a0_MD5.png]]

自定义脚本示例2

 **GIS地图自定义脚本示例3：** 匹配仪表板的外观风格。

GIS地图的默认状态匹配白色的外观。但是仪表板中有深色的外观风格，如果处于这样的环境中，可以尝试改变地图的样式。

```
_target.setMapStyleV2({styleId: _helper.getStyleIdByName("darkBlue")});
```

这里用到了`_helper`的`getStyleIdByName()`方法，通过一个名称获取到一份地图样式配置。与仪表板预置的外观风格名称对应，轻分析提供了如下一组可供使用的名称。

- ◆ 淡雅白：white（默认）
- ◆ 深邃蓝：darkBlue
- ◆ 静谧青：cyan
- ◆ 土豪金：gold

![[d1a485b6379603df188200e19b8dd49a_MD5.png]]

自定义脚本示例3

#### 2.3.2 “绘图区”属性分类

**● 标记**

“标记”属性用于选择地图上标记点的形式，有“位置标记”和“气泡”二个选项。这既是标记点的外观，又是数据呈现模式。

位置标记是典型的上圆下尖的倒水滴形状，该模式仅标明位置，其它数据都必须通过悬停信息阅读。

气泡的形状是圆形，该模式除了标明位置，还可以通过圆形的大小将一个度量值进行可视化呈现。

**● 调色板**

GIS地图的标记点颜色呈现是离散值颜色，调色板详见[数据分析与可视化/数据斗方/设计器之图表属性/调色板](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Thwwslr0o)。

#### 2.3.3 其它

“通用”属性分类，详见[数据分析与可视化/数据斗方/设计器之图表属性/分类：通用](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tipglthcp)。

### 2.4 关于经纬度

GIS地图要求数据中必须有经度和纬度两个字段，并且假设直接来自业务系统的数据、或者经过加工处理的数据，就应该带有这样的信息。

如果确实没有，也可以在数据准备过程中，自行对表示地点的维表，补充关联上带有经纬度的属性表。经纬度的数值，从百度地图的[坐标拾取器](https://api.map.baidu.com/lbsapi/getpoint/index.html)可以手工获取得到，保存到数据库或者Excel表格，就能作为关联的数据表。百度地图开放平台的网站上也有开发资源以供下载，包括了国内城市的经纬度信息（JSON格式）等等。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

为什么没有做地址到坐标的自动转换？因为轻分析的图表只承载数据的可视化环节，且对数据是只读的。如果在这个阶段，每次展现甚至于视图刷新，都要重复去调用第三方API做转换，既低效又浪费。数据的加工处理，应该在之前的环节就完成。

虽然经纬度是标准统一的理论，但具体的坐标系却有所差异。不同的坐标系可能有不同的加密，如果混用在地图上呈现出来就会有位置的偏移。百度地图服务使用BD09坐标系，其它还有WGS84、GCJ02等坐标系。通过百度地图服务的坐标拾取器或者API生成的经纬度，用在百度地图上，不会有位置偏移的问题。更多的信息，可以参考[百度地图开放平台](https://lbsyun.baidu.com/)的相关文档。

坐标的精度问题，6位小数可以精确到大约一米，这足以表示缩放到街道级别的地址。如果只是表示省份、城市等大致位置，2位小数也已经足够了，而且也不用过多担心不同坐标系加密的差异。

需要补充说明的是，如果在数据斗方设计器中随意使用两个维度表示经纬度，不能正确识别为数字，标记点将会出现在经度为0、纬度为0的地方，即本初子午线和赤道的交叉位置，这个地方在非洲西部的海面上。