# 1.2.3定义阶段-关联数据表

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%87%86%E5%A4%87/%E5%AE%9A%E4%B9%89%E9%98%B6%E6%AE%B5/%E5%85%B3%E8%81%94%E6%95%B0%E6%8D%AE%E8%A1%A8/content.html?timestamp=1774595121608

---
# 关联数据表

表关联相当于SQL查询语句中的“join”，就是两个表左右拼接，这在 [数据准备/入门基础/基本概念](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tmwv6m6pb#3) 第3小节中已做了介绍。

怎样设置关联关系，见 [数据准备/定义阶段/操作概要](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#5) 第5小节。

## 1 再论关联

假设存在这样一个业务模块的数据存储设计，“售货单”表记录了商品卖出的每一笔交易，一笔交易就是一行记录，每一行都由[ID]字段的值标识了它的唯一性；“售货单明细”表是售货单的分录，每一笔交易可能卖出多种商品，一种商品是一行记录，[单据ID]字段表示它属于哪一笔交易。

因此，字段[售货单.ID]和字段[售货单明细.单据ID]就是正确的关联关系。

![[c0ab2437dd2fdcd732949d25c1ab7fcd_MD5.png]]

关联关系

当[关联发生](https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%87%86%E5%A4%87/%E5%AE%9A%E4%B9%89%E9%98%B6%E6%AE%B5/%E5%85%B3%E8%81%94%E6%95%B0%E6%8D%AE%E8%A1%A8/content.html?timestamp=1774595121608#6)时，两个表按照关联字段的值相同的对应关系，进行拼接。

![[dcfce6b6d366d963bc17105be6da957b_MD5.png]]

表关联

## 2 对应关系与统计结果翻倍

从上述的例子及插图可以看到，在“一对多”关系中，“一”端的表的每一行总是会被“多”端的表撑开成为多行。反过来“多对一”是同样的道理。

![[db2042ca279c3939fe3c29311523126e_MD5.png]]

关联后的数据

这会不会使得在后续的分析过程中，“一”端的度量字段的统计结果（特指求和）翻倍？答案是不会。只要对应关系“一对多”的设置，符合实际情况，系统（指“数据分析”和“数据斗方”的分析引擎）能够自动感知到“‘一’的一端被撑开”的客观事实，从而进行正确的处理。

如果错误地将“一对多”设置成“一对一”或者“多对一”，则系统被错误地引导，翻倍的现象将会发生。

## 3 保留无法关联的行

关于设置界面上“保留无法关联的行”选项，如果了解SQL查询语言，组合起来就是内连接与外连接。

- ◆ 两边都不勾选，相当于内连接“inner join”；
- ◆ 两边同时勾选，相当于全外连接“full outer join”。
- ◆ 左边勾选，相当于左外连接“left join”；
- ◆ 右边勾选，相当于右外连接“right join”；

![[886e2a0d1c99144c9e9b7f118ffdf24d_MD5.png]]

“保留无法关联的行”与关联类型对比

不熟悉SQL也没关系，可以结合下图的例子来理解。通常情况下，两个表的关联字段的值能对应上，才做拼接，这是两边都不“保留无法关联的行”，也就是所谓的内连接。对于单据与其分录，如果业务逻辑上允许一个单据没有分录，那么应该让单据“保留无法关联的行”；反过来，分录没有对应的单据应该是不合理的。两头都“保留无法关联的行”则一般没有必要。

![[85ee88b657e5584a18027dd78ec67e78_MD5.png]]

不同“保留无法关联的行”结果对比

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

绝大部分情况下，不应该让两边的表都“保留无法关联的行”,一来没有必要，二来对性能的影响很大。

## 4 错误设置了关联字段的后果

如果错误地设置了关联字段，两边的值无法匹配。当两边都不开启“保留无法关联的行”时，很明显，结果将是没有数据。而开启“保留无法关联的行”，没有关联上，结果也不对。

![[67fc8577350f6cbef1008b417e1c0e1b_MD5.png]]

错误的关联字段

## 5 业务实体

### 5.1 实体内部关系

以业务实体作为数据表的数据源时，常见的是实体封装了单据及其分录。在已创建的数据表列表中，实体表现成树形的层级结构。它们看起来是多个数据表（当进行数据提取，在物理存在上确实也是多个），但相互之间却不能创建关系。这是由于它们的关系已经明确，在实体元数据内部已有描述。

在“数据准备”定义阶段，设置界面上虽然不能主动创建实体内部的关系，但允许对实体类型的数据表“编辑内部关系”。在这些内部关系中，表、字段及对应关系是确定不变的，可编辑的是是否开启“保留无法关联的行”。

![[b39de8bbbb7a96733b05c6e9799d6c1b_MD5.png]]

实体类型的数据表“编辑内部关系”

### 5.2 实体中的基础资料名称

对于实体数据源，还有另外一个特性。实体中的外键字段（基础资料字段是以外键关联到基础资料表）的值，本来就是ID。为了方便使用，轻分析在数据提取过程中，自动将这些外键关联到基础资料表，并取得基础资料名称字段的值，将其替换。这样，基础资料字段都能显示为中文名称。

![[aec9986d3e755de6703fd55f69cbfe0f_MD5.png]]

实体中的基础资料字段自动显示为中文

不过，一旦外键字段被设置了其它关联，则其ID值必须保留，从而自动替换成名称的特性就失效了。

这种情况下，有两种常用的解决方法。方法一是在“数据准备”的设置中主动引入基础资料表做关联，并且在分析时主动使用基础资料表的名称字段。方法二是在“数据准备”的设置中，为实体数据表创建一个计算字段，该字段引用基础资料外键字段（相当于复制了一列），新的字段用来做必要的对外的关联，而原来的字段则仍然保持了自动被替换成名称的特性。相对而言，方法二更为高效，且后续使用起来也更为便捷。

## 6 设置上的限制

### 6.1 关系的数量

在“数据准备”的“关系”设置中，有一些逻辑合理性的约束：两个数据表之间只能建立一个关系，且多个表之间的关系不能形成环状。

两个数据表之间只能建立一个关系。这对于绝大多数以主外键进行关联的表设计来说，已经足够了。如果存在联合主键，即由两个字段的值决定每一行的唯一性，只需要创建计算字段，将两个字段拼接起来，用来设置关联关系即可。

多个表之间的关系不能形成闭环。环状意味着从一个表到另一个表，存在两条不同的路径，这让系统无法识别“从哪走”。从业务的逻辑关系上，存在唯一的一条关系路径，足以将关系描述清楚并正确地解决问题。而对于确实有多种不同关系的情况，或许应该考虑在主题式分析中创建不同的“业务主题”。

另外，再举一个例子。对于一个星型模型，事实表中存在“创建人”、“经手人”、“审核人”等等与人相关的维度，它们可能需要同时关联到“人员”维表。不能创建多个关系，是不是就没法进行了？这种情况下，创建多个“人员”相关的数据表作为维表即可，本来逻辑视图就是如此。至于系统在实现中，会不会将同一数据源的多个表，优化成同一物理存在，这是系统的内部实现，就无需过多操心了。

![[cdf9d2284f1c5d87b98fda049a63da19_MD5.png]]

两个表间的多个关系

### 6.2 多对多

在设置关联关系时，对应关系只有“一对一”、“一对多”、“多对一”，没有“多对多”。这是因为“多对多”的关系只出现在逻辑模型中，物理模型不出现。换句话说，就是概念上允许“多对多”，但数据库的表设计，不应该直接出现两个表之间的“多对多”关系。当有“多对多”的关系存在时，通常出现第三个表（中间表）用于描述这个对应关系。中间表与前两者分别都是“多对一”的关系。

![[d6299713bca8dc6952a9e9782b9a310b_MD5.png]]

间接的“多对多”关系

这样的“多对多”关系，按实际情况，所有的对应关系都描述在了中间表当中，数据量也是由中间表决定的。再怎么关联，数据的总行数都不会超过中间表的行数。

而另外一种情况则不同，考虑多分录的单据，单据与不同分录同时关联，也会产生“多对多”的实际效果。分录A将每一行单据撑开成多行，分录B再将已撑开多行的单据逐行进一步撑开多行，m*n的效果就出来了。假如还有第三个、第四个分录呢，数据量的爆炸性增长不言而喻。

![[e31698010530fe763a9547e7313d7d3d_MD5.png]]

单据与其多个分录的对应关系

![[67490da48b777bd51a94cd33d9b5162f_MD5.png]]

单据头与其两个分录同时关联

这种有多个分录同时参与分析的情形，单据头的字段可以作为维度也可以作为度量，但各个分录的字段则只能作为度量。换一种说法，多分录交叉将单据撑开多行展示是没有意义的。

![[1b29a44ab040fd6f43fe05e1b5e12bb6_MD5.png]]

单据多分录同时关联的使用

## 7 关联的模式

在传统BI的方法论中，有“星型模型”的设计，通常数据来源于数据仓库。在“星型模型”中，中间是一个“事实表”，周围是多个“维表”，事实表与维表是“多对一”的关系。

![[f8df27baf33e236108180c25c685f4c4_MD5.png]]

星型模型

在业务系统的单据的设计中，常见单据有多分录。单据（也习惯称为“单据头”）与它的各个分录（也习惯称为“单据体”），是“一对多”的关系。如果是以实体作为数据源，这些关系包含在实体“内部关系”中，无须修改。

![[7cc3e865d1d61ee7ae168dc4dea052ed_MD5.png]]

单据与分录

至于单据与单据之间（也有可能是不同单据的分录之间）的关系，是“一对多”还是“多对一”，则必须按照实际业务逻辑去设置。

## 8 关联发生的时机

表关联发生的时机，并不是发生在“数据准备”的数据提取阶段。

“数据准备”在进行数据提取时，按所定义的数据表，逐一提取数据。这个过程不做关联。只有当后续使用时，分析工具中使用到不同数据表的字段，分析引擎在执行过程中，才会动态地去做关联。

这也意味着，两个数据表的数据，来源于不同的数据库、来源于平面数据文件，都可以关联。