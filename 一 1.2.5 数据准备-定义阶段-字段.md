# 1.2.5定义阶段-字段

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%87%86%E5%A4%87/%E5%AE%9A%E4%B9%89%E9%98%B6%E6%AE%B5/%E5%AD%97%E6%AE%B5/content.html?timestamp=1774595172448

---

# 字段

在前面的章节中，[数据准备/定义阶段/操作概要 第2小节 数据表的列](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T94t32k8d#2) 已经对字段及相关的操作有了较为全面的介绍，不再一一重复。对于未提及的问题，本文再做一些补充。

## 1 “消失”的字段

以业务实体作为数据源，偶尔会有字段不出现的情况。业务单据中存在的属性，在“数据准备”选择字段时的可选列表中却没有出现。这有可能是开发人员有意控制了字段不“支持轻分析”。具体可见 [常见问题/苍穹/“业务实体未开启‘支持轻分析’”第3小节 拓展](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tyd1f0nw5#3) 中的论述。还有一种可能，是实体中定义的属性，没有绑数据库的字段，只是一种业务单据在前端展示时加工处理的逻辑。这种属性也不会出现在字段可选列表中。

另外，如果字段的数据类型是二进制对象（BLOB）等，轻分析也不予支持。这是由于这样的字段既不能直接用于多维分析，又严重影响数据提取的性能。

## 2 弹性域

业务实体中存在一种叫做“弹性域”的字段。允许每一行数据中，在“弹性域”这个字段下，记录若干个字段的值，行与行之间的字段不完全一样。

![[4ccc8c8c5b4143b9a876d7dee9c8a7cc_MD5.png]] ![[eac96430c8d6608f41efafcccc6e6ea0_MD5.png]]

业务系统中的弹性域

从“数据准备”的预览数据中可看出，一个字段中组合了多个不确定的字段的值（预览数据经过显示上的加工处理，但本质上没有变化）。这虽然违背了数据库设计范式，但想必也是开发平台在业务处理中妥协的最佳实践。

![[b666466cf62936e697f174b40ba0d419_MD5.png]]

“数据准备”预览数据中的弹性域

然而，多个信息合并在一个字段中，对于分析系统是莫大的障碍。因此，轻分析会将弹性域拆解出各个内部字段的[Number]和[Name]（通常叫[编码]和[名称]）属性，变成“数据表”中的字段，以供分析工具使用。并且，对于一份确定的数据，如果内部字段在全部行中都没有值（在全部行的弹性域字段不出现），系统将自动将其隐藏。

![[b5df3a6d5dec548ae6a2c8e07f80f395_MD5.png]]

“弹性域”字段在数据分析工具中

## 3 列转行

通常，业务系统在数据的存储设计上，字段应该是与对象的“属性”对应，而不会与“属性值”对应。后者更多的是一种统计结果的视图，例如轻分析的“数据分析”工具就可以做出这种“交叉表”。

![[9d37b8600c9f50086c79ed24e977e6f0_MD5.png]]

正常情况下字段与属性对应

但是，对于将Excel文件等作为数据源，很容易这种交叉表的视图就会成为“数据表”。将它转换为字段与属性对应，以方便后续分析工具的使用，就是“列转行”要解决的问题。

![[1151a1fda66b1e1063082dede37a7205_MD5.png]]

列转行数据示例

 **示例：** 以“地区”和“月份”这份数据为例，其中“月份”是横向展开的，表现成字段[地区]、[一月]、[二月]、[三月]……。

（1）在数据表的操作菜单上选择“列转行”（已设置过的数据表，此处相应变为“修改列转行”）。

![[231aa161dddbea6d92a09bc4fc0378a9_MD5.png]]

列转行操作示例（1）

（2）在设置窗口中，表格表示的是转换后的结构。右侧“目标值列”，逐行依次选择转换前的字段[一月]、[二月]、[三月]。对应的，左侧“目标属性列”，会依次填上转换后的属性值“一月”、“二月”、“三月”。

![[698327f2a2a1f723243cdeb733f416df_MD5.png]]

列转行操作示例（2）

（3）编辑表头的两个转换后的字段名称。左侧是“一月”、“二月”、“三月”的属性名“月份”；右侧是数值对应的名称“销售额”。

![[715c2be83d0084691fcc5cb1972fa264_MD5.png]]

列转行操作示例（3）

（4）完成后，数据表的结构就变成了[地区]、[月份]、[销售额]三个字段。

![[e5b7e7c6ce9f403071a2489d53cab71e_MD5.png]]

列转行操作示例（4）

设置窗口中，右下角的“高级”操作，允许“添加‘目标值列’”。那是对于多个度量值，每个度量值都有重复的同一属性横向展开的结构的转换操作。

![[33ae418980a045b36ef8c0c9aa3113aa_MD5.png]]

多度量值的列转行数据示例

综上示例可以看出来，原先[地区]的“华东”一行，转换后变成了三行。“列转行”会将原来的数据一行变成多行，这是它的特征。如果表中同时有其它不需要转换的数值字段，由此产生的重复，对于后续的统计计算会直接造成错误，使用时需要慎重。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

业务实体由于与业务功能对应，通常也不会有需要转换的数据存储设计，不支持“列转行”。