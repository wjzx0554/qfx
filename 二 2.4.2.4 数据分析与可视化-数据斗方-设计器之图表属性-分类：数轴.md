# 2.4.2.4数据斗方-设计器之图表属性-分类：数轴

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E6%96%97%E6%96%B9/%E8%AE%BE%E8%AE%A1%E5%99%A8%E4%B9%8B%E5%9B%BE%E8%A1%A8%E5%B1%9E%E6%80%A7/%E5%88%86%E7%B1%BB%EF%BC%9A%E6%95%B0%E8%BD%B4/content.html?timestamp=1775613974418

---

# 分类：数轴

基于直角坐标系实现图形化，是常见的图表表达形式。柱形图、折线图就是这种类型。其中横轴表示类别，与多维计算模型的维度对应，纵轴是数轴，与多维计算模型的度量对应。将柱形图翻转，纵轴是类别，横轴是数轴，就变成条形图。如果两个轴都是数轴，分别对应不同的度量，维度分类形成一个个的点，那就是散点图。

数轴是设计器图表属性区的一个分类。根据具体的图表，分类小标题可能为“纵轴”、“横轴”，在组合图中由于存在两个纵轴，还被称为“左轴”、“右轴”。

## 1 标题/单位

该属性用于在数轴上显示出附加的文字，以表明数轴所表示的业务语义或者数量单元。在属性区条目的编辑控件上直接输入文字即可，例如填上“人数”、“元”、“销售额：万元”之类等等。

在纵轴上，文字显示在上方，与刻度值右对齐（右轴则左对齐）。在横轴上，文字显示在最右侧刻度值下方。

![[30c8afa221de608079af6b8965d01aba_MD5.png]]

数轴的“标题/单位”属性

## 2 数字格式

为数轴上的刻度单独设置数字格式。当数值比较大、数位比较多时，在数轴上显示精确的数字或小数位数会浪费空间，而且也影响阅读。在数字格式的设置中，去掉小数位数、改大数量单位都是不错的选择。

![[b30dc94f74aa52ab85f50fc58aadf544_MD5.png]]

数轴的“数字格式”属性

系统默认提供了整数的格式设置。在编辑控件上也可以将其清除，清除后显示“跟随度量字段”，即使用数轴对应的度量字段的数字格式。

百分比相关的图表（百分比堆积柱形图、百分比堆积条形图、百分比面积图）未提供数轴的“数字格式”属性项，以界面上是否出现属性条目为准。

## 3 标尺比例

“标尺比例”属性提供了“线性”、“对数”、“自动识别”三个选项。系统默认是线性比例、人类的习惯也是线性比例。

线性比例，以柱形图为例，当同时存在有的类别数值非常大，有的类别数值非常小，相差达到多个数量级（十进制的百倍、千倍……）以上时，为了能够显示较大的值，数轴刻度必然与之匹配，但此时较小的值按比例就会变得很小，进行图形化表示时，可能就看不到了。

在不得已时，改为对数比例，则数轴刻度等间距变为10、100、1000……这时，非常大的值和非常小的值都可以同时图形化显示。虽然图形都看得见了，但也违背了直觉，并不利于直观地进行比较。

![[28b2e6f1db9fc7d1660244941155cf92_MD5.png]]

数轴的“标尺比例”属性

系统中，柱形图、条形图、折线图、组合图等提供了“标尺比例”的属性设置，相应的堆积类、进度类图表，以及散点图、瀑布图等未提供该属性项，以界面上是否出现属性条目为准。

## 4 数轴范围

早期的版本，未提供数轴范围的自定义，此处对应的是属性“起始刻度”，可以选择是否“从零开始”。在新的版本中，由“数轴范围”属性取代，是否包含零刻度的选项移到数轴范围设置弹窗中。

在数轴范围的自定义设置中，范围两端的值可以分别保持“自动”或指定“固定”值。完全“自动”时，才可以设置包含零刻度的选项。

值得注意的是，设计器制作了图表，保存下来的分析方案是固定不变的，但每次执行的业务数据却是变化的。如果指定了数轴范围的固定值，以后数据变化了是否还能匹配，需要自行斟酌。

![[49e8bed18daa5eae0a6fc7adb070c1c4_MD5.png]]

数轴的“数轴范围”属性

注意“包含零刻度”选项的反面是“允许不包含……”。这里有允许两字，表明是一种系统自动识别的状态，并不总是发生。当所有类别的值都比较大时，可能对比看不出差异，把所有柱子下面大面积整齐砍掉（即不从零开始），相当于聚焦到柱子顶端部分，差异就显现出来了。关于数轴从零开始、标尺比例等可视化应用的讨论，另可参考[数据分析与可视化/基础知识/可视化实践指引](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tq8e8ysm7#2.6)。

系统中，柱形图、条形图、折线图、组合图等提供了“数轴范围”的属性设置，相应的堆积类、进度类图表，以及散点图、瀑布图等未提供该属性项，以界面上是否出现属性条目为准。

## 5 参考线

“参考线”属性出现在“数轴”分类中，但实际上参考线不画在数轴上，面是画在图形内容主体的绘图区中。根本上，参考线是与数轴对应的度量字段有关。

允许添加多条参考线。参考线的取值，有计算类的，可选度量值相关的平均值和中位数；有自定义类的，则可以填上固定值。

![[7c6b69971a8da1a1081a419bd2fa56c7_MD5.png]]

数轴的“参考线”属性

原始业务数据经过多维计算引擎的聚合运算，生成图表视图呈现所需的类别（维度）及数值（度量）。计算类的参考线则可以理解为视图上数据的再计算（此处可对比“数据分析”工具的[汇总计算方式](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tlny3kjvk#3)）。

另外，受自定义数轴范围的影响，参考线对应的值如果落在范围外，显然参考线不可能出现。反过来，如果未自定义数轴范围，则参考线对应的值（特指自定义的固定值）又会影响数轴范围。

设置了参考线之后，当鼠标移动到参考线附近时，参考线会加亮显示，且鼠标悬停信息显示相应的数值。

百分比相关的图表（百分比堆积柱形图、百分比堆积条形图、百分比面积图）以及瀑布图等未提供“参考线”属性项，以界面上是否出现属性条目为准。