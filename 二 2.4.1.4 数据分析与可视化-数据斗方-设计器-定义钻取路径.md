# 2.4.1.4数据斗方-设计器-定义钻取路径

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E6%96%97%E6%96%B9/%E8%AE%BE%E8%AE%A1%E5%99%A8/%E5%AE%9A%E4%B9%89%E9%92%BB%E5%8F%96%E8%B7%AF%E5%BE%84/content.html?timestamp=1775613791314

---

# 定义钻取路径

数据斗方区分设计阶段和执行阶段。设计阶段是使用设计器制作图表，执行阶段就是图表小卡片的展示。不像“数据分析”工具一样总是工具的形态，阅览过程可以自由更换维度的粒度，从而达到下钻、上卷的目的（参考[数据分析与可视化/数据分析/数据穿透/下钻、上卷](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tvllo36o7)）。在钻取的能力上，数据斗方回归到传统的模式，在设计器中定义钻取路径，阅览时在图表的图形元素上点击即可沿着预先定义好的路径下钻。

## 1 规则

并不是所有的图表都支持向下钻取。以纯值类（业务指标图、仪表图等）图表为例，本身就没有维度，谈何从维度的大粒度穿透到小粒度。

数据斗方常见的柱图类、折线类、饼图类等图表支持下钻。在设计器的设计区中，图表类型切换器下方的多个功能卡，最后一个如果是“可钻取到”，表明该类型的图表支持下钻的设置。

![[51eab5547e8736dff22a4221e097f410_MD5.png]]

设计器界面（钻取相关）

这些支持下钻的图表，隐藏了一个“可下钻”的概念，界面上未明确标识出来。

对于柱图类、折线类图表，直角坐标系有两个相互垂直的轴，其中一个是数轴，另一个是类别轴。类别轴是可下钻的，类别轴中的维度字段是可下钻主维度。这些图表如果还有系列、堆积等其它维度，那并非可下钻维度。

对于饼图类图表，维度是唯一的，圆饼的每一角可下钻。功能卡标题为“颜色”中的维度字段是可下钻主维度。

“可钻取到”功能卡中允许放入多个维度，它们将依次排列到可下钻主维度之后，形成下钻的路径。更直观地来看，初始时图表呈现的是可下钻主维度，“可钻取到”这些维度是不参与图表呈现的。点击图表下钻时，用“可钻取到”的第一个维度替换可下钻主维度；再次点击图表继续下钻时，用“可钻取到”的第二个维度替换第一个维度……（从技术原理上看，已下钻的维度会自动变成不可见的筛选器，使用时无须关注。）

## 2 设置

了解以上规则，设置就非常简单了。将希望向下钻取的维度拖入“可钻取到”功能卡就完成了设置。

值得注意的是，在所见即所得的设计器中，中间预览区的卡片本身就支持钻取操作。而钻取到某一层维度，这个状态是会跟随分析方案一起被保存。如果不希望这个状态成为阅览的初始状态，在发布之前，应该先上卷回到顶层。

另外，钻取可能意味着度量值范围的急剧变化。如果在图表属性设置中，定义了数轴的固定值范围，很可能会造成不良影响。还有“前N项”、调色板维度成员与颜色绑定等等之类的属性，同样如此。这一类的问题需要自行把握。

## 3 使用

### 3.1 下钻

下钻的操作，是直接点击图表的图形元素。

如果是柱图类图表，以柱形图为例，点击横轴分类对应的阴影块向下钻取。阴影块范围内，单系列时是一个柱子，多系列时是并排的多个柱子，堆积时则是多个色块堆起来的一个柱子。条形图同理。

如果是折线类图表，鼠标所在的横轴上的类别，出现的是一条阅读提示线，这相当于柱形图的阴影块，点击向下钻取。

如果是饼图类图表，鼠标划过的扇形，半径会稍微变大，有突出的视觉效果，点击向下钻取。

![[ee4bcfea1126e86bedef7772ec057bc4_MD5.png]]

不同图表的下钻

点击这些图形元素，可能存在操作歧义。例如同时定义了查看明细数据、同时定义了单据联查、仪表板中用到了联动等。这种情况在点击后会有菜单询问点击的目的，需要再选择一下“钻取到字段[……]”。

![[17dd7e596491b18c4d9fdf5d589dd5a8_MD5.png]]

有歧义时的询问菜单

### 3.2 上卷

下钻之后，图表左上角会有图标为向上箭头的按钮，以及已下钻维度成员组成的“面包屑”提示。这既是一路下钻的信息提示，也是上卷操作的按钮。

![[9a02b2ff7e54bc1c5c4c17d31aecd4f3_MD5.png]]

“面包屑”也称为“面包屑导航”。童话故事中，小女孩被坏人抓走了，她一路偷偷撒下面包屑，逃走后沿着这些标记找回了家。

如果仅下钻一层，点击上卷按钮后直接返回。如果已下钻多层，点击上卷按钮后会有菜单询问“返回到顶层”或者具体哪一层。

![[163057ccb1664d7fc557dbdfce9f93cc_MD5.png]]

上卷

早期的版本，上卷图标较小，面包屑每一段可分别点击，没有询问菜单。新的版本中，考虑到移动端更好的操作体验，图标更大以方便触摸，面包屑文字也统一成上卷按钮的一部分，多层下钻后由询问菜单确定返回目标。注意版本差异。

## 4 跨图表钻取？

是否可以跨图表钻取，比如从柱图下钻到饼图，从饼图下钻到列表？

数据斗方不支持跨图表钻取，柱图下钻还是柱图，饼图下钻还是饼图。但是通过仪表板的组合，可以实现跨图表下钻的效果。结合仪表板的图表联动、页签切换等特性来组合实现。而且只要支持联动的图表，就可以作为下钻的源头，一定程度上这也可以作为更多支持下钻的图表类型的补充。

可参考：

[仪表板/设计器/关键特性/跨图表钻取](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tq3r6eacj)

[仪表板/设计器/组件/数据斗方](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tea6hwxym#4)