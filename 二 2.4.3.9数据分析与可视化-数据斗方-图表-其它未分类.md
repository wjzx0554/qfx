# 2.4.3.9数据分析与可视化-数据斗方-图表-其它未分类

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E6%96%97%E6%96%B9/%E5%9B%BE%E8%A1%A8/%E5%85%B6%E5%AE%83%E6%9C%AA%E5%88%86%E7%B1%BB/content.html?timestamp=1775632829532

---

# 其它未分类

前文已对常用的图表，分类逐一介绍。

在众多的图表中，还有若干个不好归类的图表，它们是：雷达图、瀑布图、漏斗图。

## 1 雷达图

### 1.1 综述

雷达图可在极坐标上分类展示一组数值，或展示多组数值进行对比。

雷达图的底图是一个正多边形，正常呈现要求最少有三个顶点，形成正三角形。三到六个顶点都比较合适，过多的顶点则影响阅读。

正多边形每个顶点对应一个数值，数值的大小映射在中心到顶点的轴线位置上，描绘成数据点。相邻的数据点连线，形成不规则的多边形。这称为一组数值。

雷达图可以呈现一组数值，也可以两组（或三组）数值同时出现进行对比，三组以上同时出现则影响识别和理解。通过不规则多边形可以判读数据的偏向，通过两个不规则多边形的比较可以对比两组数值的偏向及差异。

![[acda351939eca03bc138a82736fe3d0e_MD5.png]] ![[fb399ddf1c2f43c30d13b28f6bd47ca0_MD5.png]]

雷达图一组数值与多组数值

### 1.2 图表的制作与呈现

从多维分析模型到图表的可视化呈现，雷达图有两种不同的“多边形模式”。一种是“维度值与顶点对应”，另一种是“度量字段与顶点对应”。

对于“维度值与顶点对应”的多边形模式，要求至少有一个维度字段。这个维度有几个维度成员，则形成几边形。度量字段是观察的目标数值，自然必不可少。其中，对于多组数值又有两种变化。一个度量字段是一组数值，多个度量字段就是多组数值，这是第一种变化。用第二个维度字段生成多组数值，是第二种变化。

对于“度量字段与顶点对应”的多边形模式，要求至少有三个度量字段。这种模式如果没有维度字段，则是一组数值；有一个维度字段，则这个维度有几个成员，就是几组数值。

|多边形模式|基本要求（一组数值）|更多字段（多组数值）|
|---|---|---|
|维度值与顶点对应|1个维度、1个度量|N个度量（建议N=2、N=3）|
|加1个（共2个）维度|
|度量字段与顶点对应|N个度量（建议3≤N≤6）|加1个（共1个）维度|

#### 1.2.1 图表的制作

**多边形模式一：维度值与顶点对应**

图表类型确定为“雷达图”，图表属性“多边形模式”确定为“维度值与顶点对应”。在“维度”功能卡中创建一个维度字段，在“度量”功能卡中创建一个度量字段。

![[f86d3cb09f65441dcccefb1d39f9fd24_MD5.png]]

制作雷达图：多边形模式一、一组数值

在上述1个维度、1个度量的基础上，再创建一个度量字段。则两个度量字段形成两组数值。

在上述1个维度、1个度量的基础上，再创建一个维度字段。则第二个维度字段的维度成员形成多组数值。

这很像柱形图的两种多系列的做法。

![[bd68bae70391dbfa94afc703d92d57e3_MD5.png]] ![[124ab8961dc8c799b6c45ead82742d80_MD5.png]]

制作雷达图：多边形模式一、多组数值

**多边形模式二：度量字段与顶点对应**

图表类型确定为“雷达图”，图表属性“多边形模式”确定为“度量字段与顶点对应”。在“度量”功能卡中创建多个度量字段。

![[d382caf3b2a39c012923af670550aef1_MD5.png]]

制作雷达图：多边形模式二、一组数值

在上述多个度量的基础上，再创建一个维度字段。则这个维度字段的维度成员形成多组数值。

![[7cc886471e75c01a9dd475af97d1e053_MD5.png]]

制作雷达图：多边形模式二、多组数值

#### 1.2.2 图表的呈现

雷达图的底图是正多边形，在其上呈现的每一组数值则是不规则多边形。由于每一个值对应的点需要与其相邻的值连线，以形成不规则多边形，如果存在空值，则会出现连线不能闭合，围不成多边形的情况。

在底图的正多边形上，从中心到每个顶点的半径，可以看成是一个数轴，与柱形图、折线图的数轴无异。这些数轴是统一单位长度的，因此它们的刻度也一致。相邻轴上的相同刻度连接起来（相当于直角坐标上数轴的网格线），就形成内圈一个个小的正多边形。

如上所述，多边形的多个轴是统一单位的。正因如此，不同单位的量不应该在同一个雷达图上呈现。除此以外，以考试成绩为例，如果有的科目满分100分，有的科目满分150分，也不适合按原始分数直接呈现。即使同样100分满分，有的科目90分是最高分，有的科目95分也只是中下等水平，同时比较也有失偏颇。或许应该按名次统一换算之后再出现在雷达图上，才更为科学。

雷达图的鼠标悬停信息，以每个顶点为目标。在底图正多边形的外接圆范围内，鼠标更靠近哪个半径，就显示该顶点的信息。

### 1.3 图表属性

#### 1.3.1 “雷达图”属性分类

**● 多边形模式**

上文图表的制作已明确了“多边形模式”是雷达图的关键属性。它的两个选项“维度值与顶点对应”、“度量字段与顶点对应”决定了维度、度量字段的呈现方式。不赘述。

#### 1.3.2 “绘图区”属性分类

**● 填充颜色**

图表上每组数值的不规则多边形，中间的面积是不是填充上颜色，受此开关属性影响。

多组数值的多个不规则多边形，相互之间会有重叠。因此，填充颜色是在描点和描边的基本颜色之上经过自动处理的半透明颜色。

**● 数据标签**

开启后可在每个数据点的附近，显示数字。标签数字格式，以度量字段的“数字格式”属性为准。

参考[数据分析与可视化/数据斗方/设计器之图表属性/数据标签](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tonkllqmm)。

**● 刻度值**

此开关控制正多边形底图的数轴刻度是否显示数值。

刻度值显示在六点钟方向。当底图的正多边形是奇数顶点时，尖朝上，六点钟方向是横向的网格线，刻度值居中显示在其上。当底图的正多边形是偶数顶点时，六点钟方向是数轴，刻度值显示在其一侧。

**● 调色板**

调色板属性，详见[数据分析与可视化/数据斗方/设计器之图表属性/调色板](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Thwwslr0o)。

与折线图一样，雷达图的调色板系统默认是6个颜色。通常2至3个颜色系列进行对比是恰当的，如果有更多颜色出现，合理性存疑。

#### 1.3.3 其它

“图例”属性分类，详见[数据分析与可视化/数据斗方/设计器之图表属性/分类：图例](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Trbqb82u9)。

“通用”属性分类，详见[数据分析与可视化/数据斗方/设计器之图表属性/分类：通用](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tipglthcp)。

## 2 瀑布图

### 2.1 综述

从视觉效果上看，瀑布图有点像柱形图。区别在于，柱形图的每一个柱子都是从横轴画起，而瀑布图的每一个条目的量是从上一个条目的量的结束位置开始，依次累加。看起来又像是将堆积柱形图一个柱子的每一个堆积部分错开成为不同条目。

这种连续的依次累加，除了可以表示正数，还可以表示负数。并且最终的累计结果作为汇总项，允许再单独呈现为像柱形图的一个柱子。

瀑布图典型的应用场景是表达企业的营业利润，各项营业收入（正数条目），减去各项营业成本及费用（负数条目），得到营业利润。套用到打工人的个人场景，大概就是：工资 + 补贴 + 奖金 - 社保 - 公积金 - 个税 = 实发工资。

数据斗方中的瀑布图，除了上述这种单维度的基本模式之外，还支持双维度模式。横轴上允许有两个维度，相当于多了一级分组。第一个维度的成员称为“条目”，第二个维度则称为“阶段”。每个阶段都有汇总，每个阶段的第一个条目在纵向上的起点都是从上一阶段的汇总值位置开始。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

瀑布图横轴上存在汇总项（数据项之外），还可能出现表示阶段的第二个维度。因此在仪表板的组件联动中，不适合作为联动源。当前文档中，图表分类上也没有将其纳入柱图类。

### 2.2 图表的制作与呈现

#### 2.2.1 图表的制作

**单维度模式**

图表类型确定为“瀑布图”，在“纵轴”功能卡中创建一个度量字段，在“横轴（条目）”功能卡中创建一个维度字段。这就是最基础的瀑布图。

在图表属性区，找到“单维度模式”分类中的“显示总计”属性项。默认此开关开启，即在图表中，最后会有一个总计项，且与前面的各条目颜色不同，即除正负条目之外的第三种颜色。若此开关关闭，则不显示这一总计项。

![[712aff1f552097b7d96e3ff02ad8ed68_MD5.png]] ![[9ebff1084d7964530ac297f7cb271142_MD5.png]]

制作瀑布图：单维度模式

**双维度模式**

在上述一个维度、一个度量的基础上，“横轴（阶段）”功能卡中再创建一个维度字段。则以此字段作为阶段（分组），每个阶段内是一个单维度模式的子瀑布。在双维度模式中，单维度时的条目总计项总是必须出现，而且名称不再是“总计”，而是显示为阶段维度字段的值。

![[0226b8fdebb2894ca0361fbbb9e3f8a1_MD5.png]]

制作瀑布图：双维度模式

在图表属性区，找到“双维度模式”分类中的“起始于”属性项。选择起始于“条目”，则图表从第一个阶段的第一个条目开始呈现；选择起始于“第一阶段汇总”，则第一个阶段中的条目将被隐藏，图表从第一个阶段的总计项开始呈现。

![[3b0e39eeab48e003f92312f8c1c8c09f_MD5.png]]

双维度模式的“起始于”属性比较

利用起始于“第一阶段汇总”的特性，构造数据时可以将此前的余额作为一个条目，并用阶段字段分类，然后图表上就能够隐藏起来成为累积的起点。

#### 2.2.2 图表的呈现

系统默认用绿色系表示条目中的正数，用红色系表示条目中的负数，总计或第二个维度的阶段合计则使用蓝色系。这也可以在调色板属性中自行修改。

每个条目的末端（正数是顶端、负数是底端），有浅灰色的横线连接到下一个条目的位置，成为下一个条目在纵向上的起点。“瀑布”的形容正是源于此。

图表在横轴、纵轴、鼠标悬停信息等其它方面，与柱形图的呈现较为相似，不赘述。

### 2.3 图表属性

#### 2.3.1 “绘图区”属性分类

**● 数据标签**

详见[数据分析与可视化/数据斗方/设计器之图表属性/数据标签](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tonkllqmm)。其中，正数显示在条目色块的顶端，负数显示在条目色块的底端。

**● 调色板**

瀑布图的调色板，总是三个颜色，分别称为“条目正值”、“条目负值”和“横轴（阶段）”。

系统默认以绿、红、蓝色系的变化分别与之对应。颜色已具有明确的语义，而且图形本身（这里特指根据条目之间的灰色横线）可以看出正、负值或者阶段汇总项。因此瀑布图没有图例，不像其它图表一样用图例来呈现字段名称或字段值。

#### 2.3.2 “纵轴”属性分类

瀑布图的“纵轴”是数轴，有“标题/单位”和“数字格式”的属性项。为保证各条目在不同数值区间的线性比例，没有“标尺比例”等其它数轴的属性项。参考[数据分析与可视化/数据斗方/设计器之图表属性/分类：数轴](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Trrrh3f93)。

#### 2.3.3 “数据”属性分类

**● 条目顺序**

条目顺序属性有“按名称”和“按度量”二个选项。

如果选择“按名称”，则以维度字段的值来决定顺序。此时并不一定正值在前、负值在后，而是看名称排序依据是什么。可参考[数据分析与可视化/分析工具总体介绍/备选字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tlmhf2wv7#4.4)的自定义顺序的说明。

![[09c8bf87ca83f8b7715e2e9ba4911f4f_MD5.png]]

瀑布图条目顺序按名称

如果选择“按度量”，则以条目的数值大小来决定顺序。此时正值在前、负值在后，从“大”到“小”排列。为什么这里“大”和“小”特别加了引号？对于正值来说大小明确，而对于负值来说则有两种变化。

**● 负值**

如上所述，如果“条目顺序”选择“按度量”，则负值从大到小有两种呈现方式。一种是数字本身的大小比较，负得少的数值比负得多的数值大；另一种是用绝对值做大小比较，效果相反。

因此，“负值”属性用“小块在前”和“大块在前”作为选项。大块、小块是绝对值，与可视化的直观效果一致，足够明确，无需多言。

![[1301e5c6548ef8d88cf985eaa4265329_MD5.png]]

瀑布图负值条目顺序

**● 正值前N项、负值前N项**

“正值前N项”和“负值前N项”很像饼图的“前N项”属性，如果条目很多，允许仅关注比重大的前若干项，而将后面比重小的多项合并为一项。

#### 2.3.4 “单维度模式”属性分类

**● 显示总计**

该属性只在单维度模式时生效，控制汇总项是否显示。前文[图表的制作](https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E6%96%97%E6%96%B9/%E5%9B%BE%E8%A1%A8/%E5%85%B6%E5%AE%83%E6%9C%AA%E5%88%86%E7%B1%BB/content.html?timestamp=1775632829532#2.2.1)已有明确阐述，不重复。

#### 2.3.5 “双维度模式”属性分类

**● 起始于**

该属性只在有表示阶段的第二个维度出现，即双维度模式时有意义。前文[图表的制作](https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E6%96%97%E6%96%B9/%E5%9B%BE%E8%A1%A8/%E5%85%B6%E5%AE%83%E6%9C%AA%E5%88%86%E7%B1%BB/content.html?timestamp=1775632829532#2.2.1)已有明确阐述，对比了“起始于条目”和“起始于第一阶段汇总”两个选项的两种效果，不重复。

#### 2.3.6 其它

“通用”属性分类，详见[数据分析与可视化/数据斗方/设计器之图表属性/分类：通用](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tipglthcp)。

## 3 漏斗图

### 3.1 综述

除了列表、业务指标图等文字类型的图表另当别论之外，几乎所有的图表都是在实现数据可视化，只有漏斗图不是。

将数据统计结果在视觉通道上进行映射，以图形化的方式呈现不同的量，这是[数据可视化的基本理论](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Teg3p7ovt#1)。所以，随着数据变化，柱子有高低（柱形图）、扇形角度有大小（饼图）、颜色有深浅（地图）等等。

漏斗图（这里特指数据斗方漏斗图的“整体梯形”外观）是个另类，数据怎么变化都不影响图形。它更象是一个倒梯形的图片作为底图，在上面显示几个数据标签。这一类的图表，不受统计数据的约束，是“非数据·可视化”的图表，也称为[“概念图”](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Teg3p7ovt#4)。漏斗图通过上大下小堆叠的梯形，表达了数据经过几个阶段层层筛选的隐喻。

### 3.2 图表的制作

图表类型确定为“漏斗图”，在“维度”功能卡中创建一个维度字段，在“度量”功能卡中创建一个度量字段。这样，维度字段的每个维度成员依次表示了一个漏斗层级。

![[43617d6ad47e1f83e9fe79acb1f976c3_MD5.png]]

制作漏斗图：维度成员表示多个漏斗层级

受数据表的结构的影响，可以有另外一种做法。“维度”功能卡保持空白没有维度字段，在“度量”功能卡中创建多个度量字段，则每一个度量字段表示了一个漏斗层级。这和柱图类多系列的两种制作方式是一样的。

![[27d058924e9b4c6a47b481664cab2408_MD5.png]]

制作漏斗图：每个度量字段表示一个漏斗层级

### 3.3 图表属性

#### 3.3.1 “绘图区”属性分类

**● 梯形外观**

梯形外观属性有“整体梯形”和“逐级梯形”二个选项。

“整体梯形”效果，多个漏斗层级是整齐的，图形整体是一个倒梯形。这就是如上文综述所说的，不管数据统计结果如何，图形都没有变化。

“逐级梯形”效果尝试颠覆这一现象。每个漏斗层级的上边长度是按度量值的比例呈现，这样每个漏斗层级自成一个梯形，而整体则是不规则的图形。

![[0e09c67ab7248d8e04e6156f35d9d194_MD5.png]]

漏斗图“整体梯形”与“逐级梯形”

**● 数据标签**

漏斗图的图形，更多只是起到隐喻的作用。因此也主要依赖数据标签来呈现数据。

数据标签按名称、数值和比率，分别有“名称标签位置”、“数值标签位置”、“比率标签位置”属性项，可以分别设置显示在左、中、右，或隐藏不显示。

**● 调色板**

漏斗图使用通用的离散值调色板，可参考[数据分析与可视化/数据斗方/设计器之图表属性/调色板](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Thwwslr0o)。

#### 3.3.2 “图例”属性分类

漏斗图通常直接在图形上开启名称标签，而不显示图例。图例位置属性保持“隐藏”即可。图例相关说明可参考[数据分析与可视化/数据斗方/设计器之图表属性/分类：图例](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Trbqb82u9)。

#### 3.3.3 “数据”属性分类

**● 排序**

数据的排序属性，决定漏斗层级从上到下的顺序。选项有“按名称”、“按度量从大到小”。

“按名称”时，与其它图表一样，名称值的顺序受字段排序依据的影响。可参考[数据分析与可视化/分析工具总体介绍/备选字段](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tlmhf2wv7#4.4)的自定义顺序的说明。

当选择了“按名称”，且梯形外观属性是“逐级梯形”时，这时候各个漏斗层级的形状就不一定是倒梯形了。一个层级的量可能比它的下一层级小，则这个层级的形状就是正梯形。而整体可能就是锯齿状的不规则图形。这样的表达很可能已经没有意义，还不如柱形图、条形图更为正确。

![[7745a4b3818226880df5323c3c358434_MD5.png]]

漏斗图的诡异呈现

**● 负数**

由于需要计算比例，应该避免出现正数、负数混杂的情况。这一点和饼图类图表的逻辑一样。

**● 比率计算方式**

比率计算方式有三个选项，分别是“逐级差异”、“占第一层百分比”、“占合计的百分比”。具体使用哪个选项，与需要表达的目标，以及数据表的结构紧密相关。

“逐级差异”是指从第二个层级开始，每一层级与它的上一层级比较。上一层级的量是分母，当前层级的量是分子。这表示了上一层级变化了多少，成为这一层级。选择“逐级差异”时，当比率标签位置单独（“单独”是指名称、数值不与其在同一位置）显示在左侧或右侧，会同时呈现从上一层级指向这一层级的箭头。

“占第一层百分比”是指每一层级与第一个层级比较。第一个层级的量是分母，各个层级的量是分子。这表示了从最开始的一个阶段，到后来各个阶段的留存率。

“占合计的百分比”是指每一层级与各个层级的总和比较。总和是分母，各个层级的量是分子。这表示了各个层级的占比。这种情形原始业务数据的记录方式与前两种明显不同，每一条记录应该有一个状态值，而状态字段就是图表的层级，每条记录非此即彼地属于某个层级。

![[7998e5782fae1a1d3ecab075553d010b_MD5.png]]

漏斗图比率计算方式

**● 比率格式**

该属性设置比率作为标签显示、以及出现在鼠标悬停信息中时的显示格式。通常用百分比。

#### 3.3.4 其它

“数据穿透”属性分类，详见[数据分析与可视化/数据斗方/设计器之图表属性/分类：数据穿透](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T5bam6crv)。

“通用”属性分类，详见[数据分析与可视化/数据斗方/设计器之图表属性/分类：通用](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tipglthcp)。