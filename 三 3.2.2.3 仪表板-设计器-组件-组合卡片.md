# 3.2.2.3 设计器-组件-组合卡片

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E4%BB%AA%E8%A1%A8%E6%9D%BF/%E8%AE%BE%E8%AE%A1%E5%99%A8/%E7%BB%84%E4%BB%B6/%E7%BB%84%E5%90%88%E5%8D%A1%E7%89%87/content.html?timestamp=1775634898115

---

# 组合卡片

组合卡片是一个提供卡片外观的容器。

在仪表板中，数据斗方等数据可视化内容组件，本身也是自带卡片外观的。但是，当把它们放进组合卡片中时，它们将不再单独显示卡片外观。这也正是“组合”的意义。

不应该使用画着卡片的图片作为整个页面的背景，那是未提供“卡片边框”属性时早期版本的临时方案。正确的做法是在“卡片边框”属性中设置边框样式或贴图。

## 1 卡片外观

### 1.1 整体外观

该属性提供了“页面统一”、“无”、“自定义”三个选项。

在页面组件中已经介绍，尽量在页面属性上设置全局的卡片外观，并在每一个卡片组件中选择“页面统一”，这样既减少重复工作，又保证整体一致。

当然不排除特殊的情况，例如选项“无”就可以让该卡片完全透明。又例如“自定义”选中后，则下方多出“卡片背景”和“卡片边框”两个属性，它们可以单独个性化设置。

![[8b0810ee4ad3624f83171ef821e434b5_MD5.png]] ![[ea6514d914d9086a128fbcc56c2fbf49_MD5.png]]

卡片外观

### 1.2 卡片背景

当卡片的“整体外观”属性选择“自定义”时，开放“卡片背景”属性，可对当前卡片的背景颜色做个性化定义。

卡片背景颜色的“页面统一”对应的全局设置项，被包含在页面组件“自定义外观风格”的“整体-卡片”分类中。

### 1.3 卡片边框

当卡片的“整体外观”属性选择“自定义”时，开放“卡片边框”属性，可对当前卡片的边框做个性化定义。有“页面统一”、“自定义”、“自定义（贴图）”三个选项。

卡片边框的“页面统一”对应的全局设置项，是页面组件的“卡片边框”属性。

“自定义”和“自定义（贴图）”，参考[仪表板/设计器/关键特性/卡片外观·之卡片边框](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tlwgav69v)。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

当“卡片边框”属性选择“自定义（贴图）”时，“卡片背景”颜色属性自动失效。

## 2 卡片标题栏

标题栏是指卡片顶部显示标题文字或者保留空白的部分，这一部分也是给右上角操作按钮预留空间。

![[fe1e3a12643063f321b19d0ee1455e24_MD5.png]]

卡片标题栏

### 2.1 标题文字、标题样式

“标题文字”如字面，毋须赘言。当设置了标题文字，下方自动出现“标题样式”属性项，可针对标题文字的字体、字号、颜色等进行个性化定义。

这些样式属性，“页面统一”对应的全局设置项，被包含在页面组件“自定义外观风格”的“整体-卡片”分类中。

### 2.2 标题栏图片

通过该属性项，可以在标题文字前添加一个小图标。特殊的，也可以通过调整图片与文字的位置关系，使图片成为整个标题栏的背景或文字的底图。

![[8566d0cdd5b689355cb59ed668811eb2_MD5.png]]

卡片标题栏图片

标题栏图片“页面统一”对应的全局设置项，是页面组件的“卡片标题栏”属性。参考[仪表板/设计器/关键特性/卡片外观·之卡片标题栏](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T9kcqgdz8)。

### 2.3 卡片说明

设置该属性的效果，是当鼠标划过卡片时，在标题文字后出现一个问号小图标。在该属性填入的简短文字，将作为“问号”的鼠标悬停信息。这是为阅览者提供的针对卡片内容的释疑。

![[bf7620b5a62d53f2c44703b0a833f8d7_MD5.png]]

卡片说明

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

以上2.1~2.3的属性，在数据斗方、数据分析等自带卡片外观的组件中是完全一致的。以下的2.4则是组合卡片容器特有的。

### 2.4 全屏

该属性用于为组合卡片容器开启位于右上角的“全屏”按钮。并且可以在开启后让容器中的图表不再单独允许全屏。所以，选项有“关闭”、“开启且下级组件禁止”、“开启且下级组件允许”三项。

在移动端中，全屏按钮总是显示在界面上，而多个业务指标图的数据斗方放置在组合卡片中又是常见的移动端风格。让组合卡片的全屏“开启且下级组件禁止”，可以使杂乱的视觉效果得以改善。

![[ee0901eec858ed24224033618ecbd402_MD5.png]]

组合卡片全屏按钮设置