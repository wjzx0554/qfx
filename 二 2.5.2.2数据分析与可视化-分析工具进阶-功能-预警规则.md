# 2.5.2.2分析工具进阶-功能-预警规则

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E8%BF%9B%E9%98%B6/%E5%8A%9F%E8%83%BD/%E9%A2%84%E8%AD%A6%E8%A7%84%E5%88%99/content.html?timestamp=1775633215531

---

# 预警规则

当简单地提及“预警”时，可能存在两种不同的期望。一种是当数据达到一定条件时，改变界面上的显示；另一种和定时推送相关，定时推送触发时先进行判断，满足条件才发生推送。

对于前者，从实现原理上称为“条件样式”，数据斗方的某些图表支持这样的设置。例如业务指标图，当指标值达到预期时显示为绿色，未达到时显示为红色。这部分内容不是当前要讨论的，具体可参考[公式系统/条件样式](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tbgymvp4i)。

对于后者，也称为“预警推送”，是接下来要讨论的“预警规则”的使用场景。

## 1 定义预警规则

预警规则是基于当前分析方案的分析模型来设置预警条件的。分析模型经过执行生成立方体，因此预警条件的判断，也就是在立方体上进行。

预警规则和输出参数一样，都可以看作是从立方体上拎出部分数据来，所以设置入口在工具栏“摘要”菜单中。“数据分析”工具、“数据斗方”设计器是一样的。另外，在使用预警规则的地方——推送的设置界面上，也有机会调起预警规则设置界面。

![[127dc38ceadd9871301abe92bc32beb9_MD5.png]]

预警规则定义入口

### 1.1 添加规则

进入设置界面，左侧列表自动选中“当前模型”节点，中间明确显现了当前模型有哪些维度、哪些度量，这是构成立方体的基本信息。将要设置的预警条件，必须基于这些分析字段来进行判断，所以对此应该先有概念。

![[f0eb54c5bbdfb6eb01b7b06c7f207ccd_MD5.png]]

预警规则设置前

点击左侧列表中的“+ 添加预警规则”，创建了一条新的规则，系统自动选中新规则并切换为定义界面。填写上名称（限字母、数字、下划线）、描述，设置好预警条件。

![[299737edfadd3976aac29d5be3836dd6_MD5.png]]

预警规则设置中

右下角“尝试执行以查看结果”按钮用来预览设置的效果。立方体模型经过预警条件的过滤之后，仍有数据存在，视为匹配规则，判断结果显示为“是”，否则显示为“否”。

![[3c14ef2c9bf5d326216c242c114670bf_MD5.png]]

预览结果

### 1.2 预警条件

预警条件的设置，类似于通用过滤条件的界面。“字段”栏选择当前模型的维度或度量；根据数据类型，“比较”栏的选项可能略有差异，比如文本型的维度就没有大于、小于比较符；“值”栏填上比较的值即可。

其中，如果比较符选择为“自定义”，则可以写一些简单的脚本来判断。脚本语法与计算字段相同，这里必须以返回布尔值为目标。在脚本中通用的函数都可以使用，另外有`VALUE`变量表示字段的值。但是多数时候一行自定义脚本的效果，也可以拆解成多行预警条件的逻辑组合，这样效率更高。例如，设置一行预警条件，[度量] 自定义 `AND(VALUE>=400,VALUE<=500)`，其实应该拆成设置两行预警条件：[度量] 大于等于 400 并且，[度量] 小于等于 500。

### 1.3 后处理脚本

大多数情况下，不需要写后处理脚本。

后处理脚本用来判断立方体模型经过预警条件的过滤之后，有多少行剩下，以决定是不是匹配规则。脚本语法与计算字段相同，这里以返回布尔值为目标。主要有rowsCount()函数可用。

默认情况下，不写后处理脚本，相当于进行了`rowsCount()>0`的判断。仅当需要判断有多行符合条件的结果时，才考虑用后处理脚本。

### 1.4 删除规则

预警规则设置界面，左侧列表，当鼠标移动到已添加的规则上时，该条目右侧的“删除”按钮可以将其移除掉。

所有对规则的添加、编辑、删除，当预警规则窗口“确定”后才被保留，窗口“取消”则放弃当次弹窗的全部改动。

## 2 使用预警规则

将定义好的预警规则使用到定时推送配置中，就是预警推送。

在邮件定时推送、移动端定时推送等设置过程中，有“预警规则”设置项，选用一条预警规则。每当定时调度触发时，先进行计算并校验规则，匹配规则才进行推送。

![[c05d9e8fdf9902c7ad676973e2f3f53e_MD5.png]]

使用预警规则

定时推送适用于发送月报、周报、日报等，如果过程中重点是为了关注某些事情的发生，则非常适合使用预警功能。设想如果没有重要事情发生，天天收到消息，很容易产生麻痹，真有事情发生可能就会忽略了。使用预警推送，就可以保证有关注的事情发生时才收到消息。

虽然称为预警，但也不总是和坏事有关，这还得结合具体业务的使用来看。销售额超过预期的额度，就是一份喜报。所以本质上称为条件推送可能更为精准。

需要避免一个理解上的偏差，预警推送不是“一旦有事发生就发消息”。从应用场景上看，它是结合定时推送来实现的。先有定时触发，然后才是预警规则判断，之后才决定是不是发消息。推送频率决定了多久进行一次判断。过密的推送频率设置也是不合适的，因此，随时监控事件发生是其它领域要解决的问题，不是预警推送的职责。