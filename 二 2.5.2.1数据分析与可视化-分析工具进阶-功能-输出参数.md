# 2.5.2.1分析工具进阶-功能-输出参数

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E8%BF%9B%E9%98%B6/%E5%8A%9F%E8%83%BD/%E8%BE%93%E5%87%BA%E5%8F%82%E6%95%B0/content.html?timestamp=1775633169690

---

# 输出参数

输出参数是当前分析方案的分析模型在执行结果上的再加工。分析模型经过执行生成了立方体，在立方体上进一步挑选出简要的信息，以“输出参数”的形式供外部引用。

上述“进一步挑选出”的描述，就是输出参数的定义。“供外部引用”就是输出参数的使用场景，是指仪表板中的文字组件在文字内容中用到了带输出参数名称的变量。

## 1 定义输出参数

输出参数和预警规则一样，都可以看作是从立方体上拎出部分数据来，所以设置入口在工具栏“摘要”菜单中。“数据分析”工具、“数据斗方”设计器是一样的。

![[7c2cc088f4e0a478daffc0b52f4b52d3_MD5.png]]

输出参数定义入口

### 1.1 添加输出参数

进入设置界面，左侧列表自动选中“当前模型”节点，中间明确显现了当前模型有哪些维度、哪些度量，这是构成立方体的基本信息。将要进行的设置，必须基于这些分析字段，所以对此应该先有概念。

![[8b2ca021f469dda5011d01c28b7b21ff_MD5.png]]

输出参数设置前

点击左侧列表中的“+ 添加输出参数”，创建一条新的条目，对应了一个输出参数。系统自动选中新条目并切换为定义界面。填写上名称（限字母、数字、下划线）、描述。在“计算结果定位到”表格中，按维度、度量字段分别详细设置。然后选择“输出字段”。必要时用“后处理脚本 ”拼接一下多行多列的信息。

![[4acee9c0ca40ca953e9f690c16708609_MD5.png]]

输出参数设置中

右下角“尝试执行以查看结果”按钮用来预览设置的效果。立方体模型经过上述的定位，可以看到更为概要的结果，这些信息拼装出来的“显示文字”，就是输出参数的内容。

![[8b511dbcccbcc4f39ab0f7d52f136b76_MD5.png]]

预览结果

### 1.2 挑选行

将立方体看成一个二维的表格，维度、度量字段就是列，立方体的单元即每个维度成员组合就是一行。设置界面上“计算结果定位到”，其实就是在挑选行。

![[da140dcb27e88421b1a1c1a2e543139d_MD5.png]]

计算结果定位到

如果字段不予关注，“定位方式”就是“全部”，不需要详细设置。如果关注某个度量字段，希望挑出统计值最大/最小的前几项，则“定位方式”选择“最大前几名”或“最小前几名”，后面输入一个数字表示前几名。如果某个维度、度量字段“定位方式”选择“筛选条件”，则后面可以写一段判断脚本，挑出具体值对应的行来。

挑选行的时候，筛选条件脚本的语法与计算字段相同，这里必须以返回布尔值为目标。在脚本中通用的函数都可以使用，另外有`VALUE`变量表示字段的值。

![[e6a2f6579be3e613f0e2859574e4de6a_MD5.png]]

筛选条件脚本

多个度量字段存在时只能有一个度量字段设置前几名，多余的设置无效。当设置了前几名时，预览界面上会多出一列“排名”，可以辅助看到排名结果，度量值相同时会并列排名。

如果以度量值前几名的方式做了定位，挑选出来的结果已经自动排好序，无需再设置“排序依据”。否则，当挑选出来的结果有多行时，可以通过下面“排序依据”的设置来确定顺序。

![[a9f6932155cac37f6cdc04621b3c7996_MD5.png]]

多行结果排序

### 1.3 挑选列

维度、度量天然就是字段形式的存在，因此挑选列比挑选行要好理解得多。设置界面上称为“输出字段”的设置项，就是在挑选列。

哪些分析字段需要在最终输出参数的文字内容中显示出来，是输出字段要定义清楚的，这是一个必要的步骤。

而且，输出字段的顺序也很重要，后面的“后处理脚本”将会用序号来表示列。

![[901c18003078a4c80c0690bf5581bda4_MD5.png]]

输出字段

### 1.4 后处理脚本

输出参数的目标是产生一小段文字用于显示。当挑选出来的结果有多行或者多列时，怎样组成显示文字，是后处理脚本要解决的问题。

后处理脚本的基本语法与计算字段相同，以返回字符串为目标。这个场景下有`eachRow()`、`topRows()`、`rowsCount()`等函数可以使用，中间的参数还可以使用`rankTop()`、`rank()`等函数表示前几名、第几名。参考界面上的语法示例可以举一反三。

![[df9d56e8e6fcd4f15523ebf934fe76d7_MD5.png]]

后处理脚本

这里以eachRow()为例再稍做解释。假如有[地区]维度（维度成员有“东北”、“华东”、“西南”等），已设置按某度量值定位前3名，已设置[地区]作为输出字段且排在第1个。后处理脚本写上`"前三名是：" + eachRow("{1}地区", " | ")`，预览看到的显示文字可能是“前三名是：东北地区 | 华北地区 | 西北地区”。

可见，后处理脚本中用了加号在拼接字符串。其中的eachRow()函数是将每一行拼起来的意思。它有两个字符串类型的参数，第一个参数是显示模式，当中花括号里面数字1表示了第一个输出字段，这就是上面说到输出字段的顺序很重要的原因；第二个参数是分隔符，预览结果中两个竖线符号把三个值分隔开了。

### 1.5 删除输出参数

输出参数的设置界面，左侧列表，当鼠标移动到已添加的条目上时，该条目右侧的“删除”按钮可以将其移除掉。

所有对输出参数的添加、编辑、删除，当窗口“确定”后才生效，窗口“取消”则放弃当次弹窗的全部改动。

![[b0ed9ce3dbd944db082ae493e8d2f0b9_MD5.png]]

删除输出参数

## 2 使用输出参数

在仪表板中，文字组件、多行文字组件在编辑文字内容时，可以插入变量。其中有一种变量叫“可视化内容的输出参数”，就是仪表板中已创建的数据斗方组件、数据分析组件提供的输出参数。

在仪表板相关章节[仪表板/设计器/关键特性/图表的摘要信息–来自“输出参数”](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T0ab434ih)中，有定义输出参数，以及使用输出参数的完整示例。

![[4205db99f3ed44fe4ec7ee01ea457a50_MD5.png]]

在仪表板的多行文字组件中引用输出参数

![[da0dd2479b2f3e80e1b0ebc14338da2b_MD5.png]]

文字内容中插入了变量

![[8645fe7d665c1835ccb65fb384ce1233_MD5.png]]

仪表板中的图表摘要信息