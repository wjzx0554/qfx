# 2.3.6.1数据分析-其它功能-颜色

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90%E4%B8%8E%E5%8F%AF%E8%A7%86%E5%8C%96/%E6%95%B0%E6%8D%AE%E5%88%86%E6%9E%90/%E5%85%B6%E5%AE%83%E5%8A%9F%E8%83%BD/%E9%A2%9C%E8%89%B2/content.html?timestamp=1775613352227

---

# 颜色

在此前的章节[数据分析与可视化/数据分析/可视化视图及其制作/图表](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T2ddlfnkf#1)中，已对“视觉设置”有过介绍。在“数据分析”工具中，按照不同的图表类型，将分析字段进行视觉设置，是数据与视觉通道映射的方式。

颜色是最为常见的可视化表现。在视觉通道映射中，颜色又按定性映射，表现为离散的色值，这是维度成员与颜色映射的结果；按定量映射，表现为连续的渐变色，这是度量值与颜色映射的结果。这些在[数据分析与可视化/基础知识/数据可视化](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Teg3p7ovt#1)已有阐述。

## 1 离散颜色

“数据分析”工具暂未提供离散颜色的自定义功能。调色板是系统默认的色值，其中影响柱形条形图、饼图等图表的基本调色板有十种颜色；影响折线图、散点图等图表，色值需要强对比的调色板有六种颜色。

“数据分析”工具用于交互式分析，而更多的图表、大屏等可视化呈现能力，由数据斗方的图表承载。数据斗方的调色板属性，以及仪表板统一的外观风格属性，对离散颜色都可以自定义设置。

## 2 连续颜色

### 2.1 自定义设置

当度量字段在视觉设置上作为颜色的时候，数值映射成连续的渐变色。该度量字段的设置菜单上，有“颜色”设置项，可以对渐变色进行自定义设置。

![[8bd1910b8d069210ca74a8b0535c577b_MD5.png]]

分析字段的设置菜单

在颜色的设置界面上，从“系统默认”切换为“自定义”。

渐变模式有三种，按数轴方向，颜色由浅变深，是“正向单色渐变”；按数轴方向，颜色由深变浅，是“负向单色渐变”；先一个颜色由深变浅，再另一个颜色由浅变深，则是“正负向双色渐变”。

![[53747e66389bfee86cc052cb1e360a2a_MD5.png]] ![[5c0db4a0fa6688ccad5b4967771bb2af_MD5.png]] ![[f3d60d27e0ae7a576258205678fb1ac9_MD5.png]]

三种渐变模式

在渐变色预览条上方的颜色控件，可以选择期望的颜色。预览条下方的属性，下面分别说明。

![[2553e84ebabe0adafd32da662a3e204d_MD5.png]]

选颜色

**● 指定数据最小/最大值**

当未指定最小、最大值时，实际统计结果的数值范围，与颜色渐变的两端对齐。对于单次的查看，这可能没什么问题。但是对于多次的查看，比如切换筛选条件，先看一个地区、再看另一个地区，这时实际统计结果的数值范围会有变化，相同色值比如深绿色，可能一会儿表示1万，一会儿表示10万。这就让人无所适从了。

对于这种情况，如果可以预期到数值的范围，应该填上最小、最大值。这让多次的查阅，相同的颜色表示相同的数值，既避免误解，又便于比较。

**● 指定数据中心点** （仅双色渐变模式有）

经常是在从负到正的数值范围内，以零为中心，这样负数可以对应成一端的颜色，正数则对应了另一端。另一个例子，日标准工作时长8小时，小于8的对应一端的颜色，大于8的对应另一端，这个例子说明中心点未必总是零。

**● 使用完整颜色范围** （仅双色渐变模式有）

由于中心点的存在，数值范围两头到中心点的距离可能不等长。距离较长的一头对应了所在一端的全部颜色范围，距离较短的一头按比例只能对应部分颜色范围。如果勾上该属性，则距离较短的一头也对应上全部颜色范围。这样，虽然两头映射的比例不一致，但视图上两端最深的颜色都存在。

**● 分段**

分段是将渐变的斜坡变成台阶。数值范围映射成渐变颜色，对于人来说，在观察的当时大致判断深和浅没有问题，但记忆就比较困难。分段是将连续的变化转换成“大、小”或“高、中、低”等几档，虽然降低了精确性，但是颜色的个数少了，更有利于记忆。所以通常不应该分很多段，分段分得多了可能就失去了分段的意义。

![[5896c601857a6fc65e11347037f4e7be_MD5.png]]

分段

### 2.2 影响范围

当图表类型为表格、热力图、树图等图表时，可以将度量字段设置为颜色。

在视觉通道映射时，立方体模型的每个计算单元内有一个数值，映射成一个色值。因此，在可视化的表现上，各个色值影响的范围，也就是立方体模型中色值所在的那个单元。这对于图表来说，通常都很自然。但是对于表格来说，当有多个不同的度量字段存在时，同属于一个立方体单元的多个值，在视图上就会是同一颜色，使用不恰当时会很奇怪。

![[9ad8bd35dd94e1ac43bb82d46e8e9cb8_MD5.png]]

颜色影响维度成员相同的立方体单元（一）

![[974dd3f869acffbf877573f71c37bed5_MD5.png]]

颜色影响维度成员相同的立方体单元（二）

![[79fb69fc9c91c14f5cbd047598213f45_MD5.png]]

颜色影响维度成员相同的立方体单元（三）

### 2.3 负数红色

在会计或金融相关领域，在表格上显示数值，常需要以红色来表示负数。可以通过自定义连续颜色来达到这样的效果。

重复创建两个相同的度量字段，一个显示为数字，另一个设置为颜色。设置颜色字段的“颜色”属性。切换为“自定义”，将左侧颜色改为红色（#FF0000），将右侧颜色改为灰黑色（#333333）。“指定数据中心点”为零，“分段”为2。这样设置之后，正数和零显示为灰黑色，负数显示为红色。

![[4b5d58a3dcebdd14c988de79fad057b4_MD5.png]]

设置负数红色

接下来处理一下图例。一种做法是保留图例的显示，通过自定义的数字格式，将图例上的数字改成“负”和“正”（参考[数据分析与可视化/分析工具进阶/技巧/数字格式](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T39csl6ot#2)）。另一种做法更为简单，“图表设置”中反勾选“显示图例”直接隐藏。

![[1ab928fb3fe45179dc927f3265d35296_MD5.png]] ![[8b0484c560b058125b56a98aad2a82a7_MD5.png]]

图例的两种推荐处理方式

应该注意到上一小节所讲的影响。如果同时显示多个度量字段，无法做到颜色只影响其中一个字段。这又回到了[适用性](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tnki6bckh)的问题上，作为数据分析与可视化的工具，当成报表开发工具使用，总会在哪里出点问题。