# 3.2.2.7 设计器-组件-文字

**原文链接:** https://qing.kingdee.com/support/markDownFile/qing-analysis/%E4%BB%AA%E8%A1%A8%E6%9D%BF/%E8%AE%BE%E8%AE%A1%E5%99%A8/%E7%BB%84%E4%BB%B6/%E6%96%87%E5%AD%97/content.html?timestamp=1775635062699

---

# 文字

区别于“多行文字”组件，“文字”组件也可称为“单行文字”。它是不换行的、不出滚动条的文本显示区域。

文字组件最常用于作为仪表板的大标题。而卡片小标题通常不需要用到文字组件，那是因为卡片本身自带了标题文字属性。

文字组件在设计区中，会有一个灰色的方框提示组件所在的区域（文字、多行文字、图片、页签等组件都有）。这只是一个设计器的提示信息，预览或发布之后将不存在。

![[21ffc8a04e8ed979eaa29b29a07ca532_MD5.png]]

文字的编辑窗口

## 1 文字内容包含变量

文字内容中可以插入变量，包括“联动参数”和“可视化内容的输出参数”。

### 1.1 联动参数

“联动参数”是其它组件设置联动“作用于”该文字组件时填上的参数名称，在该文字组件内容中引用，则源组件的选中状态将显示在该文字组件上。

 **示例一：**

（1）新建仪表板，创建“按钮组1”，备选值自定义。自定义的“值”和“显示值”分别填上：apple，苹果；banana，香蕉；cherry，樱桃。

（2）创建“文字1”，暂不填写文字内容。

（3）设置“按钮组1”联动作用于“文字1”，参数名称填上`fruit`。

（4）重新编辑“文字1”的文字内容，填上“您选择了：”，在文字弹窗的菜单栏，选择“插入变量”-“在光标位置插入联动参数”-“${#fruit}$”，使得文字内容为`您选择了：${#fruit}$`。

（5）完成以上设置之后，从按钮组选择不同的选项查看效果。例如选择“香蕉”，则文字组件上显示“您选择了：香蕉”。

![[7245626a30ab17560dd4e2a101f7456c_MD5.png]] ![[c0ff5526afcbd07f82b17d111e66eaa6_MD5.png]]

示例一：在文字组件中使用联动参数

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

从以上示例可以看出，文字组件的联动参数呈现的是“显示值”。

### 1.2 可视化内容的输出参数

“可视化内容的输出参数”，是指数据分析/数据斗方组件将计算结果做进一步的加工，挑选出一些维度或者度量拼接成文字，定义为“输出参数”。

这些输出参数可以被文字组件以变量的形式引用，在仪表板上形成图表的摘要信息。具体可参考[仪表板/设计器/关键特性/图表的摘要信息–来自“输出参数”](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=T0ab434ih)。

## 2 文字内容包含链接

文字内容中还可以插入链接，其中包括了“页签切换”链接，这相当于可以将文字组件作为页签导航按钮。

 **示例二：** 假设已创建了“页签控件1”并携带“页签1”和“页签2”。“页签1”中有数据斗方已设置了联动页签切换到“页签控件1”的第2个页签（即“页签2”）。现在要在“页签2”中做一个返回链接。

（1）在“页签2”中创建“文字1”，文字弹窗的菜单栏，选择“插入链接”-“页签切换”-“添加新的定义…”。

（2）在“页签切换的定义”弹窗中，显示文字填上“返回”，添加目标组件，选择“页签控件1”的第1个页签。完成后文字内容中已自动插入“${link1}$”。

![[66ec4ea88e472574b8fc0a64c7c9c8a1_MD5.png]] ![[961b52bcd5cbe6c51ba3a04580233a4f_MD5.png]]

示例二：用文字组件控制页签切换

（3）完成文字内容的编辑，“文字1”组件显示“返回”。并且鼠标移动到文字上时，出现下划线，鼠标提示也变成手指形状。此时点击文字可以切换到“页签1”。

![[c334ad08d2ed5e4bebadee85c6960cad_MD5.png]]

以上示例，也可以在文字编辑界面先输入“返回”，鼠标拖拽选择这两个字，再弹出“页签切换的定义”窗口，则选中的文字可以带入设置弹窗。

细心的读者可能已经发现，这个示例的整体效果，实现了跨图表的钻取（另参考[仪表板设计器/关键特性/跨图表钻取](https://qing.kingdee.com/support/index.html?product=qing-analysis&docId=Tq3r6eacj)）。并且，其中“东北各省比例”的标题，用到了上文示例一的联动参数。

## 3 走马灯

走马灯是一种展现效果，文字内容从文字组件矩形区域的右侧开始，向左侧自动缓慢滑动，直至内容全部滑出区域。周而复始。

在启用走马灯效果时，当鼠标移动到文字区域上方时，文字滑动停止；鼠标移开后，文字滑动继续。

应该意识到，走马灯对于快速阅读并不友好。因为阅览者看到时往往是文字的中间，需要等待从头开始，需要等待滑动效果慢慢看完。建议只在不得已，或仅为了点缀效果时谨慎开启。

![[51926667cd823d505654804907e60ea1_MD5.gif]]

走马灯

## 4 外观

外观属性中的“颜色”，默认值是“页面统一”。“页面统一”对应了页面组件“自定义外观风格”的“整体-文字”分类的“文字颜色”属性。

外观属性中的“对齐”，当启用走马灯后，没有意义。

其它属性可直观理解，不详述。